# PXL End-to-End Audit Findings

Audit date: 2026-07-01

Scope reviewed: build scripts, TypeScript build/lint, Supabase migrations, RLS/auth model, company setup, master data dependencies, sales invoice/receipt flow, vendor bill/payment voucher flow, posting RPCs, trial balance, AR/AP ledgers, VAT/EWT/2307 compliance surfaces, and user flow from an accountant/auditor perspective.

Build/test evidence:

- `npm run build` passed.
- `npm run lint` passed with warnings, including hook dependency warnings and suspicious unused-expression warnings.
- No automated test script is defined in `package.json`.
- No local Supabase instance/seed data was available in this workspace, so the critical flow was traced statically through UI code and SQL RPCs, not executed against a live database.
- `PXL_TRANSACTION_MATRIX.md` is now a required living architecture artifact. Any transaction code change or fix session must keep it synchronized with this audit file and the codebase.

## Findings Status Index

Authoritative status for every finding — agents and readers should scan this instead of the full rows below. The detailed rows and Fix Session Log are evidence; if a status here disagrees with a row below, that is a bug (`scripts/check_docs_consistency.sh` enforces agreement in CI).

Status values: `Open` (not started), `In Progress` (scoped fixes landed, work remains), `Retested Passed` (fixed with executed evidence).

| ID | Severity | Status | Next Action |
| -- | -------- | ------ | ----------- |
| PXL-AUD-001 | Critical | Retested Passed | Follow-on module test coverage (CM/DM, inventory, assets) as normal backlog. |
| PXL-AUD-002 | Critical | Retested Passed | Broader numbered-document preflights continue under PXL-AUD-016. |
| PXL-AUD-003 | Critical | Retested Passed | None. |
| PXL-AUD-004 | High | Retested Passed | None (finer-grained named roles map onto `fn_can_perform` later if requested). |
| PXL-AUD-005 | Critical | Retested Passed | Closed with PXL-DA-011 (session 43): generic status-aware guards cover every transactional line table and header business fields; IMMUT-001 executed. |
| PXL-AUD-006 | Critical | Retested Passed | Server-side VAT computation/report standardization continues under PXL-DA-008/PXL-AUD-014. |
| PXL-AUD-007 | Critical | Retested Passed | Closed with PXL-DA-009: QAP snapshots now preserve supplier+ATC+rate rows and carry Form 2307 reconciliation evidence. |
| PXL-AUD-008 | High | In Progress | Seeded supplier/customer default-flow tests; richer withholding profiles. |
| PXL-AUD-009 | High | In Progress | Seeded negative DB tests (missing account, bad VAT code, direct transitions). |
| PXL-AUD-010 | High | In Progress | Seeded negative DB tests; supplier TIN required when withholding. |
| PXL-AUD-011 | High | Retested Passed | Ledger view as-of standardization continues under PXL-DA-013. |
| PXL-AUD-012 | High | Retested Passed | Ledger view as-of standardization continues under PXL-DA-013. |
| PXL-AUD-013 | High | Retested Passed | TB modes (unadjusted/adjusted/post-closing) drive off `entry_class`; closed with PXL-DA-014 (test 040). |
| PXL-AUD-014 | High | Retested Passed | Closed: VAT review/returns are ledger-backed, all VAT document families write per-code detail, exports snapshot, and application mutations are RPC-only. VAT-code effective dating remains under PXL-DA-010. |
| PXL-AUD-015 | High | Retested Passed | Closed with PXL-DA-009/PXL-DA-015: active non-superseded 2307 lines reconcile to QAP rows; exported snapshots remain immutable/versioned. |
| PXL-AUD-016 | High | Retested Passed | Number-series preflight now covers every numbered transaction page; default-series seeding tracked as a backlog enhancement. |
| PXL-AUD-017 | Medium | Retested Passed | Fixed (session 44): context IDs persist in localStorage and are validated against the RLS-scoped selector lists on load. |
| PXL-AUD-018 | Medium | Retested Passed | Lint is fully clean (session 46): 0 warnings, exit 0. Genuine defects fixed (dashboard refetch loop, ternary statements, unused var); the benign loader idiom carries documented per-line suppressions, as the finding's expected behavior allows. |
| PXL-AUD-019 | High | Retested Passed | None. |
| PXL-AUD-020 | High | Retested Passed | None (reversal controls delivered under PXL-AUD-021). |
| PXL-AUD-021 | High | Retested Passed | None. |
| PXL-AUD-022 | Critical | Retested Passed | Optional: diff remote schema against migrations (drift check). |
| PXL-AUD-023 | Critical | Retested Passed | None. |
| PXL-AUD-024 | Critical | Retested Passed | None. |
| PXL-AUD-025 | Medium | Retested Passed | None. |
| PXL-AUD-026 | Critical | Retested Passed | Optional: diff remote grant posture vs legacy auto-expose defaults. |
| PXL-AUD-027 | Critical | Retested Passed | Apply the same helper if CM/DM/VC posted-state cancellation is ever added. |
| PXL-AUD-028 | Critical | Retested Passed | Add a CM/DM-style UI regression test for the cash sale page payload if the page changes shape. |
| PXL-AUD-029 | Medium | Retested Passed | Fixed (session 42): `AppShell` resolves feature keys via the `ref_feature_definitions` embed; default-open semantics preserved. Typed Supabase client now fails the build on this defect class. |
| PXL-AUD-030 | High | Retested Passed | Fixed (session 42): 8 pages issued queries against non-existent columns (runtime-dead reference fetches on Quotations/SO/DR/Customer Returns/Cash Sales/Asset Acquisition/Warehouse Stock Settings; dead SLP register tab). Repaired via column aliases/embeds; generated database types (`npm run gen:types`) + typed client now catch drift at compile time. |
| PXL-DA-001 | Critical | Retested Passed | Exact rollback preview is complete for saved posting sources; atomic create-and-post forms retain a clearly labeled client draft estimate. |
| PXL-DA-002 | Critical | Retested Passed | Closed: governed source/JE/GL routes now cover financial, subledger, VAT/WHT, 2307, and immutable snapshot report families. |
| PXL-DA-003 | Critical | Retested Passed | Approval SoD (approver ≠ creator when a workflow is configured) continues under PXL-DA-012. |
| PXL-DA-004 | Critical | Retested Passed | Closed: common create/add/finalize, source-lock, tax, reversal, and audit protocol governs core writers and compatibility-wrapped secondary writers. |
| PXL-DA-005 | High | Retested Passed | Closed: normal-trigger orphan and cross-company JE rejection executed directly in test 026 (2026-07-12) alongside the existing fail-closed trace readers. |
| PXL-DA-006 | High | Retested Passed | Governed source-type table + JE foreign key cover every current writer; fresh replay and registry tests pass. |
| PXL-DA-007 | High | Retested Passed | Closed: genuine two-database-session same-source posting race executed in test 029 (2026-07-12) with exactly one surviving JE/tax set plus sequential idempotence and uniqueness backstops. |
| PXL-DA-008 | Critical | Retested Passed | Closed: server-computed VAT RPCs are the only application mutation path and reconcile document, tax ledger, GL controls, and ledger-backed reports. |
| PXL-DA-009 | Critical | Retested Passed | Fixed (session 79, `20260713000006`): QAP snapshots group by supplier + ATC + nature + rate, include a Form 2307 tie-out payload, and the QAP/SAWT pages download from the immutable snapshot payload. WHT-EXPORT-SNAP-001 extended to 19 assertions. |
| PXL-DA-010 | High | Retested Passed | Closed (session 85, `20260713000012`): VAT/percentage-tax codes now carry the same effective-date/version governance as ATCs — successive effective-dated versions per official code, overlap/successor integrity, immutability of rate/identity/effective-start once used, delete protection, and an as-of resolver; TAX-CODE-VERSION-001 executed. Optional admin successor-management UI on `TaxSetupPage` is a backlog enhancement, not a defect. |
| PXL-DA-011 | Critical | Retested Passed | Delivered session 43 (`20260704000002_status_immutability.sql`): generic line/header guards on all transactional tables; IMMUT-001 executed. Residues: `tax_detail_entries` direct-write posture stays under tax-ledger work; CM/DM totals remain lifecycle-mutable for the apply RPC. |
| PXL-DA-012 | High | Retested Passed | Residues live elsewhere: approval invalidation on post-approval edits (PXL-AUD-005/PXL-DA-011); manual-JE gating needs a manual/system discriminator; multi-step routing when requested. |
| PXL-DA-013 | High | In Progress | As-of customer/supplier ledger views; period-end subledger-to-GL reports. |
| PXL-DA-014 | High | Retested Passed | JE `entry_class`, `fn_close_fiscal_year` (revenue/expense → retained earnings), period lock, TB modes (test 040). FS-line mapping table remains a backlog enhancement. |
| PXL-DA-015 | Critical | Retested Passed | Complete: every export surface snapshots (sessions 34-39) and the snapshot reader/drilldown UI shipped (session 40). DA-019 subsequently closed the CAS hardening residue: exact bytes, DAT layout/artifacts, books reconciliation, and audit-package evidence. |
| PXL-DA-016 | High | Retested Passed | Governed `transaction_events` lifecycle stream shipped; per-document UI timeline rollout remains PXL-AUD-050. |
| PXL-DA-017 | High | Retested Passed | Line dimensions + central branch inheritance + company-consistency guards delivered (session 41). Document-level department/cost-center capture is a backlog enhancement; stock transfer JEs stay branch-unattributed by design. |
| PXL-DA-018 | Medium | Open | Server-side heavy reports; pagination/materialization. |
| PXL-DA-019 | Critical | Retested Passed | Closed across sessions 64/80/81/82: governed numbering and void evidence, exact exported-byte hashing, versioned CRLF `PXL-CAS-DAT-1.0` artifacts, source/GL-reconciled books exports, and a server-attested CAS audit package are in place (`20260713000009`; tests 017/018 at 29/22 assertions). |
| PXL-DA-020 | High | In Progress | Docs-consistency CI gate live; matrix row sync remains session discipline. |
| PXL-AUD-031 | Critical | Retested Passed | Fixed (session 49, `20260704000003_receipt_cwt_explicit_base.sql`): explicit `cwt_tax_base` + variance reason on receipt lines with PV-parity validation; OR/cash-sale tax ledger carries the VAT-exclusive base; UI defaults the net base. CWT-NET-BASE-001 executed (20 assertions). PV-side gross defaults remain under PXL-AUD-045. |
| PXL-AUD-032 | Critical | Retested Passed | Fixed (session 57, `20260705000001_cv_ewt_supplier_validation.sql`): `check_vouchers.supplier_id`/`ewt_tax_base`/`ewt_variance_reason` with PV-parity validation (trigger + post); tax detail carries the supplier link + master identity; 2307 generation skips unlinked rows with a warning instead of aborting. CV-EWT-2307-001 executed (17 assertions). |
| PXL-AUD-033 | Critical | Retested Passed | Fixed (session 57, same migration): `fn_cancel_check_voucher` now uses `fn_reverse_tax_detail_entries` (counter-row with reverses link, dated on the cancel date); legacy bare CV counter-rows backfilled with the link and re-dated onto their reversal JE. |
| PXL-AUD-034 | Critical | Retested Passed | Residue closed (session 78): `remitted_prior` is now derived from and validated against the controlled remittance flow (PXL-AUD-041). 1601FQ (`fwt_returns`) stays ungated until anything posts FWT tax detail. |
| PXL-AUD-035 | High | Retested Passed | Fixed (session 77, `20260713000002`): PV/OR/CV EWT-CWT validators take the document date and evaluate the ATC effective window as of it; all callers thread `voucher_date`/`receipt_date`. |
| PXL-AUD-036 | High | Retested Passed | Fixed (session 77, `20260713000002`): global `UNIQUE (code)` replaced by version-aware `(code, tax_category, effective_from)`, overlap/successor guard, `fn_atc_version_asof` resolver, effective_from immutable once used. |
| PXL-AUD-037 | High | Retested Passed | Fixed (session 83, `20260713000010`): AP EWT recognition is now company-configurable with default `accrual_at_source`; supplier-default EWT can accrue at VB posting, AP is credited net of source EWT, PVs against source-accrued VBs settle cash-only, and duplicate PV EWT is rejected. Test 037 executed (16 assertions). |
| PXL-AUD-038 | High | Retested Passed | Fixed (session 77, `20260713000003`): `fn_save_payment_voucher`/`fn_save_receipt` derive header `total_amount`/`total_ewt`/`total_cwt` from the persisted lines (client header ignored); the ready validators reject any header cash total that diverges from the line payment sum before posting. |
| PXL-AUD-039 | High | Retested Passed | Fixed (session 77, `20260713000004`): `fn_save_receipt`/`fn_save_payment_voucher` over-apply guards now net applied credit memos (AR) and non-reversed vendor-credit applications (AP), mirroring `fn_ar_aging_asof`/`fn_ap_aging_asof`. |
| PXL-AUD-040 | High | Retested Passed | Form 2307 issuance lines now retain and render month-1/2/3 base and withheld amounts. |
| PXL-AUD-041 | High | Retested Passed | Fixed (session 78, `20260713000005`): governed `withholding_remittances` document (WHTREM source type) posts DR EWT Payable / CR cash (0619-E) or DR income-tax-due / CR CWT Receivable; `fn_wht_gl_reconciliation` excludes WHTREM JEs so mid-quarter remittances stop blocking QAP/SAWT exports and the 1601EQ gate, while uncontrolled MANUAL JEs still surface as variance; `fn_compute_ewt_remitted_prior` feeds/validates `remitted_prior`. Test 036 (18 assertions). Follow-up: dedicated remittance UI page (paused UI lane, DEC-017). |
| PXL-AUD-042 | High | Retested Passed | Fixed (session 84, `20260713000011`): active non-EWT compliance profiles now block AP-side EWT payable on VB/PV/CV, 1601EQ/EWT returns, and QAP export snapshots; TWA auto-EWT defaults supplier-subject source-basis VB lines to WC158 1% goods / WC160 2% services. Test 038 executed (11 assertions). |
| PXL-AUD-043 | High | Retested Passed | Cash-purchase EWT plus customer-advance CWT and supplier down-payment EWT are implemented/tested with governed posting accounts and tax-detail reconciliation. |
| PXL-AUD-044 | Medium | Retested Passed | Fixed (session 91): customer CWT now uses `is_subject_to_cwt` + `default_cwt_atc_code_id`; AP defaults use supplier `default_atc_code_id`; legacy `is_withholding_agent`, `default_ewt_code_id`, `ewt_codes`, and `fwt_codes` are retired. |
| PXL-AUD-045 | Medium | In Progress | PV now defaults EWT base to the proportional VAT-exclusive bill base; remaining: flow SI expected CWT to the receipt and validate SI expected CWT against customer ATC. |
| PXL-AUD-046 | Medium | Retested Passed | Fixed (session 92): cash-sale OR headers store cash received in `receipts.total_amount`, keep CWT in `total_cwt`, derive gross as `total_amount + total_cwt`, and bounce reversals match the original JE totals. |
| PXL-AUD-047 | Medium | Retested Passed | Fixed (session 93): received certificates are RPC-governed, claimable only against posted/unreversed receipt CWT, invalidated on receipt bounce, and issued certificates are flagged for supersede after EWT reversal. |
| PXL-AUD-048 | Medium | Retested Passed | Fixed (session 77, `20260713000003`, folded into AUD-038): header `total_ewt`/`total_cwt` are now recomputed to equal the line withholding sums exactly, so the GL figure equals the tax-ledger figure with zero drift and no permanent 0.02 reconciliation variance can reach exports. |
| PXL-AUD-049 | Medium | In Progress | GL Impact panels landed on CV/cash purchase/cash sale; add tax-ledger/2307/QAP drilldowns under PXL-DA-002. |
| PXL-AUD-050 | Medium | In Progress | Sales Invoice, Receipt, Credit Memo, Vendor Bill, Payment Voucher, and Journal Entry now surface lifecycle facts and `AuditTrailSection`; roll the same pattern across remaining transaction pages. |
| PXL-AUD-051 | Medium | Retested Passed | Closed 2026-07-12: JE/FA/SDM/PRT added to the registry, DebitMemosPage reads DM-S, and all eight branch-less numbering callers now pass the transaction branch; test 030 guards registry coverage and a real FA/JE numbering path. |
| PXL-AUD-052 | Medium | Retested Passed | Fixed 2026-07-10: Cash Purchase item lookup queried non-existent columns (`units_of_measure.uom_name`, `items.purchase_account_id`), so the item picker always loaded empty; select realigned to `uom_code` and aliased `purchase_expense_account_id`. |

## Production Readiness Gate

PXL is production-ready when all of the following hold. Re-check this gate at the end of every fix session; update the standing line.

1. Every Critical finding is `Retested Passed`.
2. Every High finding is `Retested Passed`, or explicitly accepted by the user with the rationale recorded in its index row.
3. Medium findings are fixed or explicitly accepted.
4. CI is green on `main`: fresh-database migration replay, full pgTAP suite, build, lint, docs-consistency check.
5. Hosted Supabase is in sync (`supabase migration list --linked` shows no pending migrations).
6. Production Readiness Scores below have been re-assessed after the last Critical fix.

Standing (2026-07-14, session 93 — PXL-AUD-047 Form 2307 received claim lifecycle completed): 63 Retested Passed / 8 In Progress / 1 Open (72 findings). Session 93 moved received Form 2307 recording/claiming behind RPC validation, locks claimed/invalidated evidence, invalidates linked received certificates when receipts bounce, and flags sent/acknowledged issued certificates for supersede when source EWT is reversed (`20260714000005` + test 047, 18 assertions). Session 92 made draft receipt header cash/CWT totals line-authoritative, backfilled posted cash-sale OR headers to the same split, and proved cash-sale bounce reversals match the original gross JE (`20260714000004` + test 046, 13 assertions). Gate NOT met — the Critical gate is clear, but In-Progress findings and the remaining Open finding still require closure or explicit acceptance. DEC-017 supersedes the temporary transaction-workspace-first order: the active milestone is now **PXL Accounting Core Ready** (`PXL_ACCOUNTING_CORE_READINESS.md`). `PXL_ACCOUNTING_RULES_MATRIX.md` is the governed posting-behavior reference for future fixes and rollout. Do not create additional UI standards, report pilots, dashboards, or transaction workspace rollouts until accounting/tax core readiness is cleared. Recent closed sessions: AUD-047 (session 93), AUD-046 (session 92), AUD-044 (session 91), AUD-040 (session 90), AUD-043 (session 89), AUD-016/AUD-013/DA-014 (session 86), AUD-035..039 and AUD-041..042 (sessions 77-84), DA-009/019 (sessions 79-82), and DA-010/DA-016 (sessions 85/87). Remaining EWT/core fix order: continue AUD-049, plus DA-018/DA-013/DA-020 and remaining coverage items.

| Finding ID | Area | Severity | Status | Issue | Expected Behavior | Actual Behavior | Root Cause | Files Affected | Test Performed | Fix Plan | Fix Session Size |
| ---------- | ---- | -------- | ------ | ----- | ----------------- | --------------- | ---------- | -------------- | -------------- | -------- | ---------------- |
| PXL-AUD-001 | Test coverage / release readiness | Critical | Retested Passed | There was no automated end-to-end or database integration test for the core accounting journey. The app could compile while posting, tax, RLS, and report logic remained wrong. | A Philippine accounting ERP should have repeatable tests for create company -> setup -> SI -> post -> OR -> VB -> PV -> TB/VAT/EWT/2307. | Partially fixed: a local Supabase + pgTAP harness now exists (`supabase/tests/001_critical_flow_test.sql`, run with `supabase test db`). The seeded critical flow — company, branch, fiscal periods, COA, GL config, number series, customer/supplier, SI save/approve/post, OR post with CWT, VB save/approve/post, PV post with EWT — executes with 41 passing assertions on JE balance, debit/credit accounts, AR/AP clearing, tax detail rows, posted-line immutability, and GL zero-net. First executions immediately caught two runtime-fatal defects (PXL-AUD-022, PXL-AUD-023). | Quality gates stopped at TypeScript and oxlint; no seeded journal assertions existed. Harness now runs via Docker + `supabase test db` (wrapped as `npm test`); all seven documented test book scenarios are automated, and `.github/workflows/ci.yml` now runs lint/build plus the pgTAP suite on a fresh database in GitHub Actions (fresh-runner migration replay also serves as the PXL-AUD-022/PXL-DA-006 fresh-replay gate). Role-based RLS scenarios remain. | `supabase/tests/*.sql`; `supabase/config.toml`; `supabase/migrations/*`; `package.json`; `.github/workflows/ci.yml` | Executed 2026-07-02: `supabase db reset --local` replays the full migration chain cleanly, then `npm test` passes 117/117 assertions across 7 files. `npm run build`; `npm run lint` pass. First hosted GitHub Actions run (28582917878, push of commit 67269d9) completed successfully: `build-lint` green and `db-tests` green with `Files=7, Tests=117, Result: PASS` on a fresh runner database. | Role-based RLS test scenarios remain tracked under PXL-AUD-004/PXL-DA-003; broader module scenarios (CM/DM, inventory, assets) are follow-on coverage, not part of this finding's scope. | Large |
| PXL-AUD-002 | Company setup / onboarding | Critical | Retested Passed | Company setup did not expose whether the minimum usable accounting environment existed, so users reached late posting errors after creating a legal entity. | After company creation, the app should guide or enforce setup of branch, fiscal calendar, COA, number series, tax profile, VAT/ATC codes, and posting accounts before transactions are allowed. | Fixed: every company row opens a live setup checklist with progress and direct actions for legal profile, active branch, current fiscal year/period, all five COA types, SI/OR/VB/PV series by branch, compliance profile, applicable VAT/ATC/company tax codes, and applicable GL mappings. Readiness banners link back to the checklist. SI/OR/VB/PV plus CM/DM/cash sale/cash purchase/vendor credit disable save/post while relevant branch, period, series, or GL setup is missing; VAT and withholding mappings are required only when applicable. | Setup remained spread across independent pages with no aggregate readiness model. The shared readiness hook and checklist now turn those dependencies into one guided control surface. | `src/components/CompanySetupChecklist.tsx`; `src/components/SetupReadiness.tsx`; `src/lib/setupReadiness.ts`; `src/pages/CompanySetupPage.tsx`; SI/OR/VB/PV/CM/DM/cash sale/cash purchase/vendor credit pages | Retested 2026-07-10: `npm run build` and zero-warning `npm run lint` pass; code-path inspection verifies every checklist action changes company context before navigation and transaction actions use `readiness.loading/blockers` as a hard disable. Full fresh-replay pgTAP suite passes 448/448. | Closed. Broader number-series preflight coverage is independently tracked under PXL-AUD-016; lifecycle evidence rollout is PXL-AUD-050. | Medium |
| PXL-AUD-003 | Company setup / data integrity | Critical | Retested Passed | Editing an existing company could overwrite stored required fields with blanks/nulls. | Company edit should load all current values and only update intentional changes. | Fixed: edit now fetches the full company row before opening, hydrates all form fields through one shared mapper, and save blocks missing required compliance fields before update. | `openEdit` previously populated many fields as blank and `handleSave` sent the whole blank payload to `companies.update`. | `src/pages/CompanySetupPage.tsx:78`; `src/pages/CompanySetupPage.tsx:145`; `src/pages/CompanySetupPage.tsx:163` | Retest passed: `npm run build`; `npm run lint` passed with pre-existing warnings only; manual code trace confirms edit hydration uses `select('*')` and required-field guard runs before update. | Fixed in this session. Remaining risk: company setup still does not create branch/period/COA/series/posting config; continue with PXL-AUD-002/PXL-AUD-016 next. | Small |
| PXL-AUD-004 | Auth/RLS / role model | High | Retested Passed | Period, COA, number series, master data, and compliance setup writes are generally allowed for any company member, not just owner/admin. | Destructive or control-table changes should require admin/owner, especially fiscal locks, COA, tax codes, number series, approval setup, and posting configuration. | Partially fixed: branch, fiscal year/period, COA, number series, GL posting config, departments, cost centers, exchange rates, feature flags, approval setup, selected tax setup, compliance profile, payment terms, item categories, and units of measure writes now require `can_admin_company(company_id)` in `20260701000006_permissions_hardening.sql` and `20260701000007_lifecycle_permissions_broaden.sql`. Operational master-data writes (customers/suppliers/items) now follow the DEC-009 policy through `fn_can_perform`-backed RLS: members create/edit, viewers read-only, delete stays owner/admin (`20260702000010_can_perform_role_actions.sql`). | Role differentiation exists (`owner`, `admin`, `member`, `viewer`); setup writes use `can_admin_company`, operational master data uses the `fn_can_perform` `master_data` action per DEC-009. | `supabase/migrations/20260701000006_permissions_hardening.sql`; `supabase/migrations/20260702000010_can_perform_role_actions.sql`; `supabase/migrations/20260630000021_gap_fill.sql:171`; `supabase/migrations/20260630000021_gap_fill.sql:181`; `supabase/migrations/20260630000021_gap_fill.sql:211` | Seeded scenario RLS-ROLES-001 executed passing 2026-07-02 as the `authenticated` role (`supabase/tests/011_role_based_access_test.sql`): cross-company read isolation, member/viewer reads, member/viewer denied setup writes (branches 42501, number series 42501, period lock silently filtered), admin setup writes allowed, member drafts via RPC, member denied approval and posting by the `fn_can_perform` lifecycle gate, admin approves and posts, non-member denied document creation. Seeded scenario RBAC-CANPERFORM-001 executed passing 2026-07-03 (`supabase/tests/013_can_perform_test.sql`, 13 assertions): the DEC-009 matrix per role/action plus the master-data policies on customers/suppliers. | Finer-grained named roles (accountant, bookkeeper) can map onto `fn_can_perform` actions later without changing the enforcement surface; approver-not-creator SoD when a workflow is configured continues under PXL-DA-012. | Medium |
| PXL-AUD-005 | Posted document immutability | Critical | Retested Passed | Some line tables could be directly changed through PostgREST by a company member after the parent document left draft. | Posted document headers and lines should be immutable except through controlled reversal/void RPCs. | Fixed: `20260701000009_core_line_immutability.sql` guards SI/OR/VB/PV lines; session 43 (`20260704000002_status_immutability.sql`, PXL-DA-011) extended the control to every remaining transactional line table via `fn_guard_doc_lines` and to header business fields via `fn_guard_doc_header` — sales, purchasing, banking, inventory, fixed-asset, schedule-entry, and compliance-return documents included. | The app relied heavily on UI read-only state and RPC conventions, but RLS itself did not consistently enforce posted immutability. Trigger guards now enforce it beneath RLS for any role. | `supabase/migrations/20260701000009_core_line_immutability.sql`; `supabase/migrations/20260704000002_status_immutability.sql`; `supabase/tests/020_status_immutability_test.sql` | Executed 2026-07-04 (IMMUT-001): committed posted/approved fixtures reject cross-transaction line update/delete/inject and header business-field changes; controlled void/apply RPCs keep working; `npm test` 324/324 across 20 files on a fresh reset. | Direct DB tests delivered; header business-field review delivered through per-table lifecycle allowlists. Approval-invalidation-on-edit is structurally prevented (post-approval edits are blocked outright). | Medium |
| PXL-AUD-006 | VAT logic / non-VAT company behavior | Critical | Retested Passed | VAT treatment did not consistently respect `companies.tax_registration`; non-VAT/exempt companies could still reach VAT-bearing CM/DM/cash/vendor-credit paths and VAT exports. | VAT-registered companies may use VAT output/input. Non-VAT/exempt companies must use zero-VAT treatment and cannot create VAT returns or VAT-specific exports. | Fixed across SI, VB, CM, DM, cash sales, cash purchases, and vendor credits. Generic parent-aware line and header triggers enforce company registration, VAT-code direction, company consistency, and zero VAT amounts. Cash sales inherit SI gates. SLSP/RELIEF snapshot and CAS wrappers reject non-VAT companies. CM/DM/VC tax detail is rebuilt per VAT code, and authenticated direct tax-ledger mutation is denied. UI code choices/readiness follow the company registration. | Registration checks were duplicated only on SI/VB and trusted client totals elsewhere. Migration `20260710000002` centralizes the rule and wraps all applicable export entry points. | `supabase/migrations/20260710000002_vat_registration_all_documents.sql`; `supabase/tests/024_vat_registration_all_documents_test.sql`; VAT-bearing transaction pages; `src/components/CompanySetupChecklist.tsx` | VAT-REG-ALL-DOCS-001 executed 2026-07-10: 35 assertions cover line/header bypass attempts, wrong VAT direction, all added document classes, cash-sale inheritance, per-code ledger rows, non-VAT export/snapshot rejection, and authenticated tax-ledger write denial. NON-VAT-GATING-001 remains green; fresh replay + full suite pass 448/448. | Closed for registration behavior. Server-side VAT amount computation and broader ledger/report standardization remain under PXL-DA-008/PXL-AUD-014. | Medium |
| PXL-AUD-007 | EWT / withholding tax basis | Critical | Retested Passed | Payment voucher EWT was manually entered and posted without validating ATC rate or taxable base. The tax ledger used `payment_amount + ewt_amount` as tax base, which could include VAT/AP gross and be wrong for Philippine withholding. | EWT should be computed or validated from net-of-VAT taxable base, supplier tax profile, income nature, ATC code, and applicable rate; variance should be controlled. | Scoped fixes now validate PV line EWT against active EWT ATC rate, add supplier AP default ATC, add explicit PV-line `ewt_tax_base`, income nature, and controlled variance reason, and add customer CWT defaults/receipt tax detail. | ATC/versioning, richer withholding profiles, and seeded full/partial-payment test coverage remain incomplete. | `src/pages/PaymentVouchersPage.tsx`; `src/pages/ReceiptsPage.tsx`; `supabase/migrations/20260701000013_pv_ewt_atc_validation.sql`; `supabase/migrations/20260701000016_pv_ewt_explicit_basis.sql`; `supabase/migrations/20260701000017_customer_cwt_defaults.sql`; `supabase/tests/006_pv_ewt_partial_payment_test.sql`; `supabase/tests/001_critical_flow_test.sql` | Seeded scenarios executed passing 2026-07-02 via `supabase test db`: PV-EWT-PARTIAL-001 (two partial payments each withholding on an explicit net-of-VAT base, cumulative EWT 200 on base 10,000 in tax ledger and GL, over-payment rejected, rate-mismatch rejected without an authorized variance reason, expired ATC rejected) and CRITICAL-FLOW-001 (full payment with EWT, receipt CWT tax detail). ATC versioning was delivered under PXL-DA-010. | SAWT/QAP/1601EQ report-level reconciliation to the tax ledger continues under PXL-DA-009/PXL-AUD-014-style compliance work. | Large |
| PXL-AUD-008 | Customer/vendor tax defaults | High | In Progress | Customer/vendor withholding and ATC defaults were incomplete. Sales had a legacy customer expected-CWT field, while purchasing lacked supplier ATC defaults aligned to the active PV EWT flow. | Customer/vendor master data should define withholding applicability, default ATC, rate, VAT classification, and default accounts used by transaction entry. | Scoped fixes now add supplier AP EWT defaults and customer CWT defaults using active withholding ATCs. Supplier defaults prefill PV ATC/EWT; customer defaults prefill OR CWT/ATC and posted receipts write CWT tax detail. | Vendor bill `ewt_amount_expected` is still informational, richer withholding profiles and seeded default-flow tests are still missing, and ATC versioning remains open. | `src/pages/SuppliersPage.tsx`; `src/pages/CustomersPage.tsx`; `src/pages/PaymentVouchersPage.tsx`; `src/pages/ReceiptsPage.tsx`; `supabase/migrations/20260701000014_supplier_atc_defaults.sql`; `supabase/migrations/20260701000017_customer_cwt_defaults.sql` | `npm run build`; `npm run lint`; static SQL/code trace. No seeded supplier/customer default scenario executed. | Remaining fix: add seeded supplier/customer default EWT/CWT/2307 tests, richer withholding profiles, and ATC versioning/rate-effective-date controls. | Medium |
| PXL-AUD-009 | Sales posting validation | High | In Progress | Sales invoices could be approved before all posting-critical accounting fields were valid. | Approval should verify the document is accounting-ready, or posting action should clearly validate before changing state. | Partially fixed: `SalesInvoicePage` now blocks approve/post when active lines lack revenue accounts or active output VAT codes, and `20260701000008_accounting_readiness_approval.sql` adds database validation on SI approved/posted status transitions plus `fn_approve_sales_invoice`. | Save/approve and post validations were split; approve RPC did not inspect line account readiness. A scoped validation trigger now closes the SI gap. | `src/pages/SalesInvoicePage.tsx`; `supabase/migrations/20260701000008_accounting_readiness_approval.sql`; `supabase/migrations/20260630000021_gap_fill.sql:6564`; `supabase/migrations/20260630000021_gap_fill.sql:6582` | `npm run build`; `npm run lint`; migration pushed to remote Supabase. | Remaining fix: seeded DB tests for missing revenue account, inactive/non-output VAT code, and direct status update/RPC attempts; VAT registration by company remains separate under PXL-AUD-006. | Small |
| PXL-AUD-010 | Purchasing posting validation | High | In Progress | Vendor bills could be approved before expense accounts and VAT settings were complete, then fail late at posting. | Approval should verify all bill lines have postable expense accounts and valid VAT treatment. | Partially fixed: `VendorBillsPage` now blocks approve/post when active lines lack active expense accounts or active input VAT codes, and `20260701000008_accounting_readiness_approval.sql` adds database validation on VB approved/posted status transitions plus `fn_approve_vendor_bill`. | Save/approve did not enforce accounting readiness. A scoped validation trigger now closes the VB gap. | `src/pages/VendorBillsPage.tsx`; `supabase/migrations/20260701000008_accounting_readiness_approval.sql`; `supabase/migrations/20260630000021_gap_fill.sql:5691`; `supabase/migrations/20260630000021_gap_fill.sql:5716` | `npm run build`; `npm run lint`; migration pushed to remote Supabase. | Remaining fix: seeded DB tests for missing expense account, inactive/non-input VAT code, and direct status update/RPC attempts; supplier invoice/TIN completeness and non-VAT input VAT are still separate controls. | Small |
| PXL-AUD-011 | AR aging / as-of reporting | High | Retested Passed | AR aging ignored the report `asOfDate` when subtracting receipts and credit memos. Future-dated collections/CMs could reduce a past aging report. | As-of aging must include only transactions dated on or before the as-of date. | Fixed: as-of AR aging now lives in the database as `fn_ar_aging_asof` (posted SIs dated <= as-of, posted receipt applications by `receipt_date <= asOfDate`, applied credit memos by `cm_date <= asOfDate`), and `ARAgingPage` calls the RPC instead of recomputing client-side. | Aging was calculated from document lines without source-date constraints, then scoped-fixed only in page code. | `supabase/migrations/20260702000003_ar_ap_aging_asof_rpcs.sql`; `src/pages/ARAgingPage.tsx`; `supabase/tests/002_ar_aging_asof_test.sql` | Seeded test book scenario AR-AGING-ASOF-001 executed passing 2026-07-02 via `supabase test db`: future OR/CM excluded at each as-of date and aging total reconciles to the GL AR control account as of 2026-01-31 and 2026-02-28. `npm run build`; `npm run lint`. | Customer ledger view (`vw_customer_ledger`) remains current-only; ledger as-of standardization continues under PXL-DA-013. | Small |
| PXL-AUD-012 | AP aging / as-of reporting | High | Retested Passed | AP aging ignored the report `asOfDate` for payments because `vw_ap_aging` computed current balance only. | AP aging as of a date should exclude future payments/credits. | Fixed: as-of AP aging now lives in the database as `fn_ap_aging_asof` (posted VBs dated <= as-of, posted PV lines by `voucher_date <= asOfDate`, non-reversed vendor credit applications by `applied_date <= asOfDate`), and `APAgingPage` calls the RPC instead of recomputing client-side. The legacy `vw_ap_aging` remains current-balance-only but is no longer the page's source. | Aging view has no as-of parameter; page previously recomputed client-side only. | `supabase/migrations/20260702000003_ar_ap_aging_asof_rpcs.sql`; `src/pages/APAgingPage.tsx`; `supabase/tests/003_ap_aging_asof_test.sql` | Seeded test book scenario AP-AGING-ASOF-001 executed passing 2026-07-02 via `supabase test db`: future PV excluded at each as-of date and aging total reconciles to the GL AP control account as of 2026-01-31, 2026-02-28, and 2026-03-31. `npm run build`; `npm run lint`. | Supplier ledger view (`vw_supplier_ledger`) remains current-only; ledger as-of standardization continues under PXL-DA-013. | Small |
| PXL-AUD-013 | Trial balance / financial reporting | High | Retested Passed | Trial balance display used debit-minus-credit net for every account with no entry classification; the `Adjusted TB` checkbox only hid zero balances rather than including/excluding adjusting entries, and there was no post-closing definition. | TB should respect normal balances, show opening/period/closing debit-credit correctly, and have real adjusted/unadjusted/post-closing definitions. | Fixed (session 86, with PXL-DA-014): `journal_entries.entry_class` classifies every entry (regular/adjusting/closing/opening) and `vw_general_ledger` exposes it. `TrialBalancePage` replaces the checkbox with an Unadjusted / Adjusted / Post-Closing mode selector that filters the ledger by class — unadjusted = regular+opening, adjusted = +adjusting, post-closing = +closing — so a post-closing TB shows revenue/expense at zero and retained earnings carrying net income. Opening/period/closing debit-credit columns present each account on its sign, and the grand total still ties. | Reporting page computed from raw `vw_general_ledger` rows with no entry classification. Classification + a mode-aware read model now define each TB mode. | `supabase/migrations/20260713000013_je_classification_and_close.sql`; `src/pages/TrialBalancePage.tsx`; `src/pages/JournalEntriesPage.tsx`; `supabase/tests/040_financial_close_readiness_test.sql` | Retested (session 86): full pgTAP **737/737 across 39 files** on a held-out-safe reset (new test 040, FINANCIAL-CLOSE-001, 17 assertions proving unadjusted vs adjusted vs post-closing balances and close correctness); `npm run build` + zero-warning `npm run lint` pass. | Closed with PXL-DA-014. Normal-balance-grouped FS presentation and a per-account FS-line mapping table remain backlog enhancements. | Medium |
| PXL-AUD-014 | Compliance / VAT reports | High | Retested Passed | VAT reports previously mixed document/header sources, omitted transaction classes, and exposed derived VAT amounts to direct application-table or updatable-view mutation. | VAT returns and review/export surfaces must derive from posted per-code tax detail, reconcile to GL VAT controls, preserve filed/exported evidence, and reject client-forged or cross-company document VAT. | Complete: SI/VB/cash sale/cash purchase/CM/DM/VC writers produce canonical per-code detail including zero-rated/exempt bases; VAT review views and 2550 sources read `tax_detail_entries`; final/filed returns and VAT exports reconcile to GL; export/filing evidence is versioned in immutable snapshots; `20260712000001` removes application mutations on all VAT source headers/lines and updatable SI/VB register views, and makes those views security-invoker so base RLS applies. | VAT calculations, ledger rows, reports, view security, and write permissions evolved independently; permissive legacy grants/policies left paths around otherwise-correct save RPCs. | `supabase/migrations/20260703000002_vat_ledger_completeness.sql`; `supabase/migrations/20260703000003_vat_review_views_ledger_backed.sql`; `supabase/migrations/20260710000002_vat_registration_all_documents.sql`; `supabase/migrations/20260712000001_vat_amount_rpc_authority.sql`; `supabase/tests/008_vat_ledger_gl_reconciliation_test.sql`; `supabase/tests/015_vat_ledger_completeness_test.sql`; `supabase/tests/024_vat_registration_all_documents_test.sql`; `supabase/tests/028_vat_amount_integrity_test.sql` | Retested 2026-07-12: VAT-AMOUNT-INTEGRITY-001 passes 25 assertions across all six persisted VAT document families, including forged payload rejection-by-recomputation, base/view DML denial, cross-company register isolation, and mixed regular/zero/exempt SI/VB reconciliation through tax ledger, GL controls, and VAT review; focused VAT/trace suite passes 173/173. Existing NON-VAT-GATING-001, VAT-RECON-001, VAT-LEDGER-COMPLETE-001, and VAT-REG-ALL-DOCS-001 remain green. | Closed. VAT/other-tax code effective-date versioning remains independently tracked under PXL-DA-010. | Medium |
| PXL-AUD-015 | Compliance / 2307 issued | High | Retested Passed | 2307 issued certificates were generated from EWT summary data with direct UI upserts, line delete/reinsert, direct status updates, and upstream implicit EWT tax base. | 2307 generation should be based on validated, posted, ATC-level EWT detail with immutable audit trail and controlled status transitions. | `Form2307IssuedPage` now routes generation, sent/acknowledged transitions, and supersession through RPCs; generated certificates can be regenerated, sent/acknowledged certificates are locked, and `fn_supersede_form_2307_issued` (20260702000007) issues a new version from current EWT detail while preserving the old certificate as immutable superseded evidence with two-way links. Uniqueness moved to one active certificate per company/supplier/quarter (partial unique index). PV posting writes explicit EWT tax base and income nature into tax detail consumed by 2307. | Added `fn_generate_form_2307_issued`, `fn_update_form_2307_issued_status`, `fn_supersede_form_2307_issued`, certificate `version`/supersede linkage columns, explicit PV EWT tax detail basis, and direct-write denial policies for issuance headers/lines. | `src/pages/Form2307IssuedPage.tsx`; `src/pages/PaymentVouchersPage.tsx`; `supabase/migrations/20260701000015_form2307_issued_generation_rpc.sql`; `supabase/migrations/20260701000016_pv_ewt_explicit_basis.sql`; `supabase/migrations/20260702000007_form2307_version_supersede.sql` | Seeded scenarios executed passing 2026-07-02 via `supabase test db`: F2307-ISSUED-001 (`supabase/tests/007`: quarter-batch generation, regeneration, sent-status locking, direct-write denial) and F2307-SUPERSEDE-001 (`supabase/tests/010`: supersede of a sent certificate creates version 2 from refreshed detail with two-way links, preserves the old certificate and lines as evidence, keeps one active certificate per supplier/quarter, regeneration never resurrects superseded certificates, generated/superseded certificates cannot be superseded, direct un-superseding denied). | Certificate lifecycle is now fully RPC-controlled with version/supersede; downstream QAP/1601EQ reconciliation to superseded chains continues under PXL-DA-009 compliance work. | Medium |
| PXL-AUD-016 | Number series / setup dependency | High | Retested Passed | Transactions depend on number series, but company setup does not create defaults and not every transaction page preflights missing series. | Users should know before saving that document numbering for branch/document type is not configured. | Fixed: the shared `useTransactionReadiness` + `SetupReadinessBanner` preflight now covers every numbered transaction page. Prior sessions covered SI, OR, VB, PV, CM, DM, cash sale, cash purchase, and vendor credit. Session 86 added the remaining documents: the GL-posting Check Voucher (`CV`), Fund Transfer (`FT`), Inter-Branch Transfer (`IBT`, keyed on the source `from_branch_id` the allocator uses), Bank Adjustment (`BADJ`), Petty Cash Replenishment (`PCR`), and Petty Cash Voucher (`PCV`); and the pre-financial Quotation (`QT`), Sales Order (`SO`), Delivery Receipt (`DR`), and Cash Count Sheet (`CCS`). Missing series is shown as an actionable blocker and each save handler hard-blocks while `readiness.loading`/`blockers` are set. The hook gained a `requireOpenPeriod` flag (default true) so pre-financial documents that allocate a number but do not post to the GL are not over-blocked by a missing/closed fiscal period. | Number series is a separate setup page. A shared readiness hook checks `number_series` using the current `document_code` shape with fallback to the legacy `ref_document_types` join. | `src/lib/setupReadiness.ts`; `src/pages/SalesInvoicePage.tsx`; `src/pages/ReceiptsPage.tsx`; `src/pages/VendorBillsPage.tsx`; `src/pages/PaymentVouchersPage.tsx`; `src/pages/CheckVouchersPage.tsx`; `src/pages/FundTransfersPage.tsx`; `src/pages/InterBranchTransfersPage.tsx`; `src/pages/BankAdjustmentsPage.tsx`; `src/pages/PettyCashReplenishmentPage.tsx`; `src/pages/PettyCashVouchersPage.tsx`; `src/pages/QuotationsPage.tsx`; `src/pages/SalesOrdersPage.tsx`; `src/pages/DeliveryReceiptsPage.tsx`; `src/pages/CashCountSheetPage.tsx` | Retested (session 86): `npm run build` and zero-warning `npm run lint` pass; a repo-wide check confirms no page calling `fn_next_document_number` lacks the readiness preflight; GL-posting docs keep the open-period gate while `QT`/`SO`/`DR`/`CCS` use `requireOpenPeriod: false`. | Closed for the preflight defect. Seeding default number series during company/branch setup is a separate product enhancement tracked in the backlog, not a preflight gap. | Medium |
| PXL-AUD-017 | UI/user flow / context persistence | Medium | Retested Passed | Company, branch, and period selections were stored only in React state. Refreshing the browser cleared context and made pages appear empty or blocked. | User context should persist per browser/session and restore last selected company/branch/period. | Fixed (session 44): `AppContextProvider` lazily initializes the three IDs from localStorage (`pxl.ctx.*`) and the setters persist changes; `ContextSelectors` validates restored IDs against the RLS-scoped company/branch/period lists on load and clears anything the signed-in user can no longer see (cascade-clearing branch/period when the company is stale), so a different user on the same browser is never silently scoped to an inaccessible company. localStorage access is wrapped for storage-unavailable environments. | Context state was not persisted. | `src/lib/context.tsx`; `src/components/AppShell.tsx` | Executed 2026-07-04: `npm run build` passed; `npm run lint` pre-existing 39 warnings only; validation traced — restored-but-invisible IDs clear on the selector fetch every page load performs. No DB change. | Done. URL-based context for deep links remains a possible future enhancement (backlog if requested). | Small |
| PXL-AUD-018 | Lint / code quality | Medium | Retested Passed | Lint passes but shows many hook dependency warnings and suspicious unused expressions. These are real runtime risk in data-loading pages and batch actions. | Lint should be clean or warnings should be intentionally suppressed with explanation. | Partially fixed (session 45, 39→29 warnings): the 7 unstable-dependency warnings were a REAL defect — six dashboards (Books, CAS, Income Tax, PT, VAT, WT) created `now = new Date()` (WT also `quarterMonths`) on every render, giving the `load` useCallback a new identity each render, so `useEffect([load])` refired after every render and the pages refetched from Supabase in a continuous loop; `now` is now pinned at mount via `useMemo` (and `quarterMonths` derived via `useMemo`), so each dashboard loads once per company change. The 2 `no-unused-expressions` (ternary-as-statement Set toggles in `DepreciationRunPage`/`CollectionMonitoringPage`) became if/else, and the unused `department_name` destructure in `EmployeesPage` is `_`-prefixed. | Data-loading functions are often declared inline or not memoized. | `src/pages/BooksDashboardPage.tsx`; `src/pages/CASDashboardPage.tsx`; `src/pages/IncomeTaxDashboardPage.tsx`; `src/pages/PTDashboardPage.tsx`; `src/pages/VATDashboardPage.tsx`; `src/pages/WTDashboardPage.tsx`; `src/pages/DepreciationRunPage.tsx`; `src/pages/CollectionMonitoringPage.tsx`; `src/pages/EmployeesPage.tsx` | Executed 2026-07-04 (session 46): `npm run lint` reports 0 warnings, exit 0; `npm run build` passed. | Done. Session 46 resolved the remaining 29: the 27 `useEffect` missing-dependency warnings all flag one benign idiom — `useEffect(() => { load() }, [companyId, ...])` with a per-render loader whose other inputs are refetched via direct handler calls, so the effect always executes a fresh closure and no stale-state bug exists; each site now carries `// eslint-disable-next-line react-hooks/exhaustive-deps` WITH an explanation, which the finding's expected behavior explicitly allows (“intentionally suppressed with explanation”). A 27-page useCallback restructure was rejected as regression risk without user value. The 2 fast-refresh only-export-components notes (`button.tsx`, `context.tsx`) are suppressed with reason (dev-only fast-refresh concern). New warnings now fail visibly against a zero-warning baseline. | Medium |
| PXL-AUD-019 | AP aging / vendor credits | High | Retested Passed | AP aging excluded vendor credit applications, so supplier payable reports could overstate AP after a posted/applied vendor credit. | AP aging should reduce open vendor bill balances by active posted/applied vendor credits dated on or before the report as-of date. | Fixed: `fn_ap_aging_asof` subtracts non-reversed vendor credit applications with `applied_date <= asOfDate` and linked vendor credit status `open`/`applied`, and `APAgingPage` uses the RPC. | AP applications are split across payment vouchers and vendor credits, but the aging computation only considered payment voucher applications. Reversed vendor credit applications must be excluded from AP aging. | `supabase/migrations/20260702000003_ar_ap_aging_asof_rpcs.sql`; `src/pages/APAgingPage.tsx`; `supabase/tests/003_ap_aging_asof_test.sql`; `supabase/migrations/20260701000011_vendor_credit_application_reversal.sql` | Seeded test book scenario AP-AGING-ASOF-002 executed passing 2026-07-02 via `supabase test db`: VB 8,000 - applied VC 2,000 shows 6,000 as of 2026-03-31, 8,000 the day before the application, and the aging total reconciles to the GL AP control (VC JE dated on the application date). | Legacy `vw_ap_aging` view still exists for other consumers and remains current-balance-only. | Small |
| PXL-AUD-020 | Vendor credit application dating | High | Retested Passed | Vendor credit application UI did not expose an application date and called `fn_apply_vendor_credit` without `p_date`, so the database defaulted `applied_date` to the current date. | Users should be able to apply a vendor credit on the correct accounting/application date, subject to open-period validation, and reports should use that stored date. | Scoped fix added: the apply modal requires an application date and passes `p_date`; `20260701000010_vendor_credit_application_date_enforcement.sql` validates the date server-side, requires an unlocked fiscal period, and blocks direct authenticated inserts into `vendor_credit_applications`. | `fn_apply_vendor_credit` supported a date parameter, but `VendorCreditsPage` did not collect/pass it and direct table inserts could bypass RPC checks. | `src/pages/VendorCreditsPage.tsx`; `supabase/migrations/20260701000010_vendor_credit_application_date_enforcement.sql`; `vendor_credit_applications`; `supabase/tests/004_vendor_credit_application_controls_test.sql` | Seeded test book scenario VC-APPLICATION-DATE-001 executed passing 2026-07-02 via `supabase test db`: stored `applied_date` is the user-selected date, aging shows 9,000 as of the day before the application and 6,000 after, application dates before the credit date or in a locked fiscal period are rejected, and direct authenticated inserts are denied. | Direct delete/reversal controls are covered under PXL-AUD-021. | Small |
| PXL-AUD-021 | Vendor credit application deletion / reversal controls | High | Retested Passed | Direct deletes of `vendor_credit_applications` were allowed for company members and did not restore vendor credit `remaining_balance` or status. | Vendor credit application reversal should be controlled by an auditable RPC that restores balances/status and preserves reversal evidence. | Fixed and seeded-tested: `20260701000011_vendor_credit_application_reversal.sql` adds reversal metadata, replaces the unique active application rule with a partial unique index, adds `fn_reverse_vendor_credit_application`, blocks direct authenticated deletes, and updates AP aging to exclude reversed applications. | Application allocation was stored as a mutable table row without a controlled reversal/delete function. The new RPC is a scoped AP allocation reversal, not a full supplier-ledger/reconciliation test suite. | `vendor_credit_applications`; `vendor_credits`; `src/pages/APAgingPage.tsx`; `supabase/migrations/20260701000011_vendor_credit_application_reversal.sql`; `supabase/tests/004_vendor_credit_application_controls_test.sql` | Seeded test book scenario VC-APPLICATION-REVERSAL-001 executed passing 2026-07-02 via `supabase test db`: full application drops the bill balance and zeroes the credit, `fn_reverse_vendor_credit_application` restores balance/status while preserving `reversed_at`/`reversed_by`, direct authenticated delete raises 42501 and the row survives, and post-reversal aging restores the bill. | Supplier ledger view standardization (as-of `vw_supplier_ledger`) continues under PXL-DA-013. | Small |

### New Finding Detail - PXL-AUD-022

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Root Cause | `fn_next_document_number` (20260629000012, replayed in gap_fill) reads `number_series.document_code`, `current_sequence`, `padding`, and `suffix`, but no migration ever created those columns. `NumberSeriesPage` writes only the legacy shape (`document_type_id`, `next_number`, `number_length`), and `useTransactionReadiness` probes `document_code` with a legacy fallback — so the setup UI, the readiness check, and the numbering RPC disagreed on the schema. plpgsql compiles lazily, so every RPC document save (SI/OR/VB/PV and all other numbered documents) fails at runtime with 42703 on any database built purely from migrations; readiness could simultaneously report "ready" through the legacy fallback. |
| Files Affected | `supabase/migrations/20260702000001_number_series_document_code_alignment.sql`; `supabase/migrations/20260629000012_fn_numbering_hardening.sql`; `supabase/migrations/20260630000021_gap_fill.sql`; `src/pages/NumberSeriesPage.tsx`; `src/lib/setupReadiness.ts` |
| Business Risk | No document can be saved through RPCs in a correctly migrated environment; environments that work do so only through unmanaged manual schema drift. |
| Accounting Risk | Document numbering is a CAS control; drifted/unmanaged numbering schema undermines sequence integrity evidence. |
| Tax Risk | Document numbers feed SLSP/VAT/2307 source references. |
| Security Risk | None direct; the risk is governed-schema integrity. |
| Fix Applied | `20260702000001_number_series_document_code_alignment.sql` adds the four columns idempotently, backfills from the legacy shape, installs a BEFORE INSERT/UPDATE sync trigger so UI (legacy-shape) writers and the RPC (document_code-shape) reader stay consistent, and adds a partial unique index on active (company, branch, document_code). Migration pushed to remote. Validated by the seeded critical-flow test: legacy-shape series inserts produce `SI-000001`/`OR-000001`/`VB-000001`/`PV-000001` through `fn_next_document_number`. |
| Remaining Risk | Fresh-database replay of `20260630000021_gap_fill.sql` also required guards for the already-dropped `ref_atc_codes` (fixed in place this session); a full fresh-replay CI check per PXL-DA-006 is still recommended. |

### New Finding Detail - PXL-AUD-023

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Root Cause | The latest `fn_post_sales_invoice` (gap_fill period-enforcement replay) inserted the output VAT tax ledger row using `v_rec.total_net_amount`, a column that does not exist on `sales_invoices`. Every VAT-bearing SI post raised 42703 at the tax detail insert and rolled back the whole posting on a migrations-built database. |
| Files Affected | `supabase/migrations/20260702000002_si_post_tax_base_column_fix.sql`; `supabase/migrations/20260630000021_gap_fill.sql` |
| Business Risk | Core revenue posting is broken in any correctly migrated environment. |
| Accounting Risk | SI posting fails after approval; no JE or tax ledger row can be produced for VAT sales. |
| Tax Risk | Output VAT ledger cannot be populated, breaking 2550M/Q sources. |
| Security Risk | None direct. |
| Fix Applied | `20260702000002_si_post_tax_base_column_fix.sql` replaces the function using `total_taxable_amount` as the output VAT base (the regular VAT-able net; zero-rated/exempt bear no output VAT). Migration pushed to remote. Validated by the seeded test: SI posts a balanced JE and one output VAT tax detail row with base 10,000.00 and tax 1,200.00. |
| Remaining Risk | Whether the remote environment's historical tax detail rows were produced by a drifted manual column remains unverified; VAT ledger-to-GL reconciliation stays open under PXL-AUD-014/PXL-DA-008. |

### New Finding Detail - PXL-AUD-024

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Root Cause | `vw_general_ledger` and `vw_trial_balance` filter `WHERE je.status = 'posted'`, while `fn_reverse_je` sets the original journal entry to `status = 'reversed'` and creates a swapped-line reversing JE with `status = 'posted'`. After any reversal, GL/TB exclude the original entry but include the reverser, so the net reported effect is the reversal applied twice: the account balance flips sign instead of returning to zero. |
| Files Affected | `supabase/migrations/20260630000025_accounting_module.sql` (`vw_general_ledger`, `vw_trial_balance`, `fn_reverse_je`); every report page built on these views |
| Business Risk | Any user-initiated JE reversal misstates GL, trial balance, and financial statements by double the reversed amount. |
| Accounting Risk | GL and TB no longer reconcile to the journal after a reversal; subledger-to-GL reconciliation (including `fn_vat_gl_reconciliation`, which matches the views' `posted` filter) inherits the misstatement. |
| Tax Risk | Reversed tax-bearing JEs distort VAT/EWT control account balances used for compliance reconciliation. |
| Security Risk | None direct. |
| Recommended Fix Plan | Decide one convention: either include both original and reversing JEs in report views (original stays `posted`-visible, statuses `posted`/`reversed` both included), or exclude both. Update views and any consumers in one migration, then add a seeded reversal scenario asserting GL nets to zero after `fn_reverse_je`. |
| Discovered | 2026-07-02, session 23, while tracing JE status semantics for VAT reconciliation. |
| Fix Applied | `20260702000005_gl_reversal_visibility.sql` adopts the both-visible convention: every reversal/void path was verified to post a swapped-line counter-JE (SI/OR/VB void, PV cancel, banking reversal, `fn_reverse_je`), so `vw_general_ledger`, `vw_trial_balance`, and `fn_vat_gl_reconciliation` now include `je.status IN ('posted','reversed')`. Reversed activity nets to zero while both entries remain on the ledger as audit trail; drafts stay excluded. Validated by seeded scenario GL-REVERSAL-001 (`supabase/tests/009_gl_reversal_visibility_test.sql`): after `fn_reverse_je` the expense account nets to zero in GL and TB with both entries visible, whole-GL zero-net holds, and a same-period reversal restores VAT reconciliation to zero variance. Full suite 135/135 confirms aging-to-GL and VAT scenarios are unaffected. |
| Remaining Risk | Resolved in session 25: the page-level consumer audit found only `BooksDashboardPage` aggregating `journal_entries` with `status = 'posted'` (now includes `reversed`; the Books journal pages query source documents, `BooksGeneralJournalPage` uses the fixed view, and the posting/reversal review pages filter by status intentionally). Period-crossing reversals leave each period's activity visible by design (net zero only across the two periods combined). |

### New Finding Detail - PXL-AUD-025

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Medium |
| Root Cause | Every posting function writes `tax_detail_entries.posting_date` as `NOW()::DATE` (system date at posting time) while `document_date` carries the accounting date aligned with `je_date`. Any tax report or export filtering periods by `posting_date` is period-wrong for backdated documents. |
| Files Affected | All `INSERT INTO tax_detail_entries` sites (gap_fill, tax_ledger_completeness, banking, PV EWT, customer CWT, SI post migrations); any view/page filtering `tax_detail_entries` by `posting_date` |
| Business Risk | Period tax reports can silently include/exclude entries by data-entry date rather than accounting date. |
| Accounting Risk | Tax ledger period totals may not match GL period totals when documents are entered after their document date. |
| Tax Risk | VAT/EWT period reports and exports built on `posting_date` misstate BIR periods. |
| Security Risk | None direct. |
| Recommended Fix Plan | Audit all consumers of `tax_detail_entries.posting_date`; standardize period filters on `document_date` (as `fn_vat_gl_reconciliation` already does) or on `tax_period_id`; document the column semantics or rename to `entered_date`. |
| Discovered | 2026-07-02, session 23: first draft of `fn_vat_gl_reconciliation` filtered by `posting_date` and failed its own seeded test for backdated January documents. |
| Fix Applied | Consumer audit executed 2026-07-02 (session 25): zero readers of `posting_date` exist in migrations or frontend — every period filter (`vw_ewt_summary_ap` exposing `document_date AS invoice_date`, `fn_generate_form_2307_issued`, `fn_vat_gl_reconciliation`, and the WT dashboard/EWT summary/ATC summary/CAS DAT/QAP/1601EQ pages) already uses `document_date` or `tax_period_id`. No behavioral defect existed beyond the session-23 draft already fixed. `20260702000006_tax_detail_date_semantics.sql` adds schema `COMMENT`s marking `posting_date` as the system entry date (never for period filtering) and `document_date` as the accounting date. Renaming was rejected as gratuitous churn: all posting-function INSERTs reference the column and would need replacement. |
| Remaining Risk | None specific; existing seeded scenarios (F2307-ISSUED-001 with backdated PVs, VAT-RECON-001) already prove period filtering follows the accounting date. |

### New Finding Detail - PXL-AUD-026

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Root Cause | The migration chain defined RLS policies for every table but never granted table privileges to the PostgREST roles. On a migrations-only database, `authenticated` has no SELECT/INSERT/UPDATE/DELETE on any of the 143 public tables — the entire app is dead through PostgREST. Existing environments work only because they were provisioned under Supabase's legacy auto-expose default privileges, which the platform removes on 2026-10-30; production would break on that date without governed grants. |
| Files Affected | `supabase/migrations/20260702000008_authenticated_table_grants.sql`; every table in the migration chain |
| Business Risk | Any freshly provisioned environment (new project, region move, restore) is completely non-functional; production breaks on the platform's 2026-10-30 legacy-behavior removal. |
| Accounting Risk | None while broken (nothing works); once granted, RLS is the security boundary — verified 2026-07-02 that all 143 public tables have RLS enabled before granting. |
| Tax Risk | Same as business risk. |
| Security Risk | Three earlier direct-write denial assertions (tests 004/007/010) were passing on missing grants rather than the intended RLS policies; with grants restored they now prove the policies themselves (UPDATE/DELETE `USING (false)` silently matches no rows; INSERT `WITH CHECK (false)` raises 42501). `anon` deliberately receives no grants. |
| Fix Applied | `20260702000008_authenticated_table_grants.sql` grants schema usage plus SELECT/INSERT/UPDATE/DELETE on all public tables and USAGE/SELECT on sequences to `authenticated` (ALL to `service_role`), with matching `ALTER DEFAULT PRIVILEGES` so future migration objects inherit the posture. Discovered by RLS-ROLES-001's first `SET ROLE authenticated` query; validated by the full suite (tests 004/007/010 denial assertions rewritten to effect-based checks, 165/165 passing). |
| Remaining Risk | Whether the remote's legacy auto-expose grants match this posture exactly has not been diffed. Correction (2026-07-02, session 28): the discovering session's "pushed to remote" claim was false — `supabase migration list --linked` showed 20260702000008 absent from the remote; it was actually pushed and verified on the hosted project during session 28's remote sync. |
| Discovered | 2026-07-02, session 27, by the first seeded test to run queries as the `authenticated` role. |

### New Finding Detail - PXL-AUD-027

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Root Cause | GL-side void/cancel/bounce paths post a proper reversing counter-JE, but the tax ledger was left un-netted: `fn_void_sales_invoice`, `fn_cancel_payment_voucher`, and `fn_bounce_receipt` never touched `tax_detail_entries`, while `fn_void_vendor_bill` mutated the original rows in place (`is_reversal = true`, `filing_status = 'amended'`) without changing amounts. `fn_vat_gl_reconciliation` sums all rows, and `is_reversal` carried two incompatible meanings (voided-original flag vs born-negative CM/VC rows), so no single filter could be correct. |
| Files Affected | `supabase/migrations/20260702000009_tax_ledger_void_reversal.sql`; superseded function bodies in `supabase/migrations/20260630000021_gap_fill.sql`; `supabase/tests/012_tax_ledger_void_reversal_test.sql` |
| Business Risk | Voiding a posted SI/VB produced a false VAT reconciliation variance that blocked legitimate VAT returns through the session-23 final/filed gate. |
| Accounting Risk | Tax ledger diverged from the GL after any void/cancel/bounce: GL netted to zero, ledger kept full amounts. |
| Tax Risk | A cancelled PV's EWT kept feeding `vw_ewt_summary_ap` and 2307/QAP/1601EQ source data; a bounced OR's CWT stayed claimable as a tax credit. |
| Security Risk | None direct; the VB void path's evidence mutation violated the DEC-002 preserve-original principle. |
| Fix Applied | Shared `fn_reverse_tax_detail_entries` (not user-callable) inserts negating counter-rows (`is_reversal = true`, `reverses_tax_detail_id` link, dated on the reversal date to match the counter-JE period). Wired into SI void, VB void (flag-flip removed), PV cancel, and OR bounce. `is_reversal` semantics now uniform: TRUE only on negative/corrective rows; reversed originals are found via the incoming link (schema comments added). `vw_ewt_summary_ap` excludes reversed originals via `NOT EXISTS`. Backfill un-flips previously mutated VB rows and inserts missing counter-rows for already-voided/cancelled/bounced documents dated at their reversal JE. |
| Remaining Risk | CM/DM/VC cannot be cancelled from posted/applied states today, so their born-negative rows cannot strand; if such transitions are added they must call the same helper. Cash sales write no tax detail at all (pre-existing completeness gap in PXL-AUD-014/PXL-DA-008 scope). |
| Discovered | 2026-07-02, session 28, while tracing PXL-AUD-014 report standardization against the session-23 reconciliation gate. |

### New Finding Detail - PXL-AUD-028

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Root Cause | `fn_save_cash_sale` had never been executed (no seeded test covered it; plpgsql compiles lazily). It inserted into non-existent `sales_invoices` columns `remarks` (actual column: `memo`) and `total_net_amount` (the same phantom column PXL-AUD-023 removed from `fn_post_sales_invoice`), passed NULL into the NOT NULL `receipts.payment_mode_id`, inserted the receipt as `posted` before its lines (correctly blocked by the PXL-AUD-005 line-immutability trigger), computed totals from `net_amount`/`vat_amount` payload fields that `CashSalesPage` never sends (all-zero documents), over-applied the SI by putting the gross in `receipt_lines.payment_amount` alongside the CWT, and carried no ATC for CWT (rejected by the PXL-AUD-007 validator). |
| Files Affected | `supabase/migrations/20260703000002_vat_ledger_completeness.sql`; superseded body in `supabase/migrations/20260630000021_gap_fill.sql`; `src/pages/CashSalesPage.tsx`; `supabase/tests/015_vat_ledger_completeness_test.sql` |
| Business Risk | The entire cash sale feature was runtime-dead; any attempt failed with a column error. Had the column names matched, UI-submitted cash sales would have posted with zero amounts. |
| Accounting Risk | Zero-amount SI/OR/JE documents and an AR over-application by the CWT amount. |
| Tax Risk | No output VAT, no CWT evidence, and no ATC for any cash sale. |
| Security Risk | None direct. |
| Fix Applied | In `20260703000002`: amounts recomputed server-side from quantity/price/discount and the VAT code rate exactly like `fn_save_sales_invoice` (UI values not trusted); header classification totals split regular/zero-rated/exempt; `memo` column fixed; `payment_mode_id` defaults to the seeded CASH mode; receipt inserted draft → lines → posted; `receipt_lines.payment_amount` = gross − CWT with `atc_code_id` from the new `cwt_atc_id` header field (PXL-AUD-007 validator now passes and enforces the rate); per-code output VAT + CWT ledger rows written. `CashSalesPage` gained the ATC selector and sends `cwt_atc_id`. |
| Remaining Risk | Cash sale remains a single-RPC capture-and-post flow: if a sales approval workflow is configured (DEC-010), cash sales are blocked for the creator by design — the operator must use the SI + OR flow with approval. |
| Discovered | 2026-07-03, session 32, by the first seeded execution of `fn_save_cash_sale` (VAT-LEDGER-COMPLETE-001). |

### New Finding Detail - PXL-AUD-029

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Medium |
| Root Cause | `AppShellInner` gates nav visibility by querying `sys_feature_enablement.select('feature_key')`, but that table has no `feature_key` column — enablement is keyed by `feature_definition_id` referencing `ref_feature_definitions` (which holds `feature_key`). PostgREST returns 400 (`42703`), the code treats the null result as "no feature records configured", and gating falls back to default-open. |
| Files Affected | `src/components/AppShell.tsx` (feature enablement effect) |
| Business Risk | Per-company feature enablement configured in `FeatureEnablementPage` never affects navigation; every authenticated page load logs a 400 against the REST API. |
| Accounting Risk | None — RLS and server-side checks gate data access; only nav visibility fails open. |
| Tax Risk | None. |
| Security Risk | Low — nav gating is cosmetic; underlying pages remain protected server-side. |
| Fix Applied | Session 42: the query now selects `ref_feature_definitions(feature_key)` through the `feature_definition_id` relationship and maps embedded keys; default-open semantics (no rows configured → all nav shown) preserved. Retested 2026-07-04: the embed shape returns 200 against the local API (previously 400 `42703`), `npm run build` green. The typed Supabase client introduced in the same session turns this whole defect class into a compile-time error. |
| Remaining Risk | None for nav gating; underlying pages were always RLS-protected. |
| Discovered | 2026-07-04, session 40, while live-verifying the snapshot reader UI (the 400 surfaced in the browser console on every page). |

### New Finding Detail - PXL-AUD-030

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | High |
| Root Cause | Introducing generated database types (session 42) surfaced systematic frontend schema drift: 8 pages issued PostgREST queries against columns that do not exist, so those fetches 400 at runtime and the features never worked. Dead columns included `items.item_name/default_uom_id/default_sales_price/unit_price/vat_code_id` (actual: `description/uom_id/standard_selling_price/default_sales_vat_id`), `customers.address/payment_terms_id`, `units_of_measure.uom_name`, `suppliers.supplier_name`, `delivery_receipts.date/customer_tin_snapshot`, `delivery_receipt_lines.delivery_receipt_id/unit_price/vat_code_id/revenue_account_id`, and `vw_slp_export.period_year/period_month/supplier_name`. Same class as PXL-AUD-028/PXL-AUD-029: code that had never been executed against the real schema. |
| Files Affected | `src/pages/QuotationsPage.tsx`; `src/pages/SalesOrdersPage.tsx`; `src/pages/DeliveryReceiptsPage.tsx`; `src/pages/CustomerReturnsPage.tsx`; `src/pages/CashSalesPage.tsx` (customer/item reference lists); `src/pages/AssetAcquisitionPage.tsx`; `src/pages/WarehouseStockSettingsPage.tsx`; `src/pages/PurchaseRegistersPage.tsx` (SLP tab); `src/lib/supabase.ts`; `src/lib/database.types.ts` (new, generated) |
| Business Risk | The quotation → sales order → delivery receipt → customer return capture chain could not load customers/items/UOMs (dropdowns empty), and the SLP purchase register tab returned nothing — features dead since creation. |
| Accounting Risk | None — posting RPCs and the ledger were untouched; the drift was read-side. Customer return prefill now sources unit price from the fulfilled SO line and VAT/revenue defaults from the item master (server RPC still recomputes and validates on save). |
| Tax Risk | SLP register screen/CSV was unusable (the authoritative SLSP export snapshot RPC under PXL-DA-015 was unaffected). |
| Security Risk | None. |
| Fix Applied | Session 42: repaired every query with PostgREST column aliases (`item_name:description`, `supplier_name:registered_name`, `address:registered_address`/`delivery_address`, `date:dr_date`, `uom_name:description`) and relationship embeds (DR → `customers(tin)`; DR lines → `sales_order_lines(unit_price)` + `items(default_sales_vat_id,sales_account_id)`); SLP tab now filters `vw_slp_export` by `bill_date` month range and aggregates per supplier client-side (the never-functional transaction-count column was removed). Introduced `src/lib/database.types.ts` (`npm run gen:types`) and typed the shared client `createClient<Database>`, making table names, columns, RPC names/args, and embeds compile-time checked across all pages. |
| Remaining Risk | Types must be regenerated after every migration (`npm run gen:types`); a CI drift gate is recorded in the product backlog. |
| Discovered | 2026-07-04, session 42, by typing the Supabase client against generated schema types — 71 compile errors, of which these were the genuine runtime defects. |
| Retest Evidence | Executed 2026-07-04: all 10 repaired query shapes probed against the local REST API return 200 (previously 400); `npx tsc -b` zero errors across 206 pages; `npm run build` green; `npm run lint` pre-existing 39 warnings only; `npm test` unaffected (no database change). |

### New Finding Detail - PXL-AUD-019

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | High |
| Root Cause | AP aging treats payment vouchers as the only AP application source and omits vendor credit applications. |
| Files Affected | `src/pages/APAgingPage.tsx`; `src/pages/VendorCreditsPage.tsx`; `supabase/migrations/20260630000021_gap_fill.sql`; `vendor_credit_applications`; `vw_ap_aging` |
| Business Risk | Suppliers can appear overpaid or still payable after credits, causing incorrect payment decisions and supplier disputes. |
| Accounting Risk | AP subsidiary ledger can overstate liabilities and fail to reconcile to vendor-credit postings and GL control expectations. |
| Tax Risk | Input VAT adjustment/vendor credit reporting can be disconnected from the AP aging used for period-end review. |
| Security Risk | No direct access-control issue identified; the risk is reporting completeness and audit reliability. |
| Recommended Fix Plan | Build a server-side AP aging as-of RPC that subtracts both posted PV applications and posted/applied vendor credits dated on or before the as-of date, then add seeded VB/PV/VC reconciliation tests. |

### New Finding Detail - PXL-AUD-020

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | High |
| Root Cause | The vendor credit apply modal omits an application date even though `fn_apply_vendor_credit` stores `applied_date` and defaults it to `CURRENT_DATE`. |
| Files Affected | `src/pages/VendorCreditsPage.tsx`; `supabase/migrations/20260630000021_gap_fill.sql`; `vendor_credit_applications` |
| Business Risk | AP balances and supplier settlements can land in the wrong period when staff apply an existing credit after the intended application date. |
| Accounting Risk | AP aging, supplier ledger, and period-end AP reconciliation can be misstated because the application date is not user-controlled. |
| Tax Risk | Vendor credit timing can affect input VAT adjustment review and period support if the application date does not match the intended accounting period. |
| Security Risk | No direct access-control issue identified; the risk is accounting-date integrity. |
| Recommended Fix Plan | Frontend and server-side application-date validation are in place. Remaining work is seeded AP aging/supplier-ledger validation for backdated and blocked-period application cases. |

### New Finding Detail - PXL-AUD-021

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | High |
| Root Cause | `vendor_credit_applications` supported direct deletes under RLS, but deleting an application did not recalculate vendor credit balance/status or preserve a reversal event. |
| Files Affected | `vendor_credit_applications`; `vendor_credits`; `src/pages/APAgingPage.tsx`; `supabase/migrations/20260701000011_vendor_credit_application_reversal.sql` |
| Business Risk | Supplier balances can change without a controlled reversal, causing payment disputes and unreliable open-credit balances. |
| Accounting Risk | AP aging and supplier ledger can be altered without restoring the vendor credit subledger state or retaining reversal evidence. |
| Tax Risk | Input VAT adjustment support tied to vendor credit application history can become incomplete or inconsistent. |
| Security Risk | Ordinary company members with delete access can remove application rows directly instead of using an authorized accounting reversal workflow. |
| Recommended Fix Plan | Scoped reversal controls are in place. Remaining work is seeded database validation for direct-delete denial, balance/status restoration, AP aging exclusion of reversed applications, and supplier-ledger reconciliation. |

## EWT End-to-End Audit (2026-07-04, Session 47 — audit only)

Definitive end-to-end audit of the Expanded Withholding Tax implementation: ATC/EWT master data and governance, supplier/customer withholding profiles, company withholding settings, PV/OR/CV/cash-sale/cash-purchase withholding flows, GL impact, tax ledger, void/cancel/bounce reversal, 2307 issued/received, SAWT/QAP/1601EQ, DAT/books exports, UX, and pgTAP coverage. No fixes were implemented.

**What is genuinely strong** (verified in code and executed tests): PV EWT with explicit taxable base, income nature, and controlled variance reasons (PV-EWT-PARTIAL-001); proportional EWT on partial payments; multiple ATCs/EWT lines per document; ATC immutability after use with deprecation metadata (PXL-DA-010); the SI/VB/PV/OR void/cancel/bounce counter-row convention (TAX-LEDGER-VOID-001); server-side 2307 generation/supersede/snapshots (F2307-ISSUED-001/SUPERSEDE-001); ledger-backed SAWT/QAP exports with WHT/GL reconciliation and immutable snapshots (WHT-EXPORT-SNAP-001); EWT-payable/CWT-receivable GL account configuration checks at posting; and tax-detail date semantics (PXL-AUD-025).

**Overall EWT maturity: ~55%.** The AP-side PV pipeline (withhold → GL → ledger → QAP/2307) is ~75% production-shaped; the AR-side CWT base is computed on a statutorily wrong basis, the check-voucher EWT path is broken end-to-end, the 1601EQ return has no integrity gate, and accrual-basis withholding does not exist. Findings PXL-AUD-031 through PXL-AUD-049 follow.

### New Finding Detail - PXL-AUD-031

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Module | Sales / AR — Official Receipts, Cash Sales, SAWT |
| Scenario | A VAT customer pays an 11,200 invoice (10,000 net + 1,200 VAT) and correctly withholds 2% CWT on the VAT-exclusive base per RR 2-98: CWT = 200, cash = 11,000. |
| Expected Behavior | The OR records payment 11,000 + CWT 200, posts DR cash 11,000 / DR CWT receivable 200 / CR AR 11,200, and the tax ledger carries income payment 10,000 (net of VAT) for SAWT. |
| Current Behavior | `fn_validate_receipt_line_cwt` derives the CWT base as `payment_amount + cwt_amount` (VAT-INCLUSIVE) and hard-rejects any CWT outside ±0.02 of `rate × (payment + cwt)` with no explicit-base column and no variance-reason escape (unlike PV lines). The statutorily correct 200 is rejected (expected 224 on base 11,200); the only accepted amounts assume withholding on gross-of-VAT. `fn_post_receipt` and `fn_save_cash_sale` then write `tax_base = payment + cwt` (gross) into `tax_detail_entries`, so SAWT/2307-received income payments are overstated by the VAT. |
| Root Cause | Receipt CWT was modeled without an explicit taxable-base column; the PV-side fix (`ewt_tax_base`, `20260701000016`) was never mirrored to `receipt_lines`. |
| Files Affected | `supabase/migrations/20260701000017_customer_cwt_defaults.sql`; `supabase/migrations/20260701000018_atc_effective_date_governance.sql` (`fn_validate_receipt_line_cwt`); `supabase/migrations/20260703000002_vat_ledger_completeness.sql` (`fn_save_cash_sale`); `src/pages/ReceiptsPage.tsx` |
| Accounting Risk | GL amounts themselves stay balanced, but CWT receivable is recorded at amounts that only reconcile to customer 2307s if customers also withhold on gross — mismatched certificates guarantee reconciliation differences and unclaimable CWT. |
| Tax Risk | CWT receivable claimed as ITR tax credit will not match customer-issued 2307s computed on the net base; SAWT income payments include VAT, misstating gross income payments to the BIR. |
| Compliance Risk | SAWT/2307-received evidence diverges from certificates; BIR cross-matching (SAWT vs payor QAP) flags every VAT sale. |
| Business Risk | Accountants cannot record the correct statutory CWT at all — the system blocks the legally right entry; users will be trained into over-withholding assumptions. |
| Suggested Fix | Add `cwt_tax_base` (and optional variance reason) to `receipt_lines`; validate against the explicit base with the PV tolerance/variance mechanics; default the base to the invoice's VAT-exclusive proportion of the amount applied; write that base to `tax_detail_entries`; extend `fn_save_cash_sale` (`cwt_tax_base = total_taxable_amount` proportion). Backfill is not needed (historical rows keep their stated base) but SAWT re-export should be re-verified. |
| Dependencies | None hard; coordinate with PXL-AUD-045 (UI defaults) and PXL-DA-009. |
| Estimated Effort | 1 migration + 1 page + 1 pgTAP scenario (medium session). |
| Fix Applied | Session 49 (2026-07-04), `20260704000003_receipt_cwt_explicit_base.sql`: `receipt_lines.cwt_tax_base` + `cwt_variance_reason` (nullable; legacy rows keep the payment+CWT fallback so history and gross-convention withholding remain valid); `fn_validate_receipt_line_cwt` rewritten to the PV mechanics (explicit base preferred, 0.02 tolerance, controlled variance-reason list — old 4-arg overload dropped); trigger/ready-check/`fn_save_receipt` thread the new columns; `fn_post_receipt` writes the explicit base to `tax_detail_entries`; `fn_save_cash_sale` accepts the net convention first (records the VAT-exclusive base), the legacy gross second, and rejects amounts matching neither with both expected values. `ReceiptsPage` adds a "CWT Base (net of VAT)" column defaulted to the invoice's VAT-exclusive proportion of the amount applied (auto-tracking until manually overridden), a variance-reason select on rate mismatch, and passes both fields to the RPC; `CashSalesPage` shows the statutory net-based CWT hint. Validated by CWT-NET-BASE-001 (`supabase/tests/021_receipt_cwt_net_base_test.sql`, 20 assertions): statutory net-base OR posts balanced JE (cash 11,000 / CWT receivable 200 / AR 11,200) with tax ledger base 10,000; gross fallback still validates; variance mechanics match PV; proportional partial base; cash-sale net/gross/neither behaviors. Full suite 344/344 across 21 files on fresh replay; build + zero-warning lint green; types regenerated. |
| Remaining Risk | Tax detail rows posted BEFORE this fix carry gross bases — SAWT exports for pre-fix periods should be re-verified before filing (no backfill by design: rows state what was recorded). PV-side gross UI defaults continue under PXL-AUD-045; ATC as-of-document-date validation under PXL-AUD-035. |

### New Finding Detail - PXL-AUD-032

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Module | Banking & Treasury — Check Vouchers; Compliance — Form 2307 / QAP |
| Scenario | A company pays rent by check voucher, withholding 5% EWT, then generates the quarter's Form 2307 batch. |
| Expected Behavior | CV EWT is validated like PV EWT (current ATC, rate-on-base, explicit base), carries the supplier, feeds `vw_ewt_summary_ap`, and the quarterly 2307 batch includes it. |
| Current Behavior | `fn_post_check_voucher` writes the `ewt_payable` tax detail row with `counterparty_id = NULL` (check vouchers store a free-text payee + TIN, no supplier FK), `tax_base = gross voucher amount` (no explicit base), and a `ewt_rate` snapshot taken by the UI. There is no rate/base/effective-date validation anywhere on the CV path (`trg_pvl_ewt_validation` applies only to `payment_voucher_lines`); `total_ewt_amount` is free-entry. Because `fn_generate_form_2307_issued` raises when any source row has `supplier_id IS NULL`, a single CV EWT row **blocks generation of the entire quarter's 2307 batch** for all suppliers. |
| Root Cause | The CV module predates the PV EWT hardening (PXL-AUD-007/PXL-DA-010) and was never brought under it; 2307 generation assumes every EWT row is supplier-linked. |
| Files Affected | `supabase/migrations/20260630000024_banking_treasury_functions.sql` (`fn_post_check_voucher`); `supabase/migrations/20260702000007_form2307_version_supersede.sql` (`fn_generate_form_2307_issued`); `src/pages/CheckVouchersPage.tsx` |
| Accounting Risk | Unvalidated EWT amounts credit EWT payable at arbitrary values; base recorded as gross misstates QAP tax bases. |
| Tax Risk | CV EWT can be withheld at wrong rates, against expired/deprecated ATCs, with no variance evidence. |
| Compliance Risk | 2307 batch generation aborts for the whole quarter; suppliers paid by check get no certificate; QAP includes rows with blank payee identity that fail BIR alphalist validation. |
| Business Risk | Companies that pay suppliers primarily by check (the PH norm) cannot run the certificate process at all. |
| Suggested Fix | Add `supplier_id` to `check_vouchers` (or require selecting a supplier when EWT > 0); reuse `fn_validate_payment_voucher_line_ewt` (with explicit `ewt_tax_base`) on CV save/post; write `counterparty_id`; make 2307 generation skip-with-warning (not abort) on unlinked rows. |
| Dependencies | PXL-AUD-033 (same module); PXL-AUD-035. |
| Estimated Effort | 1 migration + 1 page + pgTAP (medium session). |
| Fix Applied | Session 57 (2026-07-05), `20260705000001_cv_ewt_supplier_validation.sql`: `check_vouchers` gains `supplier_id` (FK to suppliers, company-checked), `ewt_tax_base`, and `ewt_variance_reason`; a supplier is REQUIRED whenever `total_ewt_amount > 0`. New header trigger `trg_cv_ewt_validation` reuses `fn_validate_payment_voucher_line_ewt` (current ATC, rate-on-base with 0.02 tolerance, controlled variance reasons — full PV parity), and `fn_post_check_voucher` re-validates at post, records the ATC master rate (not the UI snapshot), writes the explicit base (fallback: gross, the legacy convention), and stamps `counterparty_id` + supplier master TIN/registered name into `tax_detail_entries`. `fn_generate_form_2307_issued` now SKIPS supplier-unlinked rows and returns `skipped_unlinked_count`/`skipped_unlinked_ewt` instead of aborting the batch (all-unlinked quarters raise an actionable message; linked rows missing TIN/ATC still abort). `CheckVouchersPage`: supplier select (defaults payee/TIN from master), EWT base auto-tracking gross until overridden, variance-reason select on rate mismatch, client-side supplier/variance checks; `Form2307IssuedPage` surfaces the skip warning. Evidence: CV-EWT-2307-001 executed (`supabase/tests/022_cv_ewt_2307_test.sql`, 17 assertions); full suite 361/361 across 22 files on fresh replay; build + zero-warning lint green; types + schema summary regenerated. |
| Remaining Risk | ATC validity is still checked as of CURRENT_DATE, not the voucher date (PXL-AUD-035). CV EWT stays header-level (one ATC per voucher); multi-ATC checks need separate vouchers. Legacy posted CVs keep NULL supplier links — their EWT rows are skipped (with warning) at 2307 generation until relinked. |

### New Finding Detail - PXL-AUD-033

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Module | Banking & Treasury — Check Voucher Cancel; Tax Ledger |
| Scenario | A posted check voucher with EWT is cancelled, then the quarter's QAP/2307/1601EQ figures are prepared. |
| Expected Behavior | Per the PXL-AUD-027 convention, cancel inserts a negating counter-row with `reverses_tax_detail_id` pointing at the original, dated on the cancellation date, so `vw_ewt_summary_ap` drops both rows and each period retains its own activity. |
| Current Behavior | `fn_cancel_check_voucher` inserts a bare negative row (`is_reversal = true`) with **no `reverses_tax_detail_id`** and `document_date = the original voucher_date` (not the cancel date). `vw_ewt_summary_ap` filters `is_reversal = false AND NOT EXISTS (reverses link)`, so the cancelled CV's ORIGINAL row still appears in QAP detail, 2307 sources, EWT summary, and 1601EQ prefill — a phantom certificate line — while `fn_wht_gl_reconciliation` (which sums all rows) still passes, so the snapshot gate does not catch it. Cross-period cancels also retroactively change the original period's ledger sum. |
| Root Cause | `20260702000009_tax_ledger_void_reversal.sql` fixed SI/VB/PV/OR but never touched the CV path, which had its own older inline reversal. |
| Files Affected | `supabase/migrations/20260630000024_banking_treasury_functions.sql` (`fn_cancel_check_voucher`); `supabase/migrations/20260702000009_tax_ledger_void_reversal.sql` (convention + view) |
| Accounting Risk | GL nets correctly (JE reversal is fine); the tax subledger presentation diverges from the GL-reconciled totals. |
| Tax Risk | Cancelled withholding still reported as withheld per payee. |
| Compliance Risk | QAP/2307 overstate per-supplier withholding versus what is actually remitted; alphalist-to-return cross-check fails at the payee level while passing in total. |
| Business Risk | Suppliers receive certificates for cancelled payments; BIR mismatch letters. |
| Suggested Fix | Replace the inline insert with `PERFORM fn_reverse_tax_detail_entries('CV', id, CURRENT_DATE, v_fp_id)`; backfill legacy CV counter-rows with the reverses link (match on source_doc_id + negative amounts). Extend test 012 with a CV leg. |
| Dependencies | PXL-AUD-032. |
| Estimated Effort | Small migration + backfill + pgTAP (small session). |
| Fix Applied | Session 57 (2026-07-05), `20260705000001_cv_ewt_supplier_validation.sql` (with PXL-AUD-032): `fn_cancel_check_voucher` now calls `fn_reverse_tax_detail_entries('CV', id, CURRENT_DATE, v_fp_id)` — the counter-row carries `reverses_tax_detail_id` and is dated on the cancellation date, so `vw_ewt_summary_ap` drops both rows and each period retains its own activity, matching the SI/VB/PV/OR convention. Legacy bare CV counter-rows are backfilled: linked to their original via source_doc_id + negated amount (a CV writes at most one `ewt_payable` row, so the match is unambiguous) and re-dated onto their `JE-CV-REV-*` reversal JE where one exists, mirroring the `20260702000009` backfill. Evidence: CV-EWT-2307-001 cancel leg (counter-row link + cancel date + view drop + post-cancel 2307 behavior) in `supabase/tests/022_cv_ewt_2307_test.sql`; the CV leg lives there rather than in test 012. |
| Remaining Risk | Cancelled-CV QAP presentation for PRE-backfill exported snapshots keeps the phantom rows those snapshots recorded (snapshots state what was exported, by design); re-export after this migration reflects the corrected ledger. |

### New Finding Detail - PXL-AUD-034

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Critical |
| Module | Compliance — 1601EQ Return, EWT Working Papers |
| Scenario | The accountant prepares the quarterly 1601EQ, edits the prefilled figures, and marks the return filed. |
| Expected Behavior | Like `vat_returns` (VAT-RECON-001), the return's figures are computed server-side from the tax ledger and the status cannot advance to final/filed while they diverge from the ledger or the ledger does not reconcile to the EWT payable GL account. |
| Current Behavior | `ewt_returns` rows are written by direct PostgREST insert/update from `EWT1601EQReturnPage`; `total_tax_base`/`total_ewt_withheld` are prefilled from `vw_ewt_summary_ap` but remain free-entry inputs; `remitted_prior` and `still_due` are client arithmetic; there is **no reconciliation trigger** equivalent to `trg_vat_returns_status_reconciled`. The PXL-DA-011 guard freezes the row after it leaves draft — it freezes whatever numbers the user typed. The EWT working papers (`compliance_ewt_working_papers_*`, 1601EQ working papers) are free-form amount lists with no tie to the ledger. |
| Root Cause | The VAT-return integrity gate (session 23) was never mirrored to the withholding returns. |
| Files Affected | `supabase/migrations/20260701000003_withholding_tax.sql`; `src/pages/EWT1601EQReturnPage.tsx`; `src/pages/EWT1601EQWorkingPapersPage.tsx` |
| Accounting Risk | Filed-return evidence can contradict the GL and tax ledger with no system objection. |
| Tax Risk | Under- or over-remittance of EWT versus what was actually withheld; `still_due` is whatever the user typed. |
| Compliance Risk | 1601EQ, QAP, and 2307 can each tell a different story for the same quarter. |
| Business Risk | Penalties/surcharges on under-remittance discovered at BIR audit. |
| Suggested Fix | Add `fn_compute_ewt_return(company, year, quarter)` (server-side from the tax ledger) and a status trigger blocking final/filed unless figures match the ledger within 0.01 AND `fn_wht_gl_reconciliation` passes for the quarter; derive `remitted_prior` from the controlled remittance flow (PXL-AUD-041). |
| Dependencies | PXL-AUD-041 (remittance flow feeds `remitted_prior`). |
| Estimated Effort | 1 migration + page rework + pgTAP (medium session). |
| Fix Applied | Session 58 (2026-07-10), `20260710000001_ewt_return_reconciliation_gate.sql`: new `fn_compute_ewt_return(company, year, quarter)` sums the `ewt_payable` tax ledger by `document_date` for the quarter (reversal counter-rows included, so cancelled withholding nets out — the same population `fn_wht_gl_reconciliation` compares to the GL). New `trg_ewt_returns_status_reconciled` (BEFORE INSERT OR UPDATE, fires before the PXL-DA-011 guard) blocks final/filed unless: `remitted_prior >= 0`; `still_due = total_ewt_withheld - remitted_prior` within 0.01; the return's `total_tax_base`/`total_ewt_withheld` match the tax ledger within 0.01; and the `ewt_payable` row of `fn_wht_gl_reconciliation` reconciles for the quarter (unconfigured control account with nonzero ledger also blocks). Metadata-only updates of a validated return pass, mirroring `trg_vat_returns_status_reconciled`. `EWT1601EQReturnPage`: ⚡ Generate now calls the RPC instead of client-summing `vw_ewt_summary_ap`; tax base/EWT withheld are read-only server-computed fields (remitted prior stays operator-entered); a hint explains the finalization gate. Evidence: EWT-RETURN-GATE-001 executed (`supabase/tests/023_ewt_return_gate_test.sql`, 12 assertions — server computation, draft free-entry, ledger-mismatch/arithmetic/negative-prior blocks, reconciled finalize + metadata edit + filing, and an uncontrolled remittance JE blocking the next quarter's finalization); full suite 373/373 across 23 files on fresh replay; build + zero-warning lint green; types + schema summary regenerated. |
| Remaining Risk | `remitted_prior` remains operator-entered (only arithmetic-gated) until PXL-AUD-041 delivers the controlled 0619-E remittance flow to derive it — and that same flow is needed before in-quarter remittance JEs stop blocking finalization, exactly as they block SAWT/QAP exports today. The 1601EQ working papers stay free-form. `fwt_returns` (1601FQ) shares the free-entry pattern but cannot be ledger-gated until something posts FWT tax detail rows. |

### New Finding Detail - PXL-AUD-035

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Resolution | Session 77 (2026-07-13), `20260713000002_atc_document_date_versioning.sql`. `fn_validate_payment_voucher_line_ewt` and `fn_validate_receipt_line_cwt` collapse to a single signature with a trailing `p_document_date DATE` and evaluate the ATC effective window as of `COALESCE(p_document_date, CURRENT_DATE)`. Callers thread the document date: PV (`fn_save_payment_voucher`, `fn_require_pvl_ewt_validation`, `fn_validate_payment_voucher_ewt_ready`), OR (`fn_save_receipt`, `fn_require_receipt_line_cwt_validation`, `fn_validate_receipt_cwt_ready`; cash sales via the receipt-line trigger), and CV (`fn_require_cv_ewt_validation`, `fn_post_check_voucher`). Master-data default triggers intentionally keep CURRENT_DATE. Test 033 (ATC-DOCDATE-VERSION-001) proves a backdated CV validates against the version in force on its voucher date and a not-yet-effective version is rejected. Trusted replacement for the held-out draft `20260710000004`, which stays excluded. |
| Severity | High |
| Module | Tax Master Data — ATC governance; PV/OR validation |
| Scenario | On July 5 the accountant posts a backdated PV dated June 28 (open period) using an ATC whose `effective_to` was June 30, or an ATC that only became effective July 1. |
| Expected Behavior | ATC validity (active, non-deprecated, effective window) is evaluated **as of the document date**, so historical documents reproduce historical validity (audit checklist Q17). |
| Current Behavior | `fn_validate_payment_voucher_line_ewt` and `fn_validate_receipt_line_cwt` hardcode `effective_from <= CURRENT_DATE AND effective_to >= CURRENT_DATE`. The June-valid ATC is rejected for the backdated June PV; a July-only ATC is accepted on a June-dated document. `fn_atc_code_is_current` already accepts `p_as_of_date` but the transaction validators do not pass the document date. Supplier/customer default-ATC triggers also validate against CURRENT_DATE (acceptable for master data, wrong for documents). |
| Root Cause | The governance layer (PXL-DA-010) added as-of capability, but the callers pin it to today. |
| Files Affected | `supabase/migrations/20260701000018_atc_effective_date_governance.sql`; `supabase/migrations/20260701000016_pv_ewt_explicit_basis.sql` (trigger passes no date) |
| Accounting Risk | None direct. |
| Tax Risk | Documents can carry ATCs that were not legally in force on the transaction date; legitimate backdated entries in open periods are blocked. |
| Compliance Risk | QAP/2307 rows can cite ATC/rate combinations invalid for the covered period. |
| Business Risk | Month-end catch-up encoding fails intermittently depending on the calendar date of encoding. |
| Suggested Fix | Thread the document date (PV `voucher_date`, OR `receipt_date`, CV `voucher_date`) through the validators and triggers; validate the effective window as of that date. |
| Dependencies | None. |
| Estimated Effort | Small migration + pgTAP additions to test 006 (small session). |

### New Finding Detail - PXL-AUD-036

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Resolution | Session 77 (2026-07-13), `20260713000002_atc_document_date_versioning.sql`. The global `UNIQUE (code)` key (`atc_codes_code_key`) is replaced by version-aware uniqueness `uq_atc_code_version (code, tax_category, effective_from)`, so a BIR rate change is a successor row under the same official code. `fn_enforce_atc_version_rules` (BEFORE INSERT/UPDATE) rejects overlapping active windows for the same code+category and validates that a `supersedes_atc_code_id` link keeps the same official code/category starting strictly later; `uq_atc_code_direct_successor` allows one direct successor per version. `fn_atc_version_asof(code, category, as_of)` resolves the version in force on a date. `fn_guard_atc_code_history` now also freezes `effective_from` once used (rate/code/category already frozen); `effective_to` stays closable. Test 033 covers coexisting versions, overlap rejection, successor integrity, and used-version immutability. |
| Severity | High |
| Module | Tax Master Data — ATC rate versioning |
| Scenario | The BIR revises an ATC rate (e.g., WI157 1% → 2%). The admin deprecates the old row and creates the successor, as the guard's own error message instructs. |
| Expected Behavior | A successor ATC row with the SAME official code and the new rate, effective from the change date; historical documents keep the old rate, new documents use the new one; QAP/2307/SAWT keep reporting the official BIR code. |
| Current Behavior | `atc_codes.code` is globally UNIQUE (`atc_codes_code_key`) and `trg_atc_code_history_guard` makes code/category/rate immutable once used. A successor row therefore **cannot reuse the official code** — the only options are mangling the code text (which corrupts every alphalist/certificate that reports it) or never changing the rate. `supersedes_atc_code_id` exists but nothing resolves "current version of code X" anywhere. There is also no seeded UI/flow for deprecate-and-succeed. |
| Root Cause | Single-row-per-code design; versioning bolted on via immutability without a version key. |
| Files Affected | `supabase/migrations/20260628000003_sprint2.sql` (unique key); `supabase/migrations/20260701000018_atc_effective_date_governance.sql` |
| Accounting Risk | None direct. |
| Tax Risk | The first real BIR rate change is unimplementable without schema surgery — the exact scenario the "low maintenance" principle (P11) promises to handle without a developer. |
| Compliance Risk | Alphalists must carry official ATC codes; mangled successor codes fail DAT validation. |
| Business Risk | Emergency migration under filing deadline pressure when a rate change lands. |
| Suggested Fix | Replace the unique key with `UNIQUE (code, effective_from)` (or a partial unique index on current rows); resolve ATC pickers/validators to the version effective on the document date (combines with PXL-AUD-035); keep per-row immutability after use. |
| Dependencies | PXL-AUD-035. |
| Estimated Effort | 1 migration + picker updates (medium session). |

### New Finding Detail - PXL-AUD-037

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Resolution | Session 83 (2026-07-13), `20260713000010_withholding_basis_policy.sql`. `companies.ap_ewt_recognition_policy` now defaults to `accrual_at_source` and supports explicit `payment` compatibility. `vendor_bill_lines` store source-basis EWT ATC/base/amount/nature/variance; supplier `default_atc_code_id` derives line EWT when the company is source-basis. VB posting now credits AP for the net cash payable and credits EWT Payable at source, with `ewt_payable` tax detail sourced to the VB line. PV save/validation/settlement guards reject duplicate PV-level EWT for source-accrued VBs and settle those bills cash-only; payment-policy companies keep the previous PV EWT path. `fn_ap_aging_asof` subtracts source-accrued EWT so AP aging agrees to the net AP liability. Test 037 (WHT-BASIS-001, 16 assertions) covers source accrual, GL/tax reconciliation, duplicate PV rejection, and cash-only settlement; test 006 was pinned to `payment` policy to preserve partial-payment coverage. |
| Severity | High |
| Module | Purchasing — Vendor Bill / withholding basis policy |
| Scenario | An accrual-basis company records December rent as a vendor bill on Dec 29 and pays it Jan 20. |
| Expected Behavior | Under RR 2-98 as amended (RR 12-2001), the obligation to withhold arises at the earlier of payment or when the expense becomes payable/is accrued. The system should support (per company or per document) recognizing EWT payable at VB posting, and the January 1601EQ/QAP timing should follow the December accrual. At minimum the basis should be a configurable, documented policy (audit checklist Q2/Q3). |
| Current Behavior | Fixed for AP/VB/PV: source/accrual is the default policy, payment-basis remains explicitly selectable per company, and duplicate withholding is blocked. CV validation/profile gates, cash-purchase EWT, and supplier down-payment EWT are now covered under PXL-AUD-032/042/043. |
| Root Cause | Payment-basis-only design; no DEC entry records this as a deliberate policy. |
| Files Affected | `supabase/migrations/20260713000010_withholding_basis_policy.sql`; `supabase/tests/037_withholding_basis_policy_test.sql`; `supabase/tests/006_pv_ewt_partial_payment_test.sql`; `src/lib/database.types.ts`; `docs/PXL/PXL_SCHEMA_SUMMARY.md` |
| Accounting Risk | EWT payable is understated at period end for accrued expenses. |
| Tax Risk | Late withholding/remittance versus the accrual/payable trigger can create withholding tax assessment and penalty exposure; pre-EOPT periods may carry additional deduction-support risk. |
| Compliance Risk | 1601EQ quarters misstate when withholding legally arose. |
| Business Risk | Deduction disallowance at BIR audit is often the single largest EWT-related assessment item. |
| Suggested Fix | Completed: per-company policy flag drives source/accrual EWT recognition at VB post while preserving explicit payment-basis compatibility. |
| Dependencies | Closed on the shared posting/tax-detail primitives delivered under PXL-DA-004/DA-009. Explicit withholding profile gates closed under PXL-AUD-042; cash-purchase/down-payment breadth closed under PXL-AUD-043. |
| Estimated Effort | Completed in session 83. |

### New Finding Detail - PXL-AUD-038

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Resolution | Session 77 (2026-07-13), `20260713000003_settlement_total_line_authority.sql`. `fn_save_payment_voucher` and `fn_save_receipt` recompute the header `total_amount` (and `total_ewt`/`total_cwt`) from the persisted lines and ignore the client-supplied header figures — the header becomes a mirror of the line sums, so GL == subledger == tax ledger by construction. `fn_validate_payment_voucher_ewt_ready` and `fn_validate_receipt_cwt_ready` additionally assert `ABS(header.total_amount − SUM(line payment_amount)) <= 0.02` so a header that reaches posting by another path (e.g. a tampered draft) is rejected before a JE is written. Cash sales are unaffected (they post their own JE and do not use these paths). Test 034 (SETTLEMENT-TOTAL-AUTHORITY-001) proves bogus client headers are ignored, the posted JE cash line equals the line sum, and a tampered draft is rejected at posting. |
| Severity | High |
| Module | Purchasing / Sales — PV and OR posting integrity |
| Scenario | A buggy or crafted client calls `fn_save_payment_voucher` with header `total_amount = 10,000` but lines summing to 8,000 (or the receipt equivalent), then posts. |
| Expected Behavior | Posting rejects any header/line divergence: header cash + EWT/CWT totals must equal line sums before a JE is written. |
| Current Behavior | `fn_validate_payment_voucher_ewt_ready` / `fn_validate_receipt_cwt_ready` reconcile ONLY the EWT/CWT totals (±0.02). The cash `total_amount` is accepted from the client header and never compared to `SUM(line payment_amount)`. The JE debits/credits use HEADER totals while bill/invoice application tracking, aging, and the tax ledger use LINE amounts — so GL cash/AP/AR can diverge from the subledger by any amount while every existing test passes. The UI happens to compute headers from lines, so this is a server-integrity gap rather than a UI bug (defense-in-depth violation of P10/P25). |
| Root Cause | Header totals treated as authoritative for the JE, line totals for application, with only the tax slice reconciled. |
| Files Affected | `supabase/migrations/20260701000016_pv_ewt_explicit_basis.sql` (`fn_save/post_payment_voucher`); `supabase/migrations/20260701000017_customer_cwt_defaults.sql` (`fn_save/post_receipt`) |
| Accounting Risk | GL-to-subledger divergence in AP/AR/cash with no detecting control until aging-to-GL reconciliation is run manually. |
| Tax Risk | EWT totals are protected, but the net-cash context they sit in is not. |
| Compliance Risk | Books cash disbursements/receipts (header-sourced) can misstate against the GL. |
| Business Risk | Silent cash misstatement. |
| Suggested Fix | In both post RPCs (or ready-validators): require `ABS(header.total_amount − SUM(lines.payment_amount)) <= 0.02` (or simply recompute header totals from lines server-side and ignore client values, the stronger fix). |
| Dependencies | None. |
| Estimated Effort | Small migration + negative pgTAP tests (small session). |

### New Finding Detail - PXL-AUD-039

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Resolution | Session 77 (2026-07-13), `20260713000004_cm_vc_aware_overapply_guards.sql`. The `fn_save_receipt` AR guard now computes invoice outstanding as `si.total_amount − SUM(non-bounced receipt applications, excluding this receipt) − SUM(applied credit memos)`, and the `fn_save_payment_voucher` AP guard computes bill outstanding as `vb.total_amount − SUM(non-cancelled PV applications, excluding this voucher) − SUM(non-reversed vendor-credit applications on open/applied vendor credits)` — the same balance formula as `fn_ar_aging_asof`/`fn_ap_aging_asof`. Scalar subqueries are used so the two credit sources do not fan out against each other. Built on the `20260713000003` bodies (document-date threading + line-derived header totals preserved). Test 035 (CM-VC-OVERAPPLY-001) proves a full-original collection/payment is rejected after a partial CM/VC and the net balance is accepted. |
| Severity | High |
| Module | Sales / Purchasing — over-application guards vs credit documents |
| Scenario | An 11,200 SI has a 2,000 credit memo applied (balance 9,200). A receipt then applies 11,200. Mirror case: vendor bill with vendor credit applied, then a PV pays the full original amount. |
| Expected Behavior | The over-apply guard uses the same balance formula as AR/AP aging: invoice/bill total − payments − applied CM/VC. The 11,200 receipt is rejected. |
| Current Behavior | `fn_save_receipt` computes outstanding as `si.total_amount − SUM(receipt lines)` — applied credit memos are ignored; `fn_save_payment_voucher` computes `vb.total_amount − SUM(pv lines)` — `vendor_credit_applications` are ignored. `fn_ar_aging_asof`/`fn_ap_aging_asof` DO subtract them, so the guard permits payments that drive aging negative. The receipts UI subtracts CMs client-side, so the normal path looks right, but the server accepts over-application (and computes CWT/EWT on the inflated amount). |
| Root Cause | Guards written before the CM/VC application features and never updated. |
| Files Affected | `supabase/migrations/20260701000017_customer_cwt_defaults.sql`; `supabase/migrations/20260701000016_pv_ewt_explicit_basis.sql`; `supabase/migrations/20260702000003_ar_ap_aging_asof_rpcs.sql` (reference formula) |
| Accounting Risk | Negative AR/AP residuals; duplicate settlement of the credited portion. |
| Tax Risk | EWT/CWT withheld on amounts exceeding the true payable/collectible. |
| Compliance Risk | QAP/SAWT bases inflated for the affected payees. |
| Business Risk | Overpayment of suppliers; over-collection disputes with customers. |
| Suggested Fix | Reuse the aging formula (subtract applied CMs by status/date, non-reversed VC applications) inside both save-guards; add negative pgTAP cases. |
| Dependencies | None. |
| Estimated Effort | Small migration + tests (small session). |

### New Finding Detail - PXL-AUD-040

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | High |
| Module | Compliance — Form 2307 certificate content |
| Scenario | A supplier is paid in January, February, and March; the Q1 certificate is printed for the supplier's ITR support. |
| Expected Behavior | BIR Form 2307 reports income payments **per month of the quarter** (columns for the 1st, 2nd, and 3rd month) per ATC. The data model must retain the monthly split. |
| Current Behavior | Fixed in session 90. `form_2307_issuance_lines` now stores month-1/month-2/month-3 tax base and withheld columns alongside the quarter totals. `fn_generate_form_2307_issued` and `fn_supersede_form_2307_issued` bucket source `vw_ewt_summary_ap.invoice_date` rows by month inside the quarter; sent/acknowledged snapshots include those monthly values in `certificate_lines`; `Form2307IssuedPage` renders each certificate's ATC/nature rows with monthly base/EWT columns and quarter totals. |
| Root Cause | Certificate modeled as quarter summary rather than the official form layout. |
| Files Affected | `supabase/migrations/20260714000002_form2307_monthly_breakdown.sql`; `supabase/tests/044_form2307_monthly_breakdown_test.sql`; `src/pages/Form2307IssuedPage.tsx`; generated `src/lib/database.types.ts`; `docs/PXL/PXL_SCHEMA_SUMMARY.md` |
| Accounting Risk | None. |
| Tax Risk | Fixed: generated and superseded certificates retain the official month-of-quarter basis per ATC/nature. |
| Compliance Risk | Fixed: immutable Form 2307 snapshots now preserve the month-1/2/3 certificate payload needed for audit/reprint evidence. |
| Business Risk | Fixed for newly generated/superseded certificates; old sent/acknowledged evidence is preserved and can be superseded rather than overwritten. |
| Suggested Fix | Completed in `20260714000002`: add month-1/2/3 base+withheld columns, populate from source invoice dates during generation/supersede, include in snapshots, and render the breakdown in the issued-certificate page. |
| Dependencies | Closed. PXL-AUD-032 already linked CV EWT rows to suppliers; this session preserves latest unlinked-row skip-warning behavior while adding monthly buckets. |
| Estimated Effort | Completed in session 90. |

### New Finding Detail - PXL-AUD-041

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Fix | Session 78, `supabase/migrations/20260713000005_withholding_remittance_flow.sql`. New governed `withholding_remittances` header (controlled writes only: RLS SELECT for members, all writes via SECURITY DEFINER RPCs; a posted row must carry its JE via CHECK; PXL-DA-011 header immutability). Registered as posting source type `WHTREM` (added to `ux_journal_entries_live_source`). `fn_save_withholding_remittance`/`fn_post_withholding_remittance`/`fn_void_withholding_remittance` drive the lifecycle through the governed posting engine (`fn_create_posted_journal_entry`/`fn_add_posting_line`/`fn_finalize_journal_entry`, reversal via `fn_reverse_posted_journal_entry`). EWT remittance posts DR EWT Payable / CR settlement (cash-bank); CWT application posts DR settlement (income-tax-due) / CR CWT Receivable; neither writes `tax_detail_entries` (settling the payable/receivable, not a new withholding event). `fn_wht_gl_reconciliation` now EXCLUDES `WHTREM` movements from the GL side, so a mid-quarter remittance no longer breaks `fn_snapshot_wht_export` (QAP/SAWT) or `fn_require_ewt_return_reconciled` (1601EQ) — while an uncontrolled `MANUAL` remittance JE still (correctly) surfaces as variance. `fn_compute_ewt_remitted_prior` derives `remitted_prior` from posted EWT remittances covering the first two months of the quarter, and the 1601EQ gate now validates the return's `remitted_prior` against it (closing the PXL-AUD-034 residue). DEC-019 records the design. |
| Test | WHT-REMITTANCE-001 (`supabase/tests/036_withholding_remittance_flow_test.sql`, 18 assertions): EWT Payable reconciles before/after a controlled remittance (net GL movement 0 but reconciliation still 200 because WHTREM is excluded); QAP export succeeds for a quarter with a mid-quarter remittance; `remitted_prior` derived (200) and voided (0); 1601EQ finalizes when `remitted_prior` matches and is blocked when it diverges; a non-draft remittance cannot be re-posted; CWT application reconciles symmetrically; an uncontrolled MANUAL remittance JE still breaks reconciliation (variance 50). Fresh held-out-safe reset: full pgTAP **648/648 across 35 files**; build/lint green; types + schema summary regenerated. |
| Remaining Risk | A dedicated remittance UI page is deferred to the paused UI lane (DEC-017); the flow is server-complete and callable. `remittance_number` is caller-supplied and unique per company (governed auto-numbering can be layered later, DEC-019). |
| Severity | High |
| Module | Compliance — EWT remittance / CWT application; SAWT/QAP export gate |
| Scenario | January EWT is remitted to the BIR on Feb 10 (0619-E) via a manual JE debiting EWT payable. In April the accountant exports the Q1 QAP. |
| Expected Behavior | Remittances are a controlled flow the reconciliation understands, so withheld-vs-remitted is tracked and quarter exports still reconcile. |
| Current Behavior | `fn_wht_gl_reconciliation` compares the quarter's tax-ledger sum to the NET GL movement on the control account for the same window. The Feb 10 remittance debit (inside Q1) makes GL ≠ ledger, and `fn_snapshot_wht_export` then **hard-blocks the Q1 QAP export** — the migration's own comment acknowledges this ("legitimate remittance JEs ... will surface as variance until a controlled remittance flow exists"). The same applies to SAWT once CWT receivable is credited when applied against income tax due. `ewt_returns.remitted_prior` is free-entry and disconnected from any remittance record. For any real filer remitting monthly, every quarter's QAP export deadlocks. |
| Root Cause | Reconciliation reads gross GL movement; no remittance document type exists to classify control-account debits. |
| Files Affected | `supabase/migrations/20260703000007_report_snapshots_wht_exports.sql`; manual JE flow |
| Accounting Risk | To un-block exports, users will be tempted to post remittances OUTSIDE the control account, corrupting the EWT payable balance. |
| Tax Risk | Withheld-vs-remitted tracking is manual; over/under-remittance invisible. |
| Compliance Risk | QAP/SAWT snapshots (the BIR evidence trail) cannot be produced for normal operating quarters. |
| Business Risk | Guaranteed operational failure the first quarter with a mid-quarter remittance. |
| Suggested Fix | Add a controlled remittance document (per form 0619-E/1601EQ: period covered, amount, JE) and, for SAWT, a CWT-application document; teach `fn_wht_gl_reconciliation` to exclude (or separately reconcile) remittance/application JEs identified by source type. Feed `ewt_returns.remitted_prior` from remittance records. |
| Dependencies | PXL-AUD-034 (return gate uses the same flow). |
| Estimated Effort | Large session (new document type + RPC + reconciliation change + tests). |

### New Finding Detail - PXL-AUD-042

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | High |
| Module | Setup — Company withholding profile |
| Scenario | A company that is NOT a withholding agent (not required to withhold) records a PV; a Top Withholding Agent (TWA) company with `twa_auto_ewt_enabled = true` buys goods from a regular supplier. |
| Expected Behavior | Per DEC-005 (tax profile drives compliance scope), the compliance profile gates withholding surfaces: non-EWT-registered companies don't get EWT fields/dashboards/returns; TWA companies with auto-EWT get the 1%/2% goods/services EWT proposed automatically on qualifying supplier payments. |
| Current Behavior | Fixed in session 84. Active compliance profiles with `ewt_registered = false` now block AP-side EWT payable on VB/PV/CV save/direct-write/post paths, 1601EQ/EWT return rows, and QAP export snapshots. Companies with no profile keep legacy compatibility until setup makes the profile explicit. TWA auto-EWT now defaults source-basis supplier-subject VB lines to WC158 1% for inventory goods and WC160 2% for services when `ewt_registered`, `is_twa`, and `twa_auto_ewt_enabled` are all active as of the document date. |
| Root Cause | Profile built for the calendar; enforcement never wired. |
| Files Affected | `supabase/migrations/20260713000011_withholding_profile_gates.sql`; `supabase/tests/038_withholding_profile_gates_test.sql`; `src/lib/database.types.ts`; `docs/PXL/PXL_SCHEMA_SUMMARY.md` |
| Accounting Risk | Companies not registered as withholding agents can accrue EWT payable they cannot remit under a 1601EQ registration they don't have. |
| Tax Risk | TWA companies required to withhold 1%/2% on regular purchases get no system support — missed withholding is a disallowance risk (mirrors PXL-AUD-037). |
| Compliance Risk | Withholding activity inconsistent with the company's BIR registration profile. |
| Business Risk | Setup promises behavior (auto-EWT toggle) that silently does nothing. |
| Resolution | Session 84 (2026-07-13), `20260713000011_withholding_profile_gates.sql`. Added `fn_company_ewt_payable_enabled`, `fn_require_company_ewt_payable_enabled`, `fn_company_twa_auto_ewt_enabled`, and `fn_twa_ewt_atc_asof`; expanded ATC history protection to `vendor_bill_lines`; repaired the WC160 TWA-service seed to 2% where unused or creates a successor when used; added VB/PV/CV EWT profile triggers and post-status gates; added return/export profile triggers for `ewt_returns` and QAP snapshots; synchronized VB expected EWT from source lines. Test 038 covers non-EWT profile blocks and TWA goods/services defaults. |
| Dependencies | ATC seed completeness; PXL-AUD-035/036. |
| Estimated Effort | Completed in session 84. |

### New Finding Detail - PXL-AUD-043

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | High |
| Module | Purchasing — Cash Purchases; AR/AP advances |
| Scenario | (a) A company pays a professional's fee in cash on the spot (cash purchase) — withholding is mandatory at payment. (b) A customer pays a 50% advance and withholds CWT on it; (c) the company pays a supplier down-payment subject to EWT before any bill exists. |
| Expected Behavior | Withholding can be recorded on cash purchases and on advances/unapplied payments (EWT on advances is due at payment per RR 2-98). |
| Current Behavior | Fixed across sessions 88-89. Cash purchases now support EWT ATC/base/amount, post CR EWT payable and net cash, and write source-line `ewt_payable` tax-detail rows. Official Receipts now support explicit `customer_advance` lines with no SI, require the customer advances clearing account when used, validate CWT ATC/base/amount, post DR cash + DR CWT receivable / CR customer advances, and write source-line `cwt_receivable` tax-detail rows. Payment Vouchers now support explicit `supplier_down_payment` lines with no VB, require the supplier down-payments asset account when used, enforce the company EWT profile gate, validate EWT ATC/base/amount, post DR supplier down payments / CR cash + CR EWT payable, and write source-line `ewt_payable` tax-detail rows. The OR/PV pages expose add-line controls and GL previews for these advance/down-payment cases. |
| Root Cause | Withholding was implemented only on invoice/bill application paths; settlement RPCs skipped null SI/VB lines and no governed clearing accounts existed for advance/down-payment postings. |
| Files Affected | `supabase/migrations/20260713000015_cash_purchase_ewt.sql`; `supabase/migrations/20260714000001_advance_payment_withholding.sql`; `supabase/tests/042_cash_purchase_ewt_test.sql`; `supabase/tests/043_advance_payment_withholding_test.sql`; `src/pages/CashPurchasesPage.tsx`; `src/pages/ReceiptsPage.tsx`; `src/pages/PaymentVouchersPage.tsx`; `src/pages/GLPostingConfigPage.tsx`; `src/components/CompanySetupChecklist.tsx`; `src/components/GLImpactPanel.tsx`; `src/lib/setupReadiness.ts`; generated `src/lib/database.types.ts`; `docs/PXL/PXL_SCHEMA_SUMMARY.md`. |
| Accounting Risk | Fixed for withholding-bearing cash purchases, customer advances, and supplier down-payments: postings now use explicit clearing accounts and reconcile to the posted tax-detail ledger. Later application of an advance/down-payment to a subsequently issued SI/VB remains a separate AR/AP settlement-product enhancement, not part of this withholding-recording defect. |
| Tax Risk | Fixed: legally required withholding at payment can be recorded and validated with ATC/base/rate evidence. |
| Compliance Risk | Fixed: QAP/2307/SAWT source evidence can include the affected cash-purchase, customer-advance, and supplier down-payment withholding rows. |
| Business Risk | Fixed for the previously silent cash/advance withholding bypass. |
| Suggested Fix | Completed: cash-purchase EWT plus OR customer-advance CWT and PV supplier-down-payment EWT have governed save, validation, posting, tax-detail, reconciliation, setup, preview, and UI coverage. |
| Dependencies | No open dependency for withholding recording. Advance/down-payment application mechanics can be expanded later as an AR/AP settlement enhancement. |
| Estimated Effort | Completed across sessions 88-89. |

### New Finding Detail - PXL-AUD-044

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Medium |
| Module | Master Data — duplicate/vestigial withholding structures |
| Scenario | An admin configures a customer as a withholding agent and sets up EWT codes in Tax Setup, expecting transactions to follow. |
| Expected Behavior | One flag and one master drive all withholding behavior (P13, One Source of Truth). |
| Current Behavior | Fixed in session 91. Customer CWT setup and SI/OR behavior now share `customers.is_subject_to_cwt` plus `customers.default_cwt_atc_code_id`; AP supplier withholding defaults use `suppliers.default_atc_code_id` directly against `atc_codes`. The old wrapper tables (`ewt_codes`, `fwt_codes`) and unused wrapper/default columns are removed from the database, seed data, and setup UI. |
| Root Cause | Successive hardening added new structures without retiring the old ones; session 91 consolidates the production path onto ATC masters. |
| Files Affected | `supabase/migrations/20260714000003_withholding_master_consolidation.sql`; `supabase/tests/045_withholding_master_consolidation_test.sql`; `customers`/`suppliers`/`items`; `src/pages/TaxSetupPage.tsx`, `SalesInvoicePage.tsx`, `SalesInvoiceDocumentPage.tsx`, `CustomersPage.tsx`, `SuppliersPage.tsx`, `ItemCatalogPage.tsx`; `src/components/CompanySetupChecklist.tsx`; seeds; generated types/schema docs |
| Accounting Risk | None direct. |
| Tax Risk | Fixed: SI CWT display and OR defaulting now consult the same customer CWT flag/default ATC path. |
| Compliance Risk | None direct. |
| Business Risk | Fixed: setup no longer exposes unused EWT/FWT wrapper rows or duplicate customer withholding flags. |
| Suggested Fix | Completed: migrated legacy wrapper/default values into ATC defaults, dropped the retired columns/tables, removed the UI forms, and added regression coverage for the final schema and ATC usage predicate. |
| Dependencies | None. |
| Estimated Effort | Completed in session 91. |

### New Finding Detail - PXL-AUD-045

| Field | Detail |
| ----- | ------ |
| Status | In Progress |
| Severity | Medium |
| Module | UX — PV/OR/SI withholding defaults |
| Scenario | A user picks a VAT vendor bill (11,200 gross / 10,000 net) on a PV with a 2% ATC and accepts the defaults. |
| Expected Behavior | The default EWT base is the VAT-EXCLUSIVE portion of the amount being settled (10,000 → EWT 200); the SI's captured expected CWT flows to the receipt for the same invoice. |
| Current Behavior | Partially fixed 2026-07-04: `PaymentVouchersPage` now fetches bill VAT/base totals and defaults `ewt_tax_base` to the proportional VAT-exclusive outstanding amount when a bill is selected. Remaining gaps: `SalesInvoicePage.cwt_amount_expected` is free-entry, validated against nothing (no ATC), and is never read by the receipt prefill. |
| Root Cause | Bill/invoice net amounts not loaded in the pages; defaults written before the base semantics were settled. |
| Files Affected | `src/pages/PaymentVouchersPage.tsx`; `src/pages/ReceiptsPage.tsx`; `src/pages/SalesInvoicePage.tsx` |
| Accounting Risk | Systematic over-withholding by the VAT amount whenever defaults are accepted. |
| Tax Risk | Over-withheld EWT on every defaulted VAT purchase (12% × rate too much). |
| Compliance Risk | QAP bases overstated versus supplier invoices. |
| Business Risk | Suppliers dispute short payments; unclaimable excess withholding for payees. |
| Suggested Fix | Remaining: prefill OR CWT from the SI's expected CWT when present; validate SI expected CWT against the customer's default ATC. Depends on PXL-AUD-031 for the OR side to accept the correct value. |
| Dependencies | PXL-AUD-031. |
| Estimated Effort | Small-medium session (frontend only, after 031). |

### New Finding Detail - PXL-AUD-046

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Medium |
| Module | Sales / Cash — receipt header semantics |
| Scenario | A cash sale with CWT posts; later the receipt bounces, or the cash receipts book is exported. |
| Expected Behavior | `receipts.total_amount` means one thing everywhere (the cash portion), and every consumer (bounce JE totals, books exports, receipt registers) derives gross as `total_amount + total_cwt`. |
| Current Behavior | Fixed in session 92. Draft receipt headers now mirror `receipt_lines` for cash and CWT via `fn_sync_receipt_totals_from_lines`, so `receipts.total_amount` is cash received and `receipts.total_cwt` is withheld tax for both standard ORs and cash-sale ORs. Posted cash-sale receipts were backfilled to the same split. Bounce uses the corrected header/gross relationship and reverses the original cash-sale OR JE at 11,200, not 11,400 in the tested 10,000 + 1,200 VAT + 200 CWT scenario. |
| Root Cause | The cash-sale CWT fix (PXL-AUD-028 follow-up) changed line semantics but left the header gross. |
| Files Affected | `supabase/migrations/20260714000004_cash_sale_receipt_total_semantics.sql`; `supabase/tests/046_cash_sale_receipt_total_semantics_test.sql`; `receipts`; `receipt_lines`; generated schema summary/types |
| Accounting Risk | Fixed: bounced cash-sale receipts now reverse with JE header totals equal to line sums, and header-sourced cash reports read cash rather than gross. |
| Tax Risk | None direct (tax ledger uses lines). |
| Compliance Risk | Fixed: cash receipts/register semantics are consistent across OR kinds; gross can be derived as `total_amount + total_cwt`. |
| Business Risk | Fixed: receipt-level cash analysis no longer double-counts CWT for cash sales. |
| Suggested Fix | Completed in session 92: store/recompute `total_amount = cash received`, derive gross as `total_amount + total_cwt`, backfill posted cash-sale receipts, and add a bounce-a-cash-sale pgTAP case. |
| Dependencies | None. |
| Estimated Effort | Completed in small session 92. |

### New Finding Detail - PXL-AUD-047

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Medium |
| Module | Compliance — Form 2307 Received / CWT claiming |
| Scenario | The accountant records a customer's 2307 certificate against a receipt line and marks it claimed; later the receipt bounces. |
| Expected Behavior | Certificate amounts are validated against the receipt line's CWT; claiming is controlled (can't claim more than withheld, can't claim twice, claim ties to an ITR period); a bounced/reversed receipt flags its tracking row as invalid. |
| Current Behavior | Fixed in session 93. `fn_record_form2307_received` validates received certificates against the receipt line CWT, active ATC, posted receipt status, and unreversed CWT tax ledger before upserting evidence; `fn_claim_form2307_received` requires an ITR claim year/quarter and only transitions `received` rows; table guards block direct application DML into terminal states and lock claimed/invalidated rows. `fn_bounce_receipt` now invalidates linked tracking evidence after reversing CWT tax detail. Issued Form 2307 certificates stay immutable when an upstream EWT row is reversed, but sent/acknowledged certificates are flagged with `requires_supersede` and a reason so the user can create corrected evidence. |
| Root Cause | Tracking implemented as plain CRUD before the ledger-integrity work. |
| Files Affected | `supabase/migrations/20260714000005_form2307_received_claim_lifecycle.sql`; `supabase/tests/047_form2307_received_claim_lifecycle_test.sql`; `src/pages/Form2307ReceivedPage.tsx`; `src/pages/Form2307IssuedPage.tsx`; generated schema summary/types |
| Accounting Risk | Fixed: received-certificate evidence cannot exceed posted/unreversed receipt CWT, and bounce reversal removes claimable support by invalidating linked evidence. |
| Tax Risk | Fixed: claimed records carry an ITR year/quarter and are blocked when the underlying receipt is no longer posted/unreversed. |
| Compliance Risk | Fixed: certificate evidence, tax ledger reversals, and issued-certificate supersede prompts stay aligned. |
| Business Risk | Fixed: unsupported ITR credits and stale 2307 evidence are surfaced instead of silently remaining claimable. |
| Suggested Fix | Completed in session 93: move receive/claim transitions to RPCs, validate against receipt-line and unreversed-ledger CWT, lock terminal tracking rows, invalidate linked rows on receipt bounce, and flag issued certificates for supersede after EWT reversal. |
| Dependencies | PXL-AUD-031 completed earlier; no remaining dependency. |
| Estimated Effort | Completed in medium session 93. |

### New Finding Detail - PXL-AUD-048

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Resolution | Session 77 (2026-07-13), folded into PXL-AUD-038's `20260713000003`. Because `fn_save_*` now recompute header `total_ewt`/`total_cwt` to equal the line withholding sums exactly, the GL figure (posted from the header) equals the tax-ledger figure (posted from the lines) with zero drift — the permitted 0.02 header/line tolerance can no longer leave a residual GL-to-ledger variance that blocks SAWT/QAP/DAT exports (reconciled at 0.01). The per-line rate tolerance for genuine rounding is retained. Test 034 asserts `total_ewt` equals the line EWT sum exactly. |
| Severity | Medium |
| Module | Compliance — tolerance alignment |
| Scenario | A PV is saved with header `total_ewt` differing from line EWT by the permitted 0.02 (or many lines each 0.02 off), then Q-end QAP export runs. |
| Expected Behavior | Anything the save/post validators accept can still be exported: validation tolerance ≤ reconciliation tolerance. |
| Current Behavior | Header-vs-line EWT and per-line rate-vs-amount checks tolerate 0.02; the GL takes the header `total_ewt` while the tax ledger takes line `ewt_amount`s; `fn_wht_gl_reconciliation` reconciles at 0.01. A legitimately saved document can therefore create a permanent 0.02 ledger-to-GL variance that blocks SAWT/QAP/DAT exports for the quarter (no adjustment workflow exists to clear it). Per-line 0.02 tolerances also accumulate unbounded across lines. |
| Root Cause | Tolerances chosen independently per layer. |
| Files Affected | `supabase/migrations/20260701000016_pv_ewt_explicit_basis.sql`; `supabase/migrations/20260701000017_customer_cwt_defaults.sql`; `supabase/migrations/20260703000007_report_snapshots_wht_exports.sql` |
| Accounting Risk | Sub-centavo-class GL/ledger drift by design. |
| Tax Risk | Negligible amounts, but export blocking is operational. |
| Compliance Risk | Quarter exports blocked by permitted rounding. |
| Business Risk | Support escalations at filing time. |
| Suggested Fix | Post the LINE-SUM EWT to the GL (not the header figure) or tighten header/line tolerance to 0.00 after server-side recomputation (PXL-AUD-038's stronger fix resolves this too); keep the per-line rate tolerance for genuine rounding. |
| Dependencies | PXL-AUD-038. |
| Estimated Effort | Folded into PXL-AUD-038's session. |

### New Finding Detail - PXL-AUD-049

| Field | Detail |
| ----- | ------ |
| Status | In Progress |
| Severity | Medium |
| Module | UX / GL Impact — withholding posting transparency |
| Scenario | A user posts a check voucher with EWT, or a cash purchase/cash sale, and wants to see the GL impact and drill from the EWT figure to the certificate/alphalist. |
| Expected Behavior | Every EWT-bearing transaction shows a GL Impact preview before posting and the posted JE after (as SI/OR/VB/PV already do), and EWT amounts drill to their tax-ledger rows and onward to 2307/QAP. |
| Current Behavior | `CheckVouchersPage`, `CashPurchasesPage`, and `CashSalesPage` now show `GLImpactPanel`; saved CV/CP sources use the exact rollback preview, and atomic cash sales show a labeled client estimate before their combined SI/OR create-and-post RPC. The remaining gap is drilldown from an EWT/CWT amount to `tax_detail_entries`, then from 2307/QAP rows back to source documents. |
| Root Cause | The GL panel rollout is complete for these pages; tax-evidence routes were not part of the original component contract. |
| Files Affected | `src/pages/CheckVouchersPage.tsx`; `src/pages/CashPurchasesPage.tsx`; `src/pages/CashSalesPage.tsx`; `src/components/GLImpactPanel.tsx` |
| Accounting Risk | Users post withholding entries they cannot preview or explain. |
| Tax Risk | None direct. |
| Compliance Risk | Auditor drill-down (P7 infinite traceability) breaks at every withholding hop. |
| Business Risk | Trust/usability. |
| Suggested Fix | Add EWT/CWT amount → tax-ledger and 2307/QAP line → source-document routes through the PXL-DA-002 trace contract. |
| Dependencies | PXL-DA-001 and PXL-DA-002 complete; reuse the delivered trace contract. |
| Estimated Effort | Small Phase 2 adoption slice for a per-document Tax Impact/evidence link surface. |

## Transaction Experience Audit (2026-07-04, Session 48 — audit only)

Architecture-alignment audit of the transaction forms (Sales, Purchasing, Journal Entry, Receipts, PV, cash documents, CM/DM/VC) against the long-term PXL vision. Output is the new design standard `docs/PXL/PXL_TRANSACTION_EXPERIENCE_STANDARD.md` — the blueprint for Phase 2 UI/UX modernization after current findings close. Per DEC-012, experience gaps are enhancements and live in the standard/backlog, not here; one genuine audit-visibility defect was found (below). Maturity vs the standard: core four (SI/OR/VB/PV) ~60%, JE ~40%, secondary documents (CM/DM/VC/cash/CV) ~30%, overall ~45%.

### New Finding Detail - PXL-AUD-050

| Field | Detail |
| ----- | ------ |
| Status | In Progress |
| Severity | Medium |
| Module | All transaction pages — audit visibility (UI Principle 26; CAS user-activity evidence) |
| Scenario | An approver, auditor, or accountant opens any document (SI/OR/VB/PV/JE/CM/DM/VC/cash/CV) and needs to see who created, edited, approved, posted, or voided it, when, and why. |
| Expected Behavior | Every document view exposes created/updated/approved/posted/voided by+at, the void reason, and lock status without navigation, plus the `sys_audit_logs` history one click away (`UI_UX_PRINCIPLES.md` Principle 26). CAS accreditation expects per-document user-activity evidence to be accessible, not only queryable. |
| Actual Behavior | Partially fixed 2026-07-04: `SalesInvoicePage`, `ReceiptsPage`, `CreditMemosPage`, `VendorBillsPage`, `PaymentVouchersPage`, and `JournalEntriesPage` now render lifecycle facts (created/updated/posted/approved/status where applicable/lock status) and the shared `AuditTrailSection` for saved SIs/ORs/CMs/VBs/PVs/JEs. Remaining transaction pages still do not expose the evidence. |
| Root Cause | Audit UI was built as a shared component but never wired into the document forms; the modal-overlay form pattern left no natural evidence section. |
| Files Affected | `src/pages/SalesInvoicePage.tsx`, `ReceiptsPage.tsx`, `VendorBillsPage.tsx`, `PaymentVouchersPage.tsx`, `JournalEntriesPage.tsx`, `CreditMemosPage.tsx`, `DebitMemosPage.tsx`, `VendorCreditsPage.tsx`, `CashSalesPage.tsx`, `CashPurchasesPage.tsx`, `CheckVouchersPage.tsx`; `src/components/ui/shared.tsx` (`AuditTrailSection`, unused) |
| Business Risk | Approvers act without visible provenance; audit questions require developer/database access. |
| Accounting Risk | None direct — the trail exists; this is an exposure gap, not a data gap. |
| Tax Risk | None direct. |
| Security Risk | Reduced practical oversight: SoD and lifecycle evidence exist but are never seen, so anomalies (e.g., same-person create/post patterns pre-DEC-010 data) go unnoticed. |
| Suggested Fix | Continue adopting the existing `AuditTrailSection` + lifecycle-facts row (created/approved/posted/voided by+at, void reason, lock status) in the document view of each remaining transaction page — the `PXL_TRANSACTION_EXPERIENCE_STANDARD.md` §13 Audit Trail tab is the target shape; roll out adopt-on-touch across the core four first. Verify `sys_audit_logs` RLS scoping supports each read path. |
| Dependencies | None hard; pairs naturally with the `DocumentLayout` shell (standard §15) but must not wait for it. |
| Estimated Effort | Small-medium session (frontend; component exists). |

### New Finding Detail - PXL-AUD-051

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Medium |
| Module | Document numbering / document-type registry governance |
| Scenario | Setting up number series for a company: the governed registry (`ref_document_types`), the UI readiness checks, and the server-side numbering calls disagree on document codes, so a correctly configured registry cannot satisfy every consumer. |
| Expected Behavior | Every document code requested by `useTransactionReadiness` and by `fn_next_document_number` callers exists in `ref_document_types`, and each transaction surface checks readiness with the same code its save/post RPC uses for numbering. |
| Actual Behavior | Discovered 2026-07-10 (session 60) while seeding the demo test environment: (1) `DebitMemosPage` checked readiness for code `DM` but saves through `fn_save_debit_memo`, which numbers with `DM-S`; (2) live RPCs requested codes `JE` (8 inventory/fixed-asset posting functions), `SDM` (`fn_save_supplier_debit_memo`), `PRT` (`fn_save_purchase_return`), and `FA` (`fn_register_fixed_asset`) that did not exist in `ref_document_types`; additionally, the eight fixed-asset/inventory posters called a nonexistent **two-argument** `fn_next_document_number(company_id, code)` signature (numbering is per company+branch+code under DEC-006), so those posting paths failed at runtime; (3) registry codes `JV`/`RJV`/`DM-P`/`PR` have no numbering consumer. |
| Root Cause | `number_series.document_code` was added as free text alongside the `document_type_id` FK, and page/RPC codes evolved without a mechanical check against the registry; the fixed-asset/inventory posters were also written against a numbering overload that never shipped. |
| Files Affected | `supabase/migrations/20260712000002_aud051_numbering_registry_alignment.sql` (fix); `src/pages/DebitMemosPage.tsx`; `supabase/seeds/demo_company_setup_seed.sql`; `supabase/tests/030_document_numbering_registry_test.sql` |
| Business Risk | Users configuring number series from the registry got pages that stayed blocked (DM) or RPCs that failed at post time (FA/inventory), with no setup path that fixed it. |
| Accounting Risk | None direct — numbering failed closed. |
| Tax Risk | None direct; document numbering gaps would surface in CAS numbering evidence (PXL-DA-019). |
| Security Risk | None. |
| Suggested Fix | Add the missing registry rows (JE, FA, PRT, SDM — or migrate RPCs to the governed codes JV/PR/DM-P), align `DebitMemosPage` readiness to the code its save RPC uses, and add a pgTAP assertion that every code passed to `fn_next_document_number` by shipped functions exists in `ref_document_types`. |
| Fix Applied | 2026-07-12 (session 63): (a) added governed rows `JE`, `FA`, `SDM`, `PRT` to `ref_document_types` (Option A — smallest blast radius, keeps existing hosted/demo series valid); (b) fixed `DebitMemosPage` readiness code `DM` → `DM-S` (`DM-S` already governed); (c) branch-scoped repair of all eight two-argument callers — each now passes the branch it already writes onto its journal_entries row (`fn_register_fixed_asset` v_branch_id for FA+JE; `fn_dispose_fixed_asset`/`fn_record_impairment`/`fn_post_depreciation_entry…` v_asset.branch_id; the four inventory posters v_adj/v_tx/v_gi/v_cs.branch_id). The held-out arbitrary-branch overload from `20260710000005` was NOT used. Demo seed realigned so each series points at its own governed type and an FA series is added. |
| Verification | DOCUMENT-NUMBERING-REGISTRY-001 (`supabase/tests/030`, 11 assertions) executed 2026-07-12: every code requested by a shipped function exists in the registry; no deployed function calls the two-argument overload; the four codes and DM-S are governed; a real `fn_register_fixed_asset` call draws an `FA-2026-…` asset number and a `JE-2026-…` acquisition journal, posts a balanced JE, and links it back as an FA source; the branch-scoped JE call used by inventory posters resolves. Fresh reset + full suite 527/527 across 29 owned files; `npm run build` and zero-warning `npm run lint` pass; demo seed idempotent (28 series). |
| Remaining Risk | Registry now contains near-duplicate purchasing codes (`SDM`/`DM-P`, `PRT`/`PR`) and unconsumed accounting codes (`JV`/`RJV`); numbering is correct and unique, so this is a cosmetic registry cleanup for a future session, not a defect. |
| Dependencies | Unblocks PXL-DA-019 (CAS numbering). Coordinate with PXL-AUD-016 (series preflight rollout). |
| Estimated Effort | Done (one migration + one-line UI fix + seed realignment + test). |

### New Finding Detail - PXL-AUD-052

| Field | Detail |
| ----- | ------ |
| Status | Retested Passed |
| Severity | Medium |
| Module | Cash Purchase — item lookup (schema drift) |
| Scenario | A user creates a Cash Purchase and opens the item picker to add item-based lines. |
| Expected Behavior | The picker lists active company items with UOM, standard cost, default input VAT, and default expense account. |
| Actual Behavior | Before the fix, `CashPurchasesPage` selected `uom:units_of_measure(uom_name)` and `items.purchase_account_id`; neither column exists (`units_of_measure` has `uom_code`/`description`, items has `purchase_expense_account_id`), so PostgREST returned 42703 and the `data \|\| []` fallback silently rendered an empty picker. Undetectable until session 60 because the `items` table had never contained rows. |
| Root Cause | Page written against an assumed column shape and never exercised with item data; no seeded item fixtures existed to surface the 400. |
| Files Affected | `src/pages/CashPurchasesPage.tsx:83` |
| Business Risk | Cash purchases could not use item-based lines at all. |
| Accounting Risk | None — the query failed closed. |
| Tax Risk | None. |
| Security Risk | None. |
| Suggested Fix | Applied 2026-07-10 (session 60): select realigned to `uom:units_of_measure(uom_code)` and `purchase_account_id:purchase_expense_account_id` (PostgREST alias keeps downstream code unchanged); `uom_label` maps from `uom_code`. Retested: the exact select shape returns seeded items with embedded UOM via REST; `npm run build` and zero-warning lint pass. |
| Dependencies | None. |
| Estimated Effort | Done (one-line fix). |

## Recommended Fix Sessions

| Session No. | Goal | Findings Included | Why First | Expected Result | Test After Fix |
| ----------- | ---- | ----------------- | --------- | --------------- | -------------- |
| 1 | Fix company edit data-loss bug | PXL-AUD-003 | This is a direct data integrity risk and small isolated UI change. | Editing a company no longer blanks required compliance/profile fields; `PXL_TRANSACTION_MATRIX.md` Company Creation row remains synchronized. | `npm run build`; manual code trace of full company fetch/update payload. |
| 2 | Add transaction readiness/preflight checks for missing setup | PXL-AUD-002, PXL-AUD-016 | Users currently hit database exceptions late; readiness blockers make the app usable for testing. | Sales, receipts, vendor bills, and PV pages show actionable blockers for missing branch, open period, number series, and GL posting config; matrix setup dependencies updated. | `npm run build`; lint relevant pages; manually trace each critical page with missing config state. |
| 3 | Enforce accounting readiness before approval/post | PXL-AUD-009, PXL-AUD-010 | Prevents approved documents that cannot post. | SI and VB cannot be approved/posted without posting accounts on every line and valid VAT codes; matrix validation/accounting rules updated. | `npm run build`; SQL review of approve/pre-post validation; manual UI validation path. |
| 4 | Harden posted document immutability at database level | PXL-AUD-005 | Accounting records must not be mutable through direct PostgREST writes. | Transaction line/header direct UPDATE/DELETE policies or triggers block posted/cancelled/reversed records; matrix edit restriction rows updated. | Migration review; attempt direct update/delete against posted row in local/staging Supabase when available. |
| 5 | Fix AR/AP as-of aging | PXL-AUD-011, PXL-AUD-012 | Aging reports are accountant-facing and currently time-wrong. | AR/AP aging respects as-of date for invoices/bills and applications; matrix report/drill paths updated. | `npm run build`; compare aging as of dates before/after future receipt/PV examples in seeded DB. |
| 6 | Enforce VAT registration behavior | PXL-AUD-006, PXL-AUD-014 | VAT reporting is compliance-critical; non-VAT behavior must be blocked. | Non-VAT/exempt companies cannot create VAT-bearing transactions or VAT returns unless explicitly allowed by configuration; matrix tax rules updated. | `npm run build`; SQL validation tests for VAT and non-VAT companies. |
| 7 | Redesign AP EWT basis and supplier withholding defaults | PXL-AUD-007, PXL-AUD-008 | EWT and 2307 outputs depend on this being correct. | PV EWT is computed/validated from supplier defaults, ATC rate, and net taxable base; matrix EWT/2307 rows updated. | Seed vendor bill/PV cases; assert JE, tax_detail_entries, and 2307 source values. |
| 8 | Server-side 2307 generation and certificate audit trail | PXL-AUD-015 | 2307 generation should not be direct UI table mutation. | Generated certificates come from controlled RPCs, have ATC-level detail, and status transitions are auditable; matrix 2307 row updated. | Build; SQL tests for generate/regenerate/status transitions. |
| 9 | Tighten role-based RLS for setup/control tables | PXL-AUD-004 | Prevents non-admin users from altering periods, COA, tax setup, and number series. | Member/viewer roles cannot mutate control/master setup tables; matrix permissions updated. | RLS tests with owner/admin/member/viewer users. |
| 10 | Make trial balance/report modes accounting-real | PXL-AUD-013 | Financial statements depend on correct GL/TB semantics. | TB has real unadjusted/adjusted/post-closing logic and normal-balance presentation; matrix reporting traceability updated. | Seed JE classifications; compare TB totals and CSV export. |
| 11 | Add critical-flow database integration tests | PXL-AUD-001 | Once the first fixes land, tests prevent regression. | A repeatable test creates full critical flow and asserts journals, ledgers, tax rows, reports, and matrix documented test scenarios. | `npm test` or documented Supabase test command; run in CI. |
| 12 | Clean lint/runtime warnings | PXL-AUD-018 | Reduces noisy data-loading bugs after high-risk accounting issues are contained. | Lint is warning-free or only intentionally suppressed; matrix unchanged unless transaction behavior changes. | `npm run lint`; `npm run build`. |

## Final Deep Audit Addendum

Audit date: 2026-07-01

Purpose: final architecture/accounting/tax/database/compliance/user-workflow validation before any fix sessions. This addendum keeps all prior findings and adds expanded findings using the requested production-readiness format.

Additional evidence reviewed:

- Transaction/RPC surface: `fn_post_sales_invoice`, `fn_post_receipt`, `fn_post_vendor_bill`, `fn_post_payment_voucher`, `fn_post_credit_memo`, `fn_post_debit_memo`, `fn_post_vendor_credit`, `fn_post_cash_sale`, `fn_post_cash_purchase`, bank/treasury posting functions, inventory posting functions, fixed asset posting functions, amortization/revenue recognition posting functions, reversal/void/cancel functions.
- Report/view surface: `vw_general_ledger`, `vw_trial_balance`, `vw_customer_ledger`, `vw_supplier_ledger`, `vw_ap_aging`, VAT review views, EWT summary views, register views.
- UI surface: sales, purchasing, receipts, payment vouchers, journal entries, posting review, reversal review, books, CAS, VAT, withholding, inventory, bank, fixed asset, financial statement, and master-data pages.
- Build evidence remains unchanged: `npm run build` passed, `npm run lint` passed with warnings, no automated test script exists.
- No live Supabase seed execution was available. This final audit is based on static transaction tracing, SQL review, UI flow review, and expected accounting/tax behavior.

### Section A - End-to-End Accounting Simulation Result

The system can represent many ERP documents, but the end-to-end accounting journey is not production-ready. Core SI/OR/VB/PV posting functions create journal entries in happy-path scenarios, but the surrounding controls are incomplete: setup readiness is missing, GL Impact is missing at the transaction level, report drilldown is weak, VAT/EWT logic is not fully enforceable, permissions are too broad, posted immutability is inconsistent, and compliance outputs do not reliably reconcile to a single tax ledger plus GL source of truth.

Critical simulated journey result:

| Step | Expected Control | Actual Validation Result |
| ---- | ---------------- | ------------------------ |
| Company -> branch -> fiscal period -> COA -> tax setup | Guided readiness gate before transactions | Missing; users can enter transactions before setup is complete and only hit late database errors. |
| Customer/supplier/item creation | Tax defaults, ATC, VAT classification, account defaults | Partial; customer CWT and supplier EWT defaults now prefill core receipt/PV withholding, but richer withholding profiles, seeded tests, and item/service account-default enforcement remain incomplete. |
| Sales invoice approval/posting | Revenue, AR, output VAT, period, branch, fiscal period, customer tax profile, JE link | Posting creates JE if approved and configured, but approval can happen before account readiness and no transaction GL Impact preview exists. |
| Receipt posting | Cash/CWT receivable/AR clearing | Posting creates JE, but AR aging and report traceability are weak; CWT tax treatment is not fully compliance-backed. |
| Vendor bill approval/posting | Expense/input VAT/AP, supplier invoice details, VAT classification | Posting creates JE if configured, but approval can happen before expense account readiness. VAT registration behavior is not enforced. |
| Payment voucher posting | AP clearing/cash/EWT payable/2307 source | JE exists, but EWT basis is manually driven and not sufficiently validated against ATC, net-of-VAT base, or supplier default rules. |
| GL -> Trial Balance -> FS -> tax reports | Single reconciled source, drilldown and drillback | Not achieved. Reports mix posted journal, document-derived views, tax-detail views, and UI-level computations. |
| Compliance reports | BIR-ready, traceable, immutable | Not achieved. 2307, VAT, SLSP/SAWT/QAP readiness is partial and not fully reconciled to posted ledgers. |

### Section B - NetSuite-Style GL Impact Review

Initial audit found no reusable transaction-level GL Impact capability. Session 4 added a shared frontend `GLImpactPanel` for Sales Invoice, Receipt, Vendor Bill, and Payment Voucher, with before-posting previews and posted-JE line display where a linked journal entry exists. Credit Memo, Debit Memo, Cash Sale, Cash Purchase, Vendor Credit, bank transactions, inventory transactions, fixed assets, amortization, and revenue recognition still need the same coverage. `PostingReviewPage` and `JournalEntriesPage` allow journal review after posting, but the remaining platform gaps are:

- no server-side preview engine shared with posting RPCs,
- no account-mapping explanation beyond resolved account labels,
- no visible rule saying why each account was selected,
- no transaction-local drilldown to JE lines for most documents outside SI/OR/VB/PV,
- no report-to-source and source-to-report drill path standard,
- no consistent drillback from JE line to source document page.

### Section C - Posting Engine Deep Audit

The posting engine has a reasonable early foundation: most core posting functions run inside PostgreSQL functions, create balanced journal headers/lines, and rely on database transaction rollback if an exception is raised. That is not enough for production.

Material weaknesses:

- RPC execute grants are broad (`GRANT EXECUTE ... TO authenticated`) and functions usually check membership, not posting authority.
- No generalized posting framework exists for account resolution, tax validation, GL impact preview, reversal, audit narration, and report traceability.
- Journal `reference_doc_type` originally only allowed `SI`, `OR`, `CM`, `DM`, `MANUAL`, while later functions insert `VB`, `PV`, `VC`, bank, inventory, asset, amortization, and revenue-recognition references. If migrations do not alter the constraint everywhere, newer posting functions can fail or the reference model is under-specified.
- Polymorphic `reference_doc_id` cannot enforce source-document foreign keys.
- Duplicate posting mostly relies on status checks and unique JE numbers. That may prevent duplicates by error, but it is not a graceful or fully locked concurrency control.
- Reversal/void behavior is implemented in multiple module-specific functions rather than one auditable reversal standard.
- There is no complete invariant that posted source document totals equal its journal totals and tax ledger totals.

### Section D - Journal Entry Validation

| Transaction | Expected Debit | Expected Credit | Actual Logic | Correct? | Finding |
| ----------- | -------------- | --------------- | ------------ | -------- | ------- |
| Sales Invoice | Accounts Receivable | Revenue; Output VAT | `fn_post_sales_invoice` debits AR total and credits revenue/VAT when configured. | Partially | GL Impact and approval readiness are scoped for core flow; VAT-bearing codes are now blocked for non-VAT/exempt companies. Drilldown and seeded validation remain. |
| Official Receipt | Cash; CWT receivable if withheld | Accounts Receivable | `fn_post_receipt` debits cash and CWT receivable, credits AR. | Partially | CWT compliance trail and as-of AR report controls are weak. |
| Vendor Bill | Expense/asset; Input VAT | Accounts Payable | `fn_post_vendor_bill` debits expense lines/input VAT and credits AP. | Partially | Approval readiness is scoped for core flow; VAT-bearing input codes are now blocked for non-VAT/exempt companies. Seeded validation remains. |
| Payment Voucher | Accounts Payable | Cash; EWT payable | `fn_post_payment_voucher` debits AP and credits cash/EWT. | Partially | Scoped ATC/rate validation now blocks arbitrary EWT amounts; supplier defaults and explicit net-of-VAT basis remain open. |
| Credit Memo | Sales returns/allowance; Output VAT reversal | Accounts Receivable | Function exists and writes negative output VAT rows. | Needs Retest | Ledger includes approved/applied states inconsistently; GL Impact missing. |
| Debit Memo | Accounts Receivable | Revenue/other income; Output VAT | Function exists and writes positive output VAT rows. | Needs Retest | Customer ledger may include approved/paid states before consistent posting semantics. |
| Manual Journal Entry | User-defined debit lines | User-defined credit lines | `fn_post_manual_je` exists; journal UI uses RPC. | Partially | No approval/segregation-of-duties control; no clear adjusting/closing classification. |
| Cash Sale | Cash/AR as applicable | Revenue; Output VAT | Function exists. | Needs Retest | Not fully traced to reports/compliance and no GL Impact preview. |
| Cash Purchase | Expense/input VAT | Cash/AP as applicable | Function exists. | Needs Retest | Not fully traced to VAT ledger and AP/cash reports. |
| Inventory Adjustment | Inventory or variance | Inventory or variance | Functions exist. | Needs Retest | Costing, negative stock, and GL traceability need seeded tests. |
| Fixed Asset Acquisition | Fixed asset | Cash/AP/clearing | Functions exist. | Needs Retest | Depreciation/disposal traceability and tax depreciation reconciliation need tests. |

### Section E - Philippine Tax Deep Audit

VAT and withholding are not production-ready. VAT codes exist and tax detail rows are written for some posting functions, but company tax registration is not enforced at the transaction boundary. EWT payable rows are created from payment voucher lines, but taxable basis and ATC/rate validation are not strong enough for 2307/QAP/1601EQ readiness. Compliance pages exist for VAT, EWT/FWT, SAWT, SLSP, QAP, 2307, and returns, but a page existing is not the same as a reconciled compliance engine.

Required tax architecture corrections:

- one authoritative tax ledger,
- server-side tax computation and validation,
- company tax registration gating,
- VAT/non-VAT/exempt/zero-rated behavior by transaction line,
- ATC effective-date/version control,
- reconciliation from tax ledger to GL control accounts,
- compliance report drilldown to posted source documents and JEs,
- immutable filing/export history with regeneration/version audit.

### Section F - Database Architecture Audit

The schema has broad coverage but weak production controls:

- heavy use of polymorphic references prevents database-enforced source-to-JE integrity,
- many RLS write policies use company membership rather than business role,
- some line tables allow update/delete based only on membership via parent,
- CAS/audit triggers exist but coverage and semantic audit fields are incomplete for approvals, posting, voiding, reversing, and report generation,
- indexes exist for core GL tables but large report pages still use broad client-side aggregation,
- concurrency control is localized rather than designed as a system-wide posting lock/versioning model.

### Section G - Multi-User Concurrency Result

Simulated concurrent scenarios show significant risk:

- User A posts while User B edits lines: blocked only where parent/line RLS and status rules are complete; not complete for every module.
- User A and User B post same document: likely one succeeds and one errors through status or unique JE number, but no explicit user-friendly lock/idempotency result exists.
- User A changes number series while User B saves: broad setup permissions make duplicate/skipped numbering and setup tampering possible.
- User A posts while User C changes GL posting config: account selection can change between documents without approval/version audit.
- Future-dated receipt/payment entered by another user can distort historical AR/AP reports because current report logic is not truly as-of.

### Section H - Security Audit

RLS is present and better than no RLS, but the authorization model is not production-grade. Read isolation mostly follows company membership. Write authority is over-broad for setup/control/master data and posting RPCs. Branch is a dimension, not a security boundary. A user with ordinary company membership can potentially initiate accounting-impacting RPCs if they can reach the function call. This is unacceptable for an ERP where posting, period locking, tax setup, and COA changes require segregation of duties.

### Section I - Report Reconciliation

Reports do not yet reconcile end-to-end at auditor standard:

- General Ledger is journal-backed.
- Trial Balance is GL-backed but lacks accounting classifications.
- AR/AP ledgers and aging are partly document/view-backed and have as-of defects.
- VAT reports mix document-derived and tax-detail-derived sources.
- 2307/QAP/SAWT/SLSP depend on upstream tax detail that is not fully validated.
- Financial statements need stricter account classification, normal balance, closing/adjusting entry handling, and drilldown.

### Section J - User Experience Audit

A normal accountant can find many screens, but cannot reliably complete work without guessing setup dependencies and late error messages. A finance manager cannot clearly see GL impact before posting. An auditor cannot drill every report line back to source and journal. A BIR examiner would see CAS pages but not enough immutable traceability, export provenance, and reconciliation evidence. The current UX looks broad but is not yet an accounting-control workflow.

### Section K - Module Completeness

Implemented or partially implemented: company, branch, departments/cost centers, COA, number series, customers, suppliers, items, sales quote/order/delivery, sales invoice, receipt, credit/debit memo, vendor bill, payment voucher, purchase order/receiving/cash purchase/vendor credit/purchase return, manual/recurring journals, bank/treasury, inventory, fixed assets, amortization, revenue recognition, VAT, withholding, income tax, percentage tax, CAS, books, financial statements.

Missing or incomplete for production:

- NetSuite-style GL Impact component,
- account-resolution rule engine,
- approval engine enforcement in posting RPCs,
- role-based permission enforcement for every transaction action,
- as-of report RPCs,
- tax-ledger-first compliance architecture,
- BIR filing/export immutability and versioning,
- complete seeded accounting test suite,
- audit drilldown/drillback contract,
- payroll/treasury/compliance future modules beyond placeholder readiness.

## Final Deep Audit Findings - Expanded Format

| Finding ID | Area | Severity | Status | Description | Business Risk | Accounting Risk | Tax Risk | Security Risk | Root Cause | Files Affected | Recommendation | Estimated Effort | Test Required | Dependencies |
| ---------- | ---- | -------- | ------ | ----------- | ------------- | --------------- | -------- | ------------- | ---------- | -------------- | -------------- | ---------------- | ------------- | ------------ |
| PXL-DA-001 | GL Impact / audit traceability | Critical | Retested Passed | Posting pages now show a shared GL Impact panel. Saved sources use `fn_preview_gl_impact`, which executes the authoritative posting RPC inside a rollback-only subtransaction and returns exact JE lines, account source, posting date, period, branch, balance, and rule explanation; posted sources return the actual JE. Schedule-maintenance, recurring, purchase-return, treasury, inventory, AR/AP, CM/DM, cash, and depreciation paths preflight before posting. Atomic create-and-post cash/fixed-asset forms show a clearly labeled client draft estimate because no source row exists yet. | Users can see the financial effect and actionable posting errors before committing. | The preview and actual post share the same server path for saved sources; no preview rows or lifecycle/tax side effects persist. | VAT/CWT/EWT lines produced by posting are visible in exact previews where applicable. | Preview RPC enforces membership through the underlying posting path; internal JE mutation primitives are not executable by authenticated callers. | Posting rules remain module RPCs, but rollback execution makes the current rule itself the preview engine instead of duplicating it. | `supabase/migrations/20260710000003_posting_engine_preview_trace.sql`; `supabase/tests/025_posting_preview_invariants_test.sql`; `src/components/GLImpactPanel.tsx`; posting pages | Closed. Preserve the rollback contract when adding a new posting source and register the source type/route in the same migration. | Large | GL-PREVIEW-PARITY-001 executed 2026-07-10 (40 assertions): SI preview exactly equals posted JE, preview persists no source/JE/tax changes, locked-period parity, amortization and recurring rollback, operator-selected recurring date, fixed-asset/schedule source links, idempotency, account/period guards, and trace mismatch rejection. Full suite 448/448. | Broader report/compliance drillback remains PXL-DA-002; posting-engine refactor remains PXL-DA-004. |
| PXL-DA-002 | Drilldown / drillback | Critical | Retested Passed | The governed trace contract now resolves source -> JE -> GL plus aggregate report trace sets across financial statements, subledgers/aging, VAT/EWT/CWT, Form 2307, and immutable report snapshots; report rows use direct or filtered trace links and a generic read-only source page. | Accountants and auditors can move from every audited report family to the exact source/ledger evidence without guessing routes. | Financial and subsidiary-ledger rows resolve canonical source keys and posted journal lines. | VAT/WHT/certificate/snapshot families expose the same fail-closed trace contract without rewriting frozen report evidence. | Membership scope and JE/source identity checks reject orphan, cross-company, and mismatched trace requests. | The original implementation stopped at core GL/TB/JE pages; session 61 added canonical report source keys and shared family trace APIs. | `supabase/migrations/20260710000003_posting_engine_preview_trace.sql`; `supabase/migrations/20260711000002_accounting_trace_reports.sql`; `supabase/tests/026_accounting_trace_report_routes_test.sql`; `src/lib/accountingTrace.ts`; `src/components/AccountingTraceLink.tsx`; `src/pages/AccountingTracePage.tsx`; `src/pages/AccountingSourcePage.tsx`; financial/subledger/compliance report pages | Closed. Preserve the canonical source pair and trace-family routing when adding future report rows; richer universal breadcrumbs remain a Phase 2 enhancement, not a release defect. | Medium | Dedicated retest 2026-07-12: ACCOUNTING-TRACE-REPORTS-001 passes 26/26 across financial, subledger, tax, 2307, and snapshot families, including orphan/cross-company fail-closed fixtures and snapshot hash preservation; GL-PREVIEW-PARITY-001 remains green. | PXL-DA-001 complete. |
| PXL-DA-003 | Posting permissions | Critical | Retested Passed | Posting, reversal, void, and many accounting RPCs were granted to all authenticated users and usually checked only company membership. | Ordinary users may not post or reverse accounting records without proper authority. | Fixed: `fn_can_perform(company_id, action, document_type)` (`20260702000010_can_perform_role_actions.sql`) implements the DEC-009 matrix, and the lifecycle gate `fn_require_admin_for_accounting_lifecycle` now routes every restricted-status insert/transition through it — 37 tables carry the gate, `approved` joined the default restricted list (closing the member-approval hole), petty cash voucher approval and `journal_entries` insert/status gained previously missing gates. BEFORE triggers fire inside SECURITY DEFINER RPC DML with `auth.uid()` as the calling user, and every posting/reversal writes a `journal_entries` row, so the JE gate backstops all posting paths without duplicating checks in 38 RPCs. | Unauthorized tax-impacting postings can corrupt compliance reports. | Enforcement was centralized in the trigger gate (the finding's "or equivalent") rather than per-RPC checks. | RLS role model existed but RPC authorization was not role/action based until DEC-009/`fn_can_perform`. | `supabase/migrations/20260702000010_can_perform_role_actions.sql`; `supabase/migrations/20260701000006_permissions_hardening.sql`; `supabase/migrations/20260701000007_lifecycle_permissions_broaden.sql` | Delivered via central `fn_can_perform` + lifecycle gate; finer-grained roles map onto the same actions later. | Large | Executed 2026-07-03: RBAC-CANPERFORM-001 (`supabase/tests/013_can_perform_test.sql`) and extended RLS-ROLES-001 (member denied approve/post, admin allowed); fresh replay + `npm test` 196/196 across 13 files. | Approval SoD (approver ≠ creator with a configured workflow) continues under PXL-DA-012 per DEC-010. |
| PXL-DA-004 | Posting engine architecture | Critical | Retested Passed | A common posting protocol now provides registry-backed source resolution/locking, JE create/add/finalize primitives, stable per-source tax writes, posting/reversal audit events, and exact-opposite reversal mutation; core AR/AP writers use it directly and secondary saved-source writers are compatibility-wrapped behind the same lock/finalization boundary. | Posting modules share one invariant and lifecycle boundary while retaining stable public RPC signatures. | Period/account/branch/balance/source/idempotency checks and exact reversals are centralized; missing/inactive accounts, wrong periods, unbalanced entries, and invalid sources fail closed. | VAT/EWT/CWT detail writes and reversals use shared primitives with stable identity and uniqueness. | Internal helpers are revoked from application roles; only governed public posting RPCs can mutate journals/tax evidence. | Legacy writers evolved independently; sessions 59/61 consolidated their enforcement boundary without a risky rewrite of every module-specific calculation body. | `supabase/migrations/20260710000003_posting_engine_preview_trace.sql`; `supabase/migrations/20260711000001_posting_engine_completion.sql`; `supabase/tests/012_tax_ledger_void_reversal_test.sql`; `supabase/tests/024_vat_registration_all_documents_test.sql`; `supabase/tests/025_posting_preview_invariants_test.sql` | Closed. New writers must register their source and use the common primitives or an equivalent source-locked compatibility wrapper. | Large | Dedicated retest 2026-07-12: focused posting/trace contracts pass 66/66; prior fresh replay/full suite passed 474/474, including exact preview parity, central negative invariants, shared tax reversals, and CM/DM/VC compatibility paths. True concurrent race evidence remains independently tracked under PXL-DA-007. | PXL-DA-003 and PXL-DA-001 complete. |
| PXL-DA-005 | Journal source integrity | High | Retested Passed | `fn_resolve_posting_source` validates registry metadata, physical source existence, source/company agreement, and optional row locking; JE construction and the deferred `trg_journal_entry_source_integrity` constraint use it, while trace readers independently fail closed. | Orphan and cross-company source links are rejected at the writer boundary itself, not only by readers. | Legitimate module journals are durably linked and traceable. | Tax reports retain canonical source keys linked to the same governed sources. | Both the trace readers and the JE writer trigger are covered by executed negatives. | PostgreSQL cannot express a direct FK across polymorphic source tables, so integrity is enforced by the registry-backed resolver and trigger. | `supabase/migrations/20260710000003_posting_engine_preview_trace.sql`; `supabase/migrations/20260711000001_posting_engine_completion.sql`; `supabase/tests/026_accounting_trace_report_routes_test.sql` | Closed. Keep new writers on the governed source registry; the deferred constraint then covers them automatically. | Small | Executed 2026-07-12 in test 026 (29 assertions): with `SET CONSTRAINTS trg_journal_entry_source_integrity IMMEDIATE`, a posted JE referencing a nonexistent SI and a posted JE referencing another company's SI are both rejected by the real trigger at statement time, while a live same-company source is accepted; replica-mode fixtures remain only for the fail-closed trace-reader contract. | PXL-DA-004 complete. |
| PXL-DA-006 | Journal reference type constraint | High | Retested Passed | The narrow check constraint was replaced by a foreign key to the governed `ref_posting_source_types` inventory covering AR/AP, treasury, inventory, fixed assets, schedules, recurring, reversals, and purchase returns. | New module writers no longer fail because their source type is missing from an early enum. | All current source types replay consistently. | Tax-bearing source types use the same inventory. | Unknown source types are rejected by the FK. | The original fixed check did not evolve with modules. | `supabase/migrations/20260710000003_posting_engine_preview_trace.sql`; `supabase/tests/025_posting_preview_invariants_test.sql` | Closed; register each future posting source in the same migration that introduces its writer. | Small | Fresh reset through `20260710000003`, registry coverage assertion, unknown-type rejection, and full 448-test suite pass. | Transaction matrix source inventory. |
| PXL-DA-007 | Duplicate posting / idempotency | High | Retested Passed | Governed saved-source writers take `FOR UPDATE` source locks before any status read or side effect, one-shot/recurring JE uniqueness prevents duplicate originals, and stable tax identity plus partial unique indexes prevent duplicate live/reversal tax rows. | A genuinely simultaneous two-session post of one approved source produces exactly one original JE and tax set, with the loser resolving to a governed no-op. | Sequential duplicate posting and duplicate live source JEs fail centrally; concurrent posts serialize on the source lock. | Tax detail uniqueness is shared across VAT/WHT writers and reversal rows. | Internal primitives are unavailable to application roles. | Earlier module RPCs lacked a shared lock/idempotency protocol; session 61 added it and session 63 executed the real concurrency evidence. | `supabase/migrations/20260710000003_posting_engine_preview_trace.sql`; `supabase/migrations/20260711000001_posting_engine_completion.sql`; `supabase/tests/025_posting_preview_invariants_test.sql`; `supabase/tests/029_posting_race_two_session_test.sql` | Closed. Keep new saved-source writers behind `fn_begin_source_posting` so they inherit the same lock/idempotency protocol. | Small | POSTING-RACE-001 executed 2026-07-12 (test 029, 14 assertions): two real database sessions (dblink) race one approved SI — the second session observably blocks on the source lock (`pg_stat_activity` wait state), resumes after the first commit without error, and exactly one original JE/tax set survives with the SI linked to it; sequential re-post is a no-op; direct duplicate JE/tax rows remain structurally impossible (`ux_journal_entries_live_source`, `ux_tde_vat_source_code`); the committed race fixture is fully cleaned up. | PXL-DA-004 complete. |
| PXL-DA-008 | VAT architecture | Critical | Retested Passed | VAT amounts for SI/VB/CM/DM/cash sale/cash purchase/vendor credit are computed in SECURITY DEFINER RPCs from quantity/price/discount and VAT master rate; registration/direction gates, per-code tax ledger, ledger-backed reviews, GL reconciliation, final/filed gates, and immutable export snapshots are enforced. `20260712000001` removes legacy application mutations on every persisted VAT header/line and updatable register view, eliminating the forged-amount bypass; the register views are security-invoker so company RLS also governs reads. | Operational VAT, management reports, and filing figures now share one server-authoritative evidence chain. | Posted document VAT equals per-code tax detail and the GL VAT controls; mismatches block final/filed returns and exports. | VAT/non-VAT/exempt, regular/zero-rated/exempt, correction, reversal, and snapshot behavior is covered by executed pgTAP scenarios. | Application roles retain RLS-scoped SELECT and governed RPC execution but have no INSERT/UPDATE/DELETE/TRUNCATE path on VAT source tables, updatable register views, or tax detail; foreign-company register rows are hidden. | Server recomputation already existed, but permissive legacy table/view grants and owner-run view semantics left alternate paths around it. | `supabase/migrations/20260701000012_vat_registration_enforcement.sql`; `supabase/migrations/20260702000004_vat_ledger_gl_reconciliation.sql`; `supabase/migrations/20260703000002_vat_ledger_completeness.sql`; `supabase/migrations/20260703000003_vat_review_views_ledger_backed.sql`; `supabase/migrations/20260710000002_vat_registration_all_documents.sql`; `supabase/migrations/20260712000001_vat_amount_rpc_authority.sql`; `supabase/tests/005_non_vat_registration_gating_test.sql`; `supabase/tests/008_vat_ledger_gl_reconciliation_test.sql`; `supabase/tests/015_vat_ledger_completeness_test.sql`; `supabase/tests/024_vat_registration_all_documents_test.sql`; `supabase/tests/028_vat_amount_integrity_test.sql` | Closed. Preserve RPC-only mutations, security-invoker report views, and per-code ledger/GL reconciliation for future VAT-bearing documents; VAT/tax-code effective dating remains under PXL-DA-010. | Large | VAT-AMOUNT-INTEGRITY-001 executed 2026-07-12 (25 assertions): forged payload values ignored across all six persisted VAT families; direct base/view DML and truncate denied; foreign-company register rows hidden; mixed SI/VB classifications reconcile header/lines to tax detail, GL controls, and review views. Focused VAT/trace suite passes 173/173; prior fresh full suite passed 474/474. | Tax setup/versioning remains PXL-DA-010. |
| PXL-DA-009 | EWT/ATC architecture | Critical | Retested Passed | EWT/CWT values are now validated from explicit taxable bases, ATC rate/version, income nature, and controlled variance paths; downstream QAP/SAWT snapshots are ledger-backed. | 2307/QAP/1601EQ/SAWT figures are now tied to posted tax detail and immutable export evidence. | AP clearing, AR clearing, EWT payable, and CWT receivable are reconciled through tax-ledger/GL gates. | Critical withholding reports now retain supplier/customer, ATC, rate, and period evidence for audit tie-out. | Low. | Closed slices store AP EWT basis/ATC/income classification/variance reason, validate server-side, post explicit tax detail, generate 2307 through RPC, add customer CWT defaults plus receipt CWT tax detail, add ATC document-date versioning, and group QAP by supplier + ATC + nature + rate with Form 2307 reconciliation payloads. | `src/pages/PaymentVouchersPage.tsx`; `src/pages/ReceiptsPage.tsx`; `src/pages/Form2307IssuedPage.tsx`; `src/pages/SAWTPage.tsx`; `src/pages/QAPPage.tsx`; `supabase/migrations/20260701000015_form2307_issued_generation_rpc.sql`; `supabase/migrations/20260701000016_pv_ewt_explicit_basis.sql`; `supabase/migrations/20260701000017_customer_cwt_defaults.sql`; `supabase/migrations/20260713000002_atc_document_date_versioning.sql`; `supabase/migrations/20260713000006_qap_multi_atc_reconciliation.sql` | Seeded partial-payment EWT, variance-control, expired-ATC, 2307 generation, ATC document-date/version, and WHT export snapshot scenarios executed; WHT-EXPORT-SNAP-001 now covers multi-ATC same-supplier QAP rows, Form 2307 tie-out, immutable snapshot-backed downloads, and per-report reconciliation blocking. | Large | None for this finding. Follow-on cash-purchase/advance withholding breadth remains tracked under PXL-AUD-043. | ATC versioning delivered under PXL-DA-010; controlled remittance flow delivered under PXL-AUD-041; AP basis policy delivered under PXL-AUD-037; explicit profile gating delivered under PXL-AUD-042. |
| PXL-DA-010 | Tax code governance | High | Retested Passed | Global tax/VAT/ATC code writes were controlled by broad admin concepts and lacked explicit effective-date/version governance for statutory changes. | Tax code drift can silently change future or historical computations. | Closed: ATC governance (`20260701000018` + `20260713000002`) effective-dates and version-locks withholding codes, and `20260713000012_tax_code_effective_date_governance.sql` extends the identical release pattern to `tax_codes` (the VAT/PT rate holder) and `vat_codes` (the classification/mapping holder): effective-from/to + deprecation/supersession columns; version-aware uniqueness `(code, effective_from)` replacing the global code key; an overlap + successor-integrity guard; immutability of `code`/`tax_type`/`rate`/`effective_from` on a tax code once it has driven a posted computation (`fn_tax_code_used` via the tax ledger or a used VAT code) and of a used VAT code's tax-code/classification/direction; delete protection for used codes; and `fn_tax_code_version_asof`/`fn_tax_code_is_current` resolvers. Because a used version's rate is now frozen and a rate change creates a NEW successor version (a new vat_code the item/document then selects), every already posted line keeps resolving the frozen rate — historical VAT/PT reports are stable without touching the heavy posting RPCs. | Tax reference governance was previously ATC-only. | Closed for tax/VAT codes; broad non-ATC master-data tampering breadth continues under governed master-data work. | `supabase/migrations/20260701000018_atc_effective_date_governance.sql`; `supabase/migrations/20260713000002_atc_document_date_versioning.sql`; `supabase/migrations/20260713000012_tax_code_effective_date_governance.sql`; `src/pages/TaxSetupPage.tsx` | Optional: an admin successor-management UI on `TaxSetupPage` for VAT/PT rate versions (backlog enhancement, not a defect). | Medium | Executed 2026-07-13 as TAX-CODE-VERSION-001 (`supabase/tests/039_tax_code_effective_date_governance_test.sql`, 17 assertions): one official code carries 12%/14% effective-dated versions resolved by document date; duplicate `(code, effective_from)`, overlapping active windows, and mis-ordered successors are rejected; a tax code used through the tax ledger or a used VAT code has its rate/effective-start/code frozen and cannot be deleted; an unused version stays editable; a used VAT code's classification is frozen; and the historical 12% version keeps its rate after the 14% successor + deprecation. | Role hardening. |
| PXL-DA-011 | Posted immutability coverage | Critical | Retested Passed | Posted-document immutability was not uniformly enforced on every header and line table. | Users or API clients could alter accounting evidence after posting. | Fixed (session 43, `20260704000002_status_immutability.sql`): two generic SECURITY DEFINER trigger guards. `fn_guard_doc_lines` blocks line INSERT/UPDATE/DELETE unless the parent status is in the editable set (18 line tables incl. `journal_entry_lines` and `bank_recon_items`; SI/OR/VB/PV keep their PXL-AUD-005 triggers). `fn_guard_doc_header` freezes every business column once a document leaves its editable statuses — only status, `updated_at/by`, and per-table lifecycle metadata (posting stamps, JE linkage, void reason, release/clear dates, application progress) may change; DELETE outside editable statuses is always blocked (DEC-002); `frozen` statuses (posted schedule entries) allow no change at all (34 header tables: AR/AP, banking, inventory, fixed assets, schedules/entries, quotations/SO/DR, non-VAT tax returns; `vat_returns` stays under the PXL-DA-015 snapshot guard). A same-transaction construction exception (`fn_row_written_by_current_txn`: visible row + xmin still in progress ⇒ written by this transaction) keeps every posting writer working — writers insert a posted JE header then its lines in one transaction — while PostgREST clients, whose calls are single-transaction, can never satisfy it. Diff-based column checking tolerates UI full-payload re-saves of unchanged values. | Tax certificates/reports stay supported by immutable posted sources. | Guards run beneath RLS, so the control holds for any role including definer-function side channels. | Some RLS policies checked membership rather than parent status; audit triggers did not replace hard immutability. | `supabase/migrations/20260704000002_status_immutability.sql`; `supabase/tests/020_status_immutability_test.sql`; `supabase/migrations/20260701000009_core_line_immutability.sql` | Delivered via generic guards; future migrations that must backfill non-draft rows need `session_replication_role = replica` around the backfill. | Medium | Executed 2026-07-04 (IMMUT-001, 25 assertions): cross-transaction tampering on posted JE lines/header, posted SI, approved CM lines/header, and approved quotation all blocked; no-change full-payload re-save tolerated; controlled void reverses the JE; CM apply-from-approved works cross-transaction; fresh replay + `npm test` 324/324 across 20 files. | Residues: `tax_detail_entries` direct-write posture continues under tax-ledger findings; CM/DM total columns stay lifecycle-mutable because `fn_save_credit_memo`/`fn_save_debit_memo` zero and recompute them when applying an approved memo (lines remain guarded, so totals are always recomputable evidence). |
| PXL-DA-012 | Approval workflow enforcement | High | Retested Passed | Approval workflow pages existed, but nothing created `approval_instances` and no posting path consulted them: a configured approval was cosmetic. | When a workflow is configured, approval must be a real control: approver ≠ creator, recorded with actor and timestamp, and posting must require it. | Fixed per DEC-010 (`20260703000001_approval_sod_enforcement.sql`): `fn_required_approval_workflow` resolves the governing active workflow (module + optional document-type label, blank = all documents of the module; `amount_exceeds` compares the document total; other trigger conditions are conservatively always-required until evaluators exist), and `fn_enforce_approval_sod` BEFORE triggers on sales_invoices, vendor_bills, receipts, payment_vouchers, purchase_orders, and petty_cash_vouchers block self-approval, record the approval instance (workflow, first step, actor, timestamp), and require a qualifying approval before a two-step post — direct status UPDATEs are caught the same way. Direct-post documents (OR/PV) treat posting as the approval act. Pre-migration approvals qualify through `approved_by` ≠ creator evidence. With no workflow configured, only the DEC-009 role gate applies (workflows are not force-enabled). | Cosmetic approval is worse than none: it implies a control that does not operate (DEC-010). | Trigger-based enforcement mirrors the DEC-009 lifecycle gate instead of rewriting five posting RPCs. | The workflow tables were setup-only; no writer or gate existed anywhere. | `supabase/migrations/20260703000001_approval_sod_enforcement.sql`; `supabase/migrations/20260628000002_sprint1.sql`; `src/pages/ApprovalWorkflowPage.tsx` | Delivered via `fn_required_approval_workflow` + `fn_enforce_approval_sod` triggers; multi-step routing and named-role approvers can layer on later. | Large | Executed 2026-07-03: APPROVAL-SOD-001 (`supabase/tests/014_approval_sod_test.sql`, 14 assertions) — workflow matching incl. thresholds and blank-vs-specific document types, self-approval blocked via RPC and via direct UPDATE, instance recorded with actor/timestamp, unqualified post blocked, legacy evidence honored, deactivated workflow restores role-gate-only behavior; fresh replay + `npm test` 210/210 across 14 files. | `journal_entries` not gated (system JEs indistinguishable from manual ones; needs a discriminator). Approval invalidation on post-approval edits is tracked under PXL-AUD-005/PXL-DA-011. |
| PXL-DA-013 | AR/AP as-of reconciliation | High | In Progress | AR/AP ledgers and aging are not consistently as-of and can include future applications. | Accountants cannot rely on period-end schedules. | Partially fixed: `fn_ar_aging_asof`/`fn_ap_aging_asof` (20260702000003) are now the server-side as-of aging source used by both aging pages, with seeded tests reconciling aging totals to GL AR/AP control accounts at multiple as-of dates. Customer/supplier ledger views remain current-only. | EWT/CWT timing can be wrong. | Low. | Ledger views (`vw_customer_ledger`, `vw_supplier_ledger`) still compute current balances without parameterized as-of logic. | `supabase/migrations/20260702000003_ar_ap_aging_asof_rpcs.sql`; `supabase/tests/002_ar_aging_asof_test.sql`; `supabase/tests/003_ap_aging_asof_test.sql`; `src/pages/ARAgingPage.tsx`; `src/pages/APAgingPage.tsx`; `vw_customer_ledger`; `vw_supplier_ledger` | Extend the as-of pattern to customer/supplier ledger views and add period-end subledger-to-GL reconciliation reports. | Medium | Executed for aging (2026-07-02): future receipt/payment does not affect prior as-of aging; aging total equals GL control. Ledger views still need equivalent tests. | Posting source links. |
| PXL-DA-014 | Financial statement readiness | High | Retested Passed | Trial balance and financial statement logic lacked adjusted/unadjusted/post-closing support, account-classification controls, and a closing process. | Management reports can be misleading. | TB/FS may not comply with accounting close process. | Taxable income bridge can be unreliable. | Low. | Fixed (session 86): `journal_entries.entry_class` (regular/adjusting/closing/opening); `fn_post_manual_je` accepts an entry classification (JournalEntriesPage exposes Regular/Adjusting/Opening); `fn_close_fiscal_year` posts one balanced closing journal (`entry_class='closing'`, `reference_doc_type='CLOSE'`) that zeroes the year's revenue/expense accounts and carries net income/loss to `fiscal_years.retained_earnings_id` (direct-to-retained-earnings close, DEC-019), then locks the year's periods and marks the fiscal year closed; re-close and manual `closing` posting are rejected. `vw_general_ledger` exposes `entry_class` and the Trial Balance mode selector defines unadjusted/adjusted/post-closing. | `supabase/migrations/20260713000013_je_classification_and_close.sql`; `src/pages/TrialBalancePage.tsx`; `src/pages/JournalEntriesPage.tsx`; `supabase/tests/040_financial_close_readiness_test.sql` | Retested (session 86): full pgTAP **737/737 across 39 files** on a held-out-safe reset; test 040 (FINANCIAL-CLOSE-001, 17 assertions) seeds regular + adjusting entries and verifies unadjusted TB, adjusted TB, post-closing TB (revenue/expense zero), retained-earnings net income, year-closed + periods-locked, and re-close rejection; `npm run build` + zero-warning `npm run lint` pass. | Large | Executed as test 040. | GL posting architecture. FS-line mapping table and IS/BS/retained-earnings statement wiring remain backlog enhancements. |
| PXL-DA-015 | Compliance report provenance | Critical | Retested Passed | Every compliance export surface snapshots: VAT final/filed returns, Form 2307 issued, SLSP/RELIEF, SAWT/QAP, CAS DAT extracts, and the seven BIR books of accounts. Session 40 delivered the snapshot reader/drilldown UI, completing the implementation scope. | BIR/external auditors may reject outputs. | Books and reports can disagree. | Critical compliance risk. | Medium. | Compliance outputs were page/view driven without a universal filing/export snapshot model tied to posted ledgers. Sessions 34-36 added the generic `report_snapshots` table, VAT return trigger slice, Form 2307 issued trigger slice, and SLSP/RELIEF export snapshot RPC. Session 37 added the SAWT/QAP slice: `vw_cwt_summary_ar` (ledger-backed SAWT source; the page previously aggregated `receipt_lines` through `sales_invoices`, missing cash-sale CWT and reporting net-of-CWT income payments), `fn_wht_gl_reconciliation` (ewt_payable/cwt_receivable vs GL control accounts), and `fn_snapshot_wht_export` with per-report reconciliation blocking. Session 38 added the CAS DAT slice: `fn_snapshot_cas_export` builds all four extract payloads server-side (the page previously assembled CSVs in the browser and self-inserted `cas_export_log` rows with client-computed counts), gates SLSP/RELIEF on VAT reconciliation, the alphalist on EWT payable, and the GL extract on debit=credit balance, links server-attested `cas_export_log` rows to versioned snapshots, and returns the frozen rows so the downloaded file is provably the hashed payload; direct client inserts into `cas_export_log` are closed. Session 39 added the books slice: `fn_snapshot_books_export` covers all seven BIR books (sales/purchase journals, cash receipts book gross of CWT, cash disbursements book net of EWT plus check vouchers and cash purchases, balanced general journal, cash sales/purchases journals) with the same frozen-rows contract, and all seven Books pages render their files from the frozen payload. Session 40 added `ReportSnapshotsPage` (route `/report-snapshots`, Compliance → Audit & CAS): a filterable read-only list over `report_snapshots` (report type, status, period overlap, RLS-scoped), with drilldown showing the full SHA-256 source hash, per-source version history with click-through between versions, and generic rendering of the frozen report/source payloads including row tables, integrity totals, and reconciliation blocks. | `supabase/migrations/20260703000004_report_snapshots_vat_returns.sql`; `supabase/migrations/20260703000005_report_snapshots_form2307.sql`; `supabase/migrations/20260703000006_report_snapshots_vat_exports.sql`; `supabase/migrations/20260703000007_report_snapshots_wht_exports.sql`; `supabase/migrations/20260703000008_report_snapshots_cas_exports.sql`; `supabase/migrations/20260703000009_report_snapshots_books_exports.sql`; `supabase/tests/008_vat_ledger_gl_reconciliation_test.sql`; `supabase/tests/010_form2307_supersede_test.sql`; `supabase/tests/016_wht_export_snapshots_test.sql`; `supabase/tests/017_cas_export_snapshots_test.sql`; `supabase/tests/018_books_export_snapshots_test.sql`; `src/pages/VATReturn2550MPage.tsx`; `src/pages/VATReturn2550QPage.tsx`; `src/pages/Form2307IssuedPage.tsx`; `src/pages/SLSPExportPage.tsx`; `src/pages/RELIEFExportPage.tsx`; `src/pages/SAWTPage.tsx`; `src/pages/QAPPage.tsx`; `src/pages/CASDATFileGenerationPage.tsx`; the seven Books pages; `src/pages/ReportSnapshotsPage.tsx`; `src/App.tsx`; `src/components/AppShell.tsx` | Done: all six snapshot families plus the reader/drilldown UI are delivered. PXL-DA-019 now closes the CAS hardening residue with exact CAS/books bytes, CRLF DAT artifacts, books reconciliation, and audit-package snapshots. | Large | VAT slice executed: final/filed 2550M creates immutable source snapshots; amount mutation after snapshot is blocked; filing metadata creates a separate filed snapshot. Form 2307 slice executed: sent certificates create versioned immutable snapshots with source hashes; amount mutation after snapshot is blocked; supersede still creates replacement evidence. SLSP/RELIEF slice executed: exports create versioned `exported` snapshots with 64-character hashes and unreconciled periods are blocked. SAWT/QAP slice executed (WHT-EXPORT-SNAP-001): versioned exported snapshots with frozen payer/payee totals, direct-write denial, and per-report reconciliation blocking — an unreconciled EWT payable blocks QAP but not SAWT. CAS DAT slice executed (CAS-EXPORT-SNAP-001): all four extracts create versioned snapshots with server-attested log rows, returned rows equal frozen snapshot rows, forged log inserts denied, per-report gates verified, and session 81 added CRLF `PXL-CAS-DAT-1.0` byte artifacts. Books slice executed (BOOKS-EXPORT-SNAP-001): all seven books create versioned hashed snapshots with server-attested log rows, source-to-export reconciliation, linked balanced GL evidence, and CAS audit package evidence; empty books still evidence. Reader UI verified live 2026-07-04 against local Supabase with seeded final+filed 2550Q snapshots and a BOOKS_SALES_JOURNAL snapshot: list, report/status/period filters, filter-aware empty state, full hash display, version history switching between final and filed evidence, and frozen row/reconciliation tables all render from `report_snapshots` alone. | Tax ledger architecture; audit log. |
| PXL-DA-016 | Audit trail semantics | High | Retested Passed | Audit triggers captured row changes, but approval/post/void/reverse business events were not consistently modeled as semantic audit events with reason and actor role. | Closed: `transaction_events` records governed business lifecycle evidence beside row audit noise. | Posting, reversal, approval, export/snapshot, and registered source status events now carry source type/id, document number, before/after status, reason, actor, actor role, and linked JE where applicable. | Compliance filings and report snapshots emit semantic export/file evidence. | Medium. | Generic row audit is now complemented by a governed lifecycle stream written by SECURITY DEFINER helpers and triggers; direct application writes are not granted. | `supabase/migrations/20260713000014_transaction_events.sql`; `supabase/tests/041_transaction_events_test.sql`; posting/approval/snapshot triggers | Closed. Future source types must register lifecycle expectations and either inherit the registered-source trigger or add an explicit source-specific trigger. | Medium | TRANSACTION-EVENTS-001 executed 2026-07-13 (14 assertions): manual JE post/reversal writes POSTED/REVERSED events with actor role, reason, document number, and legacy posting-event linkage; authenticated direct insert is denied; approval and report snapshot triggers emit APPROVED/EXPORTED events; non-member RLS reads no rows; focused and full trusted pgTAP suites passed. | PXL-AUD-050 remains for per-document UI timeline rollout; workflow and future source-specific contracts continue as normal implementation discipline. |
| PXL-DA-017 | Branch/dimension control | High | Retested Passed | Branch, department, and cost center were not consistently propagated to journal lines. Fixed per DEC-011 (branch = reporting dimension, company = security boundary): `20260704000001_je_line_dimensions.sql` adds `branch_id`/`department_id`/`cost_center_id` to `journal_entry_lines`; `trg_je_dimensions_guard` validates the header branch belongs to the JE company; `trg_je_line_dimensions_guard` welds line company to the JE company, inherits the header branch when a line has none (covers all 34 JE writers centrally, current and future), and validates every line dimension's company; existing lines were backfilled from their headers; `vw_general_ledger.branch_id` became line-accurate (`COALESCE(line, header)` — same name/type/position, so Branch P&L upgraded transparently) with line `department_id`/`cost_center_id` appended; `fn_post_manual_je` accepts per-line dimensions in `p_lines`; `fn_reverse_je` copies line dimensions onto reversal lines. | Branch P&L and cost center reporting can be wrong. | GL lines may lose dimensional traceability. | Tax branch reporting may be incomplete. | None — DEC-011 keeps security company-scoped. | Branch was optional in the GL header and not line-level; dimension integrity was unenforced. | `supabase/migrations/20260704000001_je_line_dimensions.sql`; `supabase/tests/019_je_line_dimensions_test.sql`; `journal_entries`; `journal_entry_lines`; `vw_general_ledger` | Done for propagation/validation. Document-line department/cost-center capture is a backlog enhancement (Dimension summary on documents); stock transfer JEs stay branch-unattributed by design (they span warehouses); `fn_bt_reverse_je` and doc-void counter-JEs inherit the header branch but do not copy line dept/cc (no capture path writes dept/cc on those JEs today — adopt the `fn_reverse_je` pattern when one exists). | Medium | Executed passing 2026-07-04: seeded scenario JE-DIMS-001 (`supabase/tests/019_je_line_dimensions_test.sql`, 14 assertions) — SI branch propagates header→lines, manual JE per-line dept/cc accepted with header-branch fallback, reversal preserves line dimensions, cross-company header branch / line branch / line department rejected, line company divergence blocked, `vw_general_ledger` exposes line dimensions and branch P&L revenue reconciles to the posted SI. | Master data and posting engine. |
| PXL-DA-018 | Report performance / large dataset readiness | Medium | Open | Several reports rely on broad client-side selects/aggregation and non-parameterized views. | Reports will slow or fail as data grows. | Timeout/partial data can create unreliable reports. | Compliance generation can be incomplete under volume. | Low. | Reporting layer was built page-by-page without durable server-side report APIs. | Report pages; `vw_*` views; GL/TB/aging pages | Move heavy reports to parameterized RPCs/materialized summaries where appropriate, add indexes and pagination/export jobs. | Medium | Load test with high-volume journals, invoices, receipts, bills, and tax rows. | Stable report definitions. |
| PXL-DA-019 | CAS/BIR readiness | Critical | Retested Passed | CAS evidence now covers governed document-number issuance/void evidence, exact exported CAS/books bytes, versioned CRLF DAT artifacts, source/GL-reconciled books exports, and a server-attested CAS audit support package. | CAS exports and BIR books should be reproducible, hash-attested, linked to immutable snapshots/artifacts, and packaged with reconciliation evidence. | Implemented in trusted migrations `20260712000004`, `20260713000007`, `20260713000008`, and `20260713000009`, with the audit support page invoking the package RPC. | Residual CAS certification work is operational/legal validation, not an unimplemented core evidence control in this finding. | Low. | CAS was added as a UI/schema layer without full enforcement throughout transaction lifecycle; sessions 64, 80, 81, and 82 added governed numbering/void evidence, exact export-file evidence, `PXL-CAS-DAT-1.0` artifacts, books source/GL reconciliation, and audit package snapshots. | `src/pages/CASDashboardPage.tsx`; `src/pages/CASDATFileGenerationPage.tsx`; `src/pages/CASDocumentVoidRegisterPage.tsx`; `src/pages/AuditSupportPackagePage.tsx`; the seven Books pages; `src/lib/exportDownload.ts`; `supabase/migrations/20260701000005_audit_cas.sql`; `supabase/migrations/20260712000004_cas_numbering_void_evidence.sql`; `supabase/migrations/20260713000007_cas_export_file_hashes.sql`; `supabase/migrations/20260713000008_cas_dat_layout.sql`; `supabase/migrations/20260713000009_books_reconciliation_audit_package.sql`; `supabase/tests/017_cas_export_snapshots_test.sql`; `supabase/tests/018_books_export_snapshots_test.sql`; number-series and transaction migrations | Closed. Keep new export/report surfaces on the same snapshot/hash/reconciliation package model. | Large | CAS-EXPORT-SNAP-001 passes 29 assertions and BOOKS-EXPORT-SNAP-001 passes 22 assertions on a filtered fresh replay through `20260713000009`; direct log forging is denied, DAT exact bytes/artifacts are mirrored, all seven books reconcile to source rows and linked balanced GL entries, and `fn_snapshot_cas_audit_package` blocks periods without reconciled books/export hash evidence. | Numbering, audit events, report snapshots. |
| PXL-DA-020 | Transaction matrix synchronization | High | In Progress | `PXL_TRANSACTION_MATRIX.md` exists, but final audit found transaction behavior gaps that must be reflected whenever fixes change code. `scripts/check_docs_consistency.sh` now runs in CI (2026-07-02): findings-status index agreement and test-book-to-test-file sync are mechanically enforced; matrix row sync remains session discipline. | Developers/CPAs may rely on stale documentation. | Wrong documented accounting rules can lead to wrong fixes. | Wrong tax documentation can lead to wrong compliance behavior. | Low. | The matrix is new and not yet enforced by CI or review process. | `docs/PXL/PXL_TRANSACTION_MATRIX.md`; `docs/PXL/PXL_END_TO_END_AUDIT_FINDINGS.md` | Treat matrix updates as mandatory in every fix session. Add a review checklist or CI/documentation gate before production. | Small | For every `next` fix, verify affected transaction row was updated or explicitly not impacted. | User process discipline. |

## Production Readiness Scores

Scores are intentionally conservative. Passing TypeScript build is not meaningful evidence of accounting production readiness.

| Category | Score | Rationale |
| -------- | ----- | --------- |
| Architecture Score | 42/100 | Broad modules exist, but posting, tax, permissions, traceability, and reporting are not governed by a unified architecture. |
| Accounting Score | 38/100 | Core JE happy paths exist, but GL Impact, approval controls, subledger reconciliation, dimensions, close process, and FS readiness are weak. |
| Posting Engine Score | 35/100 | RPC posting exists but is duplicated, over-broadly granted, and lacks standard locking/idempotency/explanation. |
| Database Score | 44/100 | Schema coverage is broad, but constraints, role-based policies, polymorphic references, immutability, and performance need hardening. |
| Security Score | 32/100 | RLS exists, but ordinary company membership is too powerful for setup and posting actions. |
| Performance Score | 30/100 | Many reports are page/view driven without large-ledger strategy. |
| Tax Compliance Score | 26/100 | VAT/EWT pages exist but tax computation, validation, registration gating, and reconciliation are not production-grade. |
| BIR Compliance Score | 22/100 | CAS/compliance surfaces exist, but immutable provenance, books reconciliation, and filing/export controls are incomplete. |
| Audit Readiness Score | 45/100 | Row audit, governed drilldown, report provenance, CAS evidence, and semantic lifecycle events now exist; remaining risk is per-document UI rollout and future source-specific lifecycle completeness. |
| ERP Readiness Score | 34/100 | The app is a broad prototype/early ERP foundation, not production-ready for Philippine accounting operations. |
| Overall Production Readiness | 32% | Do not go live with real businesses until critical accounting, tax, security, audit, and test findings are fixed and retested. |

## Updated Recommended Fix Sessions After Final Deep Audit

Use these sessions after the existing critical small fix unless a newer blocker appears. Each session must update both this findings file and `PXL_TRANSACTION_MATRIX.md` when transaction behavior changes.

| Session No. | Goal | Findings Included | Why First | Expected Result | Test After Fix |
| ----------- | ---- | ----------------- | --------- | --------------- | -------------- |
| 1 | Fix company edit data-loss bug | PXL-AUD-003 | Direct critical data-loss risk and small isolated fix. | Company edits no longer blank stored compliance/setup fields. | `npm run build`; targeted edit-flow code review. |
| 2 | Add setup readiness gates | PXL-AUD-002, PXL-AUD-016 | Users cannot reliably test accounting flows until required setup is visible. | Transaction pages show/block missing branch, period, number series, COA, tax setup, and GL posting config. | `npm run build`; manual preflight tests for SI/OR/VB/PV. |
| 3 | Enforce posting permissions | PXL-DA-003, PXL-AUD-004 | Unauthorized posting/setup mutation is a production-stopper. | Posting/void/reverse/setup RPCs require explicit role/action permission, not mere membership. | SQL/RLS tests for owner/admin/member/viewer. |
| 4 | Add GL Impact foundation for core AR/AP | PXL-DA-001, PXL-DA-002 | Accountants need visible JE impact before posting and auditors need traceability. | SI, OR, VB, PV show GL Impact preview and posted JE drilldown/drillback. | Preview-vs-posted JE comparison tests. |
| 5 | Enforce accounting readiness before approval/post | PXL-AUD-009, PXL-AUD-010, PXL-DA-012 | Prevents approved documents that cannot post. | SI/VB approval/posting validates accounts, VAT codes, period, and workflow state. | Build plus SQL validation cases for missing accounts/tax config. |
| 6 | Harden posted immutability | PXL-AUD-005, PXL-DA-011 | Posted source documents must not diverge from JE/tax ledger. | Header/line updates/deletes are blocked after posting/cancel/void/reversal. | Direct RLS/update tests against every transaction line table. |
| 7 | Fix AR/AP as-of reporting and reconciliation | PXL-AUD-011, PXL-AUD-012, PXL-DA-013 | Aging schedules are key accountant/auditor reports. | AR/AP aging and ledgers reconcile to GL control accounts as of a date. | Seed future receipt/payment cases; compare subledger to GL. |
| 8 | Correct VAT architecture | PXL-AUD-006, PXL-AUD-014, PXL-DA-008 | VAT errors are critical compliance exposure. | VAT registration gating and tax-ledger/GL reconciliation are enforced. | VAT/non-VAT/mixed-rate seeded tests and report reconciliation. |
| 9 | Correct EWT/ATC/2307 basis | PXL-AUD-007, PXL-AUD-008, PXL-AUD-015, PXL-DA-009 | 2307/QAP/1601EQ are invalid if EWT basis is wrong. | EWT computed/validated from supplier profile, ATC, rate, net taxable base, and posted tax ledger. | PV/EWT/2307 seeded tests including partial payments. |
| 10 | Standardize posting engine and journal source integrity | PXL-DA-004, PXL-DA-005, PXL-DA-006, PXL-DA-007 | Needed before scaling fixes to bank, inventory, assets, amortization, and revenue recognition. | Shared posting invariants, source locks, reference integrity, idempotency, and document-type registry. | Fresh migration and concurrent posting tests. |
| 11 | Add semantic audit trail and CAS/report provenance | PXL-DA-015, PXL-DA-016, PXL-DA-019 | Compliance and audit readiness require more than row audit. | Lifecycle events and filed/exported report snapshots are immutable and traceable. | CAS scenario and report snapshot immutability tests. |
| 12 | Build critical-flow automated test harness | PXL-AUD-001 | Once first controls exist, tests are mandatory to avoid regression. | Seeded integration test proves setup -> SI -> OR -> VB -> PV -> TB/VAT/EWT/2307. | `npm test` or documented Supabase test command in CI. |
| 13 | Financial statement and close readiness | PXL-AUD-013, PXL-DA-014 | FS output depends on correct TB, adjusting, and closing semantics. | Unadjusted/adjusted/post-closing TB, IS, BS, retained earnings, and close process are correct. | Seed adjusting/closing entries; reconcile FS to GL. |
| 14 | Performance and documentation gates | PXL-DA-018, PXL-DA-020, PXL-AUD-018 | Final hardening before production candidate. | Large reports use server-side patterns; lint is clean; transaction matrix is enforced in workflow. | Load test; `npm run lint`; `npm run build`; doc-sync checklist. |

## Fix Session Log

| Date | Session | Findings | Status | What Changed | Test Performed | Remaining Risk | Next Recommended Action |
| ---- | ------- | -------- | ------ | ------------ | -------------- | -------------- | ----------------------- |
| 2026-07-14 | 93 | PXL-AUD-047 | Retested Passed | Form 2307 received claim lifecycle in `20260714000005_form2307_received_claim_lifecycle.sql`. Added claim/invalidated metadata to `form_2307_tracking`, guarded terminal rows, moved received/claim transitions behind `fn_record_form2307_received` and `fn_claim_form2307_received`, validated certificate amount against receipt-line CWT and unreversed CWT ledger, invalidated linked tracking rows from `fn_bounce_receipt`, and added issued-certificate `requires_supersede` flags when source EWT reverses. Updated received/issued Form 2307 pages to use the RPC/status model. | Held-out-safe fresh `supabase db reset --local` passed with drafts `20260710000004`/`00005` moved aside/restored. Focused test 047 (FORM2307-RECEIVED-CLAIM-001) passed 18/18. Neighbor tests 001/007/010/012/016/021/022/044/046/047 passed 184/184. Full trusted pgTAP passed **825/825 across 46 files** with held-out test `027` excluded. `npm run gen:types` passed; schema summary regenerated (261 functions / 20 views / 149 tables / 285 triggers). `npm run lint`, `npm run build`, `bash -n scripts/gen_schema_summary.sh`, `scripts/check_docs_consistency.sh`, `git diff --check`, and `git diff --cached --check` passed. Hosted Supabase push applied `20260714000003`, `20260714000004`, and `20260714000005`; a held-out-safe post-push dry run returned "Remote database is up to date." | No open risk remains for the received Form 2307 claim lifecycle or issued-certificate stale prompt. Hosted Supabase is synced through `20260714000005`; the CLI emitted the known pg-delta catalog-cache warning after apply, but migration history verified clean through the dry run. | Continue PXL-AUD-049, then remaining In-Progress coverage/report items (AUD-008/009/010, DA-013/018/020, AUD-045/050). |
| 2026-07-14 | 92 | PXL-AUD-046 | Retested Passed | Cash-sale receipt total semantics in `20260714000004_cash_sale_receipt_total_semantics.sql`. Added `fn_sync_receipt_totals_from_lines` plus `trg_sync_receipt_totals_from_lines` so draft receipt headers mirror line cash/CWT totals; backfilled posted cash-sale receipt headers to store cash in `total_amount` and CWT in `total_cwt`; gross remains derived as `total_amount + total_cwt`. | Held-out-safe fresh `supabase db reset --local` passed with drafts `20260710000004`/`00005` moved aside/restored. Focused test 046 (CASH-SALE-RECEIPT-TOTAL-001) passed 13/13. Neighbor tests 001/012/015/021/034/046 passed 116/116. Full trusted pgTAP passed **807/807 across 45 files** with held-out test `027` excluded. `npm run gen:types` passed; schema summary regenerated (255 functions / 20 views / 149 tables / 283 triggers). `npm run lint`, `npm run build`, `bash -n scripts/gen_schema_summary.sh`, `scripts/check_docs_consistency.sh`, `git diff --check`, and `git diff --cached --check` passed. | No open risk remains for cash-sale receipt header cash/CWT semantics or bounce JE totals. Hosted Supabase push for `20260714000003` and `20260714000004` completed in session 93. | Continue PXL-AUD-047, then AUD-049 and remaining In-Progress coverage/report items (AUD-008/009/010, DA-013/018/020, AUD-045/050). |
| 2026-07-14 | 91 | PXL-AUD-044 | Retested Passed | Withholding master consolidation in `20260714000003_withholding_master_consolidation.sql`. Migrated legacy customer wrapper defaults into `customers.default_cwt_atc_code_id`, folded `is_withholding_agent` into `is_subject_to_cwt`, migrated supplier wrapper defaults into `suppliers.default_atc_code_id`, replaced `fn_atc_code_used` so it no longer reads wrapper tables, dropped `customers.is_withholding_agent`, all `default_ewt_code_id` columns, and the unused `ewt_codes`/`fwt_codes` tables. Removed the obsolete EWT/FWT Tax Setup tabs and default-EWT fields from customer/supplier/item setup; SI customer CWT now uses `is_subject_to_cwt`. | Held-out-safe fresh `supabase db reset --local` passed with drafts `20260710000004`/`00005` moved aside/restored. Focused test 045 (WHT-MASTER-CONSOLIDATION-001) passed 12/12. Neighbor tests 013/033/038/043/045 passed 64/64. Full trusted pgTAP passed **794/794 across 44 files** with held-out test `027` excluded. `npm run gen:types` passed; schema summary regenerated (254 functions / 20 views / 149 tables / 282 triggers); `npm run lint`, `npm run build`, `bash -n scripts/gen_schema_summary.sh`, `scripts/check_docs_consistency.sh`, and `git diff --check` passed. | No open risk remains for duplicate withholding masters. Hosted Supabase push for `20260714000003` completed in session 93. | Continue PXL-AUD-046/047/049 and remaining In-Progress coverage/report items (AUD-008/009/010, DA-013/018/020, AUD-045/050). |
| 2026-07-14 | 90 | PXL-AUD-040 | Retested Passed | Form 2307 monthly breakdown in `20260714000002_form2307_monthly_breakdown.sql`. Added month-1/month-2/month-3 base and withheld columns to `form_2307_issuance_lines`; updated generation and supersede RPCs to bucket source EWT detail by quarter month; updated sent/acknowledged snapshot payloads; and updated `Form2307IssuedPage` to render each certificate's monthly ATC/nature breakdown plus quarter totals. | Held-out-safe fresh `supabase db reset --local` passed with drafts `20260710000004`/`00005` moved aside/restored. Focused test 044 (F2307-MONTHLY-001) passed 8/8. Neighbor tests 007/010/016/022/044 passed 75/75. Full trusted pgTAP passed **782/782 across 43 files** with held-out test `027` excluded. `npm run gen:types` passed; schema summary regenerated (254 functions / 20 views / 152 tables / 287 triggers); `npm run lint`, `npm run build`, `scripts/check_docs_consistency.sh`, `git diff --check`, and `git diff --cached --check` passed. Hosted push later applied `20260714000001` and `20260714000002`; migration list and held-out-safe dry run verified the remote is up to date. | No open risk remains for this finding. CLI emitted the known pg-delta catalog-cache warning after apply, but migration history verified clean through `20260714000002`. | Continue PXL-AUD-044/046/047/049 and remaining In-Progress coverage/report items (AUD-008/009/010, DA-013/018/020, AUD-045/050). |
| 2026-07-14 | 89 | PXL-AUD-043 | Retested Passed | Advance/down-payment withholding in `20260714000001_advance_payment_withholding.sql`. Added governed `customer_advances_account_id` and `supplier_down_payments_account_id`, explicit `receipt_lines.line_type` / `payment_voucher_lines.line_type`, invoice-less OR customer-advance lines with CWT, bill-less PV supplier-down-payment lines with EWT, account-aware posting splits, WHT tax-detail rows, reconciliation-aware GL impact payload labels, setup/readiness mappings, GL posting selectors, and OR/PV UI add-line + preview support. This completes the PXL-AUD-043 scope started by cash-purchase EWT in session 88. | Held-out-safe fresh `supabase db reset --local` passed with drafts `20260710000004`/`00005` moved aside/restored checksum-clean. Focused test 043 (ADVANCE-PAYMENT-WHT-001) passed 13/13. Neighbor tests 034/036/037/038/042/043 passed 76/76. Full trusted pgTAP passed **774/774 across 42 files** with held-out test `027` excluded. `npm run gen:types` passed; schema summary regenerated (254 functions / 20 views / 152 tables / 287 triggers); `npm run lint`, `npm run build`, `scripts/check_docs_consistency.sh`, `git diff --check`, and `git diff --cached --check` passed. Hosted Supabase sync completed in session 90. | Later application of posted advances/down-payments to subsequently issued SI/VB remains a separate AR/AP settlement enhancement; withholding recording/posting/reconciliation is closed for this finding. | PXL-AUD-040 completed in session 90; continue AUD-044/046/047/049 and remaining In-Progress coverage/report items. |
| 2026-07-13 | 88 | PXL-AUD-043 | In Progress | Cash-purchase EWT slice in `20260713000015_cash_purchase_ewt.sql`. Added `cash_purchases.total_ewt_amount` plus line `ewt_atc_code_id`/explicit base/amount/income nature/variance fields, profile and ATC validation triggers, post-readiness validation, and an EWT-aware replacement for `fn_save_cash_purchase` and `fn_post_cash_purchase_source_locked_impl`. Cash purchases now store cash paid net of EWT, post DR expense/input VAT and CR EWT payable/net cash, and write source-line `ewt_payable` tax-detail rows. `CashPurchasesPage` now loads supplier EWT defaults and active EWT ATCs, captures supplier TIN, shows ATC/base/EWT/cash-paid columns, and previews EWT payable. | Held-out-safe fresh `supabase db reset --local` passed with drafts `20260710000004`/`00005` moved aside/restored checksum-clean. Focused test 042 (CASH-PURCHASE-EWT-001) passed 10/10. Neighbor tests 015/024/028/038 passed 88/88. Full trusted pgTAP passed **761/761 across 41 files** with held-out test `027` aside/restored. `npm run gen:types` passed; schema summary regenerated (248 functions / 20 views / 152 tables / 252 triggers); `npm run lint`, `npm run build`, `scripts/check_docs_consistency.sh`, and diff checks passed. Hosted Supabase is synced through `20260713000015` (CLI emitted the known pg-delta cache warning; migration list confirmed local=remote). | At session 88 close, customer advances and supplier down-payments still required the follow-up slice; session 89 later closed that scope. | AUD-043 completed in session 89 and AUD-040 completed in session 90; continue AUD-044/046/047/049 and remaining coverage/report items. |
| 2026-07-13 | 87 | PXL-DA-016 | Retested Passed | Semantic transaction lifecycle event stream in `20260713000014_transaction_events.sql`. Added governed `transaction_events` with company RLS, application SELECT-only grants, and service/internal write posture. Replaced/extended `fn_record_posting_event` so posting/reversal keeps legacy `sys_audit_logs` evidence while also creating a semantic event row. Added internal helpers for actor role and status-to-event mapping plus triggers for `journal_entries`, active registered posting source tables, `approval_instances`, and `report_snapshots`, covering create/status/source-link, approved/rejected, posted/reversed/voided/cancelled, exported/filed, actor, reason, source document number, and JE links. | Held-out-safe fresh `supabase db reset --local` passed with drafts `20260710000004`/`00005` and test `027` moved aside/restored byte-for-byte. Focused test 041 (TRANSACTION-EVENTS-001) passed 14/14. Focused posting/registry tests 025+030 passed 51/51. Full trusted pgTAP suite passed **751/751 across 40 files** with held-out test `027` aside. `npm run gen:types` and trusted-baseline schema summary regenerated (244 functions / 20 views / 152 tables / 250 triggers). `npm run lint`, `npm run build`, `scripts/check_docs_consistency.sh`, and `git diff --check` passed. Hosted Supabase is synced through `20260713000014` (held-out draft migrations excluded during push; restored checksum OK). | Per-document Activity Timeline UI is not broadly wired to `transaction_events`; that remains under PXL-AUD-050. Future source tables still need lifecycle classification at creation time. | Continue with PXL-AUD-043 (EWT on cash purchases / advances), then PXL-AUD-040, then AUD-044/046/047/049 and remaining In-Progress coverage/report items. |
| 2026-07-13 | 86 | PXL-AUD-013, PXL-DA-014 | Retested Passed | Financial-statement/close readiness in `20260713000013_je_classification_and_close.sql`. Added `journal_entries.entry_class` (regular/adjusting/closing/opening), exposed it through `vw_general_ledger`, extended `fn_post_manual_je` with an optional entry class, added `fn_close_fiscal_year` to post one balanced closing JE directly to retained earnings and lock the year, and replaced the Trial Balance checkbox with Unadjusted/Adjusted/Post-Closing modes. | Held-out-safe reset passed; full pgTAP **737/737 across 39 trusted files** (test 040, FINANCIAL-CLOSE-001, 17 assertions); build, zero-warning lint, generated types, schema summary, and docs gate were green. Committed as `38ac28a`; hosted Supabase push later synced through `20260713000013`. | FS-line mapping and IS/BS/retained-earnings statement pages remain backlog enhancements; a Period-Close UI over `fn_close_fiscal_year` is optional follow-up. | Continue DA-016, then AUD-043/AUD-040. |
| 2026-07-13 | 85 | PXL-DA-010 | Retested Passed | VAT/PT tax-code effective-date + version governance in `20260713000012_tax_code_effective_date_governance.sql`, extending the ATC release pattern to `tax_codes` (VAT/PT rate holder) and `vat_codes` (classification/mapping holder). Added effective-from/to + deprecation/supersession columns; replaced the global `tax_codes.code` unique key with version-aware `(code, effective_from)` (plus the same on `vat_codes.vat_code`); a `fn_enforce_tax_code_version_rules` overlap + successor-integrity guard; `fn_tax_code_used`/`fn_vat_code_used` usage predicates (tax ledger + all six document-line tables); `fn_guard_tax_code_history`/`fn_guard_vat_code_history` freezing rate/identity/effective-start once used and blocking deletes of used codes; and `fn_tax_code_version_asof`/`fn_tax_code_is_current` resolvers. No heavy VAT/PT posting RPC changed — historical stability comes from used-version immutability + deprecate-and-succeed, mirroring the ATC approach. | Held-out-safe fresh `supabase db reset` (drafts `20260710000004`/`00005` + test `027` moved aside then restored byte-for-byte, checksums verified) replaying through `20260713000012`; full pgTAP **720/720 across 38 files** (new test 039, TAX-CODE-VERSION-001, 17 assertions); `npm run build` passed; `npm run lint` zero warnings; `npm run gen:types` regenerated; schema summary regenerated (236 functions / 20 views / 151 tables / 242 triggers); docs-consistency gate green (72 findings, 39 test files; pgTAP ran 38 with held-out `027` aside). | Hosted push completed later in the same work lane; linked Supabase is now synced through `20260713000014` with the held-out drafts kept off hosted. Optional admin successor-management UI on `TaxSetupPage` remains backlog (DEC-012/DEC-017 UI lane). No Criticals remain open. | Continue the accounting-core lane: AUD-043/AUD-040 withholding correctness, then AUD-044/046/047 and remaining coverage/report items. |
| 2026-07-13 | 84 | PXL-AUD-042 | Retested Passed | Withholding profile gates in `20260713000011_withholding_profile_gates.sql`. Added explicit-profile enforcement helpers and triggers so active profiles with `ewt_registered = false` block AP-side EWT payable on VB/PV/CV save/direct-write/post paths, 1601EQ/EWT returns, and QAP export snapshots. TWA auto-EWT now requires `ewt_registered`, `is_twa`, and `twa_auto_ewt_enabled`, then defaults supplier-subject source-basis VB lines to WC158 1% goods or WC160 2% services based on item type. WC160 seed repair aligns the active TWA-service ATC with the BIR 2% service code, and ATC history protection now includes VB source-EWT lines. | Held-out-safe fresh reset with draft migrations `20260710000004`/`00005` temporarily excluded and restored afterward. Focused `supabase/tests/038_withholding_profile_gates_test.sql` passed 11/11. Trusted pgTAP set excluding held-out `027` passed **703/703 across 37 files**. `npm run gen:types` and `scripts/gen_schema_summary.sh` passed. | Companies without a compliance profile remain legacy-compatible until setup makes the profile explicit; cash purchases and advances still need EWT support under PXL-AUD-043. Hosted push pending. | Continue PXL-AUD-043 (cash purchases/advances withholding), then PXL-AUD-040. |
| 2026-07-13 | 82 | PXL-DA-019 | Retested Passed | Closed the books reconciliation/audit-package slice in `20260713000009_books_reconciliation_audit_package.sql`. `fn_books_export_reconciliation` now checks every source book export against posted source rows, linked journal-entry coverage, and linked balanced GL evidence; general journal exports reconcile frozen lines to `vw_general_ledger`. `fn_snapshot_books_export` blocks unreconciled books and stores reconciliation in the snapshot payload. Added `fn_snapshot_cas_audit_package` to gather books reconciliation, export hash evidence, DAT artifacts, number issuances, void events, and audit rows into a server-attested `CAS_AUDIT_PACKAGE` snapshot/log row; `AuditSupportPackagePage` now invokes that RPC for a selected date range. | Held-out-safe fresh replay through `20260713000009` on a filtered temp baseline (excluded `20260710000004`/`00005` drafts). Focused pgTAP passed: CAS-EXPORT-SNAP-001 **29/29** (`supabase/tests/017_cas_export_snapshots_test.sql`) and BOOKS-EXPORT-SNAP-001 **22/22** (`supabase/tests/018_books_export_snapshots_test.sql`). `npm run gen:types` and `scripts/gen_schema_summary.sh` passed. | DA-019 closed locally. Hosted push pending; local held-out drafts still require filtered replay for trusted verification. | Continue high-priority accounting/tax core items: AUD-037, AUD-042/043, AUD-040, AUD-044..047/049, AUD-016, DA-014, and DA-016. |
| 2026-07-13 | 81 | PXL-DA-019 | In Progress | DAT layout slice in `20260713000008_cas_dat_layout.sql`. CAS exports now render CRLF `PXL-CAS-DAT-1.0` `.dat` bytes server-side instead of CSV-shaped text, normalize CAS filenames to `.dat`, store immutable `cas_export_artifacts`, mirror `artifact_id`/`file_hash`/`layout_version` on `cas_export_log`, and expose idempotent `fn_render_cas_dat`. SLSP now freezes both sales and purchases detail records; RELIEF now freezes both sales and purchases rows with taxpayer/counterparty/document/VAT-classification fields. `CASDATFileGenerationPage` downloads plain-text `.dat` files through the generic text download helper; Books exports continue using CSV. | Held-out-safe fresh replay through `20260713000008` on a filtered temp baseline (excluded `20260710000004`/`00005` drafts). Focused pgTAP passed: CAS-EXPORT-SNAP-001 **29/29** (`supabase/tests/017_cas_export_snapshots_test.sql`) and BOOKS-EXPORT-SNAP-001 **16/16** (`supabase/tests/018_books_export_snapshots_test.sql`). `npm run gen:types`, `scripts/gen_schema_summary.sh`, `npm run lint`, `npm run build`, `scripts/check_docs_consistency.sh`, and `git diff --check` passed. | DA-019 remains Critical/In Progress: explicit books reconciliation/audit-package checks remain. Hosted push pending; local held-out drafts still require filtered replay for trusted verification. | Continue DA-019 with books reconciliation/audit-package checks. |
| 2026-07-13 | 80 | PXL-DA-019 | In Progress | Exported-byte evidence slice in `20260713000007_cas_export_file_hashes.sql`. `fn_snapshot_cas_export_unchecked` and `fn_snapshot_books_export` now render the exact CSV text server-side, store it as `source_payload.export_file_text`, store `source_payload.export_file_sha256` + byte size, mirror the hash/byte size on `cas_export_log`, and return the same `export_text` to the browser. `CASDATFileGenerationPage` and all seven Books pages now download that frozen server text through `src/lib/exportDownload.ts` instead of rebuilding CSV bytes client-side. Existing frozen `export_rows`, reconciliation payloads, source hashes, versioning, and direct-log-write denial remain in place. | Held-out-safe fresh replay through `20260713000007` on a filtered temp baseline (excluded `20260710000004`/`00005` drafts). Focused pgTAP passed: CAS-EXPORT-SNAP-001 **18/18** (`supabase/tests/017_cas_export_snapshots_test.sql`) and BOOKS-EXPORT-SNAP-001 **16/16** (`supabase/tests/018_books_export_snapshots_test.sql`), including returned export text = frozen snapshot text, SHA-256 over exact UTF-8 bytes, and `cas_export_log` hash/byte-size mirror. `npm run gen:types`, `scripts/gen_schema_summary.sh`, `npm run lint`, `npm run build`, and `git diff --check` passed. | DA-019 remains Critical/In Progress: true BIR DAT record layout and explicit books reconciliation/audit package checks remain. Hosted push pending; local held-out drafts still require filtered replay for trusted verification. | Continue DA-019 with true BIR DAT layout, then books reconciliation/audit-package checks. |
| 2026-07-13 | 78 | PXL-AUD-041 (and PXL-AUD-034 residue) | Retested Passed | Controlled EWT remittance / CWT application flow in `20260713000005_withholding_remittance_flow.sql`. New governed `withholding_remittances` header (controlled writes only — RLS SELECT for members, all writes through the SECURITY DEFINER RPCs; a posted row must carry its JE via CHECK; PXL-DA-011 header immutability guard), registered as posting source type `WHTREM` and added to `ux_journal_entries_live_source`. `fn_save/post/void_withholding_remittance` drive the draft lifecycle through the governed posting engine: EWT remittance (0619-E) posts DR EWT Payable / CR settlement cash-bank; CWT application posts DR settlement income-tax-due / CR CWT Receivable; neither writes `tax_detail_entries`. `fn_wht_gl_reconciliation` now excludes `WHTREM` movements from the GL side, so a mid-quarter remittance stops blocking `fn_snapshot_wht_export` (QAP/SAWT) and `fn_require_ewt_return_reconciled` (1601EQ), while uncontrolled `MANUAL` remittance JEs still surface as variance. `fn_compute_ewt_remitted_prior` derives `remitted_prior` from posted EWT remittances covering months 1-2 of the quarter, and the 1601EQ gate validates the return's `remitted_prior` against it (closing the AUD-034 free-entry residue). DEC-019 records the design. | Held-out-safe fresh `supabase db reset` replaying through `20260713000005` (three broken drafts moved aside then restored byte-for-byte, checksums verified); full pgTAP **648/648 across 35 files** (new test 036, WHT-REMITTANCE-001, 18 assertions); tests 023 (1601EQ gate — uncontrolled MANUAL JE still blocks) and 016 (WHT export snapshots) still green; `npm run gen:types` regenerated; `npm run build` passed; `npm run lint` zero warnings; schema summary regenerated (211 functions / 20 views / 151 tables / 267 triggers); docs-consistency gate green (72 findings, 35 tests). | Hosted push PENDING (no `SUPABASE_ACCESS_TOKEN`) — `20260713000001..000005` remain local-only. Dedicated remittance UI page deferred to the paused UI lane (DEC-017). Two Criticals remain (DA-009, DA-019). | Advance DA-009 (SAWT/QAP multi-ATC reconciliation, now that remittances no longer block exports) or continue the EWT lane: AUD-037 withholding basis policy (needs a DEC), then AUD-042/043. |
| 2026-07-13 | 77 | PXL-AUD-039 | Retested Passed | CM/VC-aware over-apply guards in `20260713000004_cm_vc_aware_overapply_guards.sql`. `fn_save_receipt` now nets applied credit memos from the invoice outstanding and `fn_save_payment_voucher` nets non-reversed vendor-credit applications (on open/applied vendor credits) from the bill outstanding, using the same balance formula as `fn_ar_aging_asof`/`fn_ap_aging_asof` (scalar subqueries so the two credit sources do not fan out). Prevents collections/payments that drive AR/AP negative and inflate the CWT/EWT base. Built on the `20260713000003` bodies (document-date threading + line-derived header totals preserved). | Held-out-safe fresh `supabase db reset` replaying through `20260713000004` (three broken drafts moved aside then restored byte-for-byte, checksums verified); full pgTAP **630/630 across 34 files** (new test 035, CM-VC-OVERAPPLY-001, 6 assertions); test 004 (vendor-credit controls) still green; `npm run build` passed; `npm run lint` zero warnings; types unchanged; schema summary regenerated; docs-consistency gate green (72 findings, 34 tests). | Hosted push PENDING (no `SUPABASE_ACCESS_TOKEN`) — `20260713000001..000004` remain local-only. Two Criticals remain (DA-009, DA-019). | Advance DA-009 (SAWT/QAP multi-ATC reconciliation) or the EWT lane: AUD-041 remittance/application flow, then AUD-037 basis policy. |
| 2026-07-13 | 77 | PXL-AUD-038, PXL-AUD-048 | Both Retested Passed | Settlement-total line authority in `20260713000003_settlement_total_line_authority.sql`. AUD-038: `fn_save_payment_voucher` and `fn_save_receipt` now recompute the header `total_amount`/`total_ewt`/`total_cwt` from the persisted lines (client header figures ignored), so GL cash/AP/AR mirror the subledger and tax ledger by construction; `fn_validate_payment_voucher_ewt_ready`/`fn_validate_receipt_cwt_ready` also reject any stored header cash total that diverges from `SUM(line payment_amount)` before a JE is written. AUD-048 (folded in): because header withholding totals now equal the line sums exactly, the permitted 0.02 header/line tolerance can no longer leave a residual GL-to-ledger variance that blocks SAWT/QAP/DAT exports. Cash sales are unaffected (own JE, separate path). Built on the session-77 `20260713000002` bodies (document-date threading preserved). | Held-out-safe fresh `supabase db reset` replaying through `20260713000003` (three broken drafts moved aside then restored byte-for-byte, checksums verified); full pgTAP **624/624 across 33 files** (new test 034, SETTLEMENT-TOTAL-AUTHORITY-001, 8 assertions); `npm run build` passed; `npm run lint` zero warnings; types unchanged; schema summary regenerated; docs-consistency gate green (72 findings, 33 tests). | Hosted push PENDING (no `SUPABASE_ACCESS_TOKEN`) — `20260713000001..000003` remain local-only. Two Criticals remain (DA-009, DA-019). PXL-AUD-039 (CM/VC-aware over-apply guards) is the natural next fix in the same two save-guards. | Fix AUD-039 (reuse the aging formula in the receipt/PV over-apply guards), then advance DA-009 or AUD-041. |
| 2026-07-13 | 77 | PXL-AUD-035, PXL-AUD-036 | Both Retested Passed | Safe trusted ATC document-date validation + rate versioning in `20260713000002_atc_document_date_versioning.sql`, built on the deployed governance (`20260701000018`) and replacing the held-out draft `20260710000004` (which stays excluded). AUD-035: `fn_validate_payment_voucher_line_ewt` and `fn_validate_receipt_line_cwt` collapse each to one signature with a trailing `p_document_date DATE` and evaluate the ATC effective window as of the document date; every caller threads `voucher_date`/`receipt_date` (PV save/trigger/ready, OR save/trigger/ready, cash sale via the receipt-line trigger, CV header trigger + post). AUD-036: global `UNIQUE (code)` replaced by `uq_atc_code_version (code, tax_category, effective_from)`; `fn_enforce_atc_version_rules` rejects overlapping active windows and validates successor linkage; `fn_atc_version_asof` resolves the version in force on a date; `fn_guard_atc_code_history` also freezes `effective_from` once used. Overload collapse avoids ambiguity — un-updated 4-/6-arg call sites bind to the single new function and fall back to CURRENT_DATE. | Held-out drafts `20260710000004`/`00005` + test `027` moved aside, fresh `supabase db reset` replaying through `20260713000002`, then restored byte-for-byte (checksums verified). Full pgTAP **616/616 across 32 files** (new test 033, ATC-DOCDATE-VERSION-001, 15 assertions); prior expired-ATC assertions in tests 006/022 still pass (message wording preserved; 6-arg calls keep CURRENT_DATE). `npm run gen:types` regenerated; `npm run build` passed; `npm run lint` zero warnings; schema summary regenerated; docs-consistency gate green (72 findings, 32 tests). | Hosted push PENDING (no `SUPABASE_ACCESS_TOKEN` this session); `20260713000001`/`20260713000002` and prior local-only migrations remain to be pushed with the held-out drafts moved aside. Two Criticals remain (DA-009, DA-019). ATC pickers in the frontend still resolve by row, not yet through `fn_atc_version_asof` by document date — a UI enhancement, not a posting defect. | Advance DA-009 (SAWT/QAP multi-ATC reconciliation now that ATC versioning exists) or continue the EWT lane: AUD-041 remittance/application flow, then AUD-037 basis policy. |
| 2026-07-12 | 64 | PXL-DA-019 | Open (first slice landed) | Built the first safe DA-019 CAS/BIR control slice fresh on the deployed branch-scoped allocator, not the held-out broken `20260710000005`. A required `supabase db lint` checkpoint first exposed three already-deployed runtime defects, repaired in `20260712000003_posting_runtime_repairs.sql` (test 031, 49 assertions): stock-transfer JE numbering now uses the source-warehouse branch while the JE stays branch-unattributed; physical-count value stays derived (no redundant `variance_cost` column) on the immutable line/inventory transaction; and an explicit optional `vendor_bills.rr_id` receiving-report FK — validated in `fn_save_vendor_bill` for received status + same company/supplier — replaces the nonexistent link that purchase-return completion read. Then `20260712000004_cas_numbering_void_evidence.sql` (test 032, 25 assertions) added immutable `cas_document_number_issuances`/`cas_document_void_events`, a `number_series` guard blocking backward counters and post-issuance identity/format/ATP changes, ATP-range exhaustion that never drifts the counter, allocation/void triggers across the numbered document tables, a hard `P0001` immutability trigger on void evidence (owner included), historical backfill, and the `vw_cas_atp_usage` governed read model. CAS pages (Void Register, ATP Usage, Dashboard, Audit Report) and `VendorBillsPage` (optional RR capture) were updated against the governed objects. | Fresh `supabase db reset` with unowned `20260710000004`/`00005` and test `027` held out then restored byte-for-byte; full pgTAP **601/601 across 31 owned files**; `supabase db lint --local` clean of the three prior actionable errors (remaining findings are the known temp-table/dynamic-record/STABLE-VOLATILE false positives; the four new CAS functions add none); `npm run gen:types` regenerated; `npm run build` passed; `npm run lint` zero warnings; schema summary regenerated (197 functions / 20 views / 149 tables / 229 triggers); docs-consistency gate green (72 findings, 31 tests). | DA-019 stays Critical/Open: true BIR DAT record layout, books reconciliation, and exported-byte hashing remain later slices. Committed `ffe7782` (pushed to `origin/main`); `20260712000003`/`20260712000004` pushed to linked project `bskjkogijpbhukjkagfj` with the held-out `20260710000004`/`00005` moved aside during the push (they stay off hosted); local = remote through `20260712000004`. | Continue DA-019 with DAT layout/books reconciliation/exported-byte hashing, or advance DA-009. |
| 2026-07-12 | 63 | PXL-AUD-051 | Retested Passed | Reconciled document-code drift and repaired branch-less numbering (`20260712000002_aud051_numbering_registry_alignment.sql`). Added governed registry rows `JE`/`FA`/`SDM`/`PRT`; fixed `DebitMemosPage` readiness code `DM`→`DM-S`; branch-scoped repair of all eight fixed-asset/inventory callers of the nonexistent two-argument `fn_next_document_number`, each now passing the branch already written on its journal_entries row (held-out arbitrary-branch overload deliberately unused). Demo seed realigned (each series points at its own governed type; FA series added). New test 030 (DOCUMENT-NUMBERING-REGISTRY-001, 11 assertions) adds a mechanical guard that every requested code is registry-backed, a structural guard that no two-argument caller remains, and a real `fn_register_fixed_asset` path drawing governed FA + JE numbers, posting a balanced acquisition JE, and linking it back as an FA source. | Fresh `supabase db reset --local` with unowned 00004/00005 and test 027 held out (00004/00005 restored byte-for-byte; 027 restored after the docs gate); full pgTAP **527/527 across 29 owned files**; `npm run build` passed; `npm run lint` zero warnings; schema summary regenerated (192 functions — the five inventory `_source_locked_impl` helpers are now captured; 19 views / 147 tables / 226 triggers); demo seed idempotent (28 series). | Registry retains near-duplicate purchasing codes (`SDM`/`DM-P`, `PRT`/`PR`) and unconsumed accounting codes (`JV`/`RJV`) — cosmetic cleanup, not a defect. Hosted synced through `20260712000002` (pushed 2026-07-12, local = remote). Two Criticals remain (DA-009/DA-019); AUD-051 closure unblocks DA-019. | Fix PXL-DA-019 (CAS numbering/void register/books/DAT/provenance) now that numbering is registry-consistent, or advance DA-009 dependencies (safe ATC date/version, PXL-AUD-041 remittance flow). |
| 2026-07-12 | 63 | PXL-DA-005, PXL-DA-007 | Both Retested Passed | Closure evidence only — no schema or application code changes. Test 026 (now 29 assertions) adds writer-boundary negatives under normal trigger execution: `SET CONSTRAINTS trg_journal_entry_source_integrity IMMEDIATE` proves the real deferred JE source constraint accepts a live same-company source and immediately rejects orphan and cross-company sources. New test 029 (POSTING-RACE-001, 14 assertions) opens two real database sessions with dblink over the harness TCP endpoint, races `fn_post_sales_invoice` on one committed approved SI, observes the second session blocking on the governed `FOR UPDATE` source lock in `pg_stat_activity`, and proves the loser resumes as a governed no-op: exactly one original JE/tax set survives, sequential re-post changes nothing, duplicate JE/tax rows stay structurally impossible, and the committed race fixture is pre-cleaned/removed so the test is rerunnable. | Fresh `supabase db reset --local` with unowned 00004/00005 and test 027 held out (restored byte-for-byte afterward, checksums verified); full pgTAP **516/516 across 28 owned files**; test 029 verified rerunnable back-to-back. No frontend source touched, so build/lint state is unchanged from session 62. | Hosted push of `20260712000001` completed later the same day with a user-supplied token (dry-run listed only that migration; `supabase migration list --linked` shows local = remote); test 029 is local-harness-only by design (dblink + local postgres credentials), which is acceptable because pgTAP never runs against hosted. Two Criticals remain (DA-009/DA-019). | Fix PXL-AUD-051 completely (registry/code alignment, DM-S readiness, seed realignment, branch-scoped repair of all eight two-argument numbering callers — not the held-out arbitrary-branch overload) to unblock DA-019; DA-009 still depends on safe ATC date/version work and PXL-AUD-041. |
| 2026-07-12 | 62 | PXL-DA-002, PXL-DA-004, PXL-DA-005, PXL-DA-007, PXL-DA-008, PXL-AUD-014 | DA-002/004/008 and AUD-014 Retested Passed; DA-005/007 In Progress | Dedicated session-61 closure review moved DA-002 to passed on the financial/subledger/tax/2307/snapshot trace contract and DA-004 to passed on the common posting protocol; DA-005 stays In Progress pending direct normal-trigger orphan/cross-company assertions, and DA-007 pending a real two-session post race. The next unblocked Critical, DA-008, was closed with `20260712000001_vat_amount_rpc_authority.sql`: all persisted VAT source headers/lines are application-read/RPC-write only; mutation policies and INSERT/UPDATE/DELETE/TRUNCATE grants are removed; the automatically updatable SI/VB register views are also read-only and `security_invoker`, closing a review-discovered mutation and cross-company RLS bypass. Existing save RPCs remain the only application mutation path and compute net/VAT/totals from quantity/price/discount plus VAT masters. Test 014 was realigned because direct SI status changes now fail at the stronger RPC-only boundary. | Fresh reset replayed through `20260712000001` with unowned 00004/00005 and test 027 held out; full pgTAP **499/499 across 27 owned files**; VAT-AMOUNT-INTEGRITY-001 (`test 028`) passes 25 assertions across six VAT families, base/view DML denial, cross-company view isolation, and document→tax ledger→GL→review reconciliation; `npm run build` passed; `npm run lint` zero warnings; generated types and schema summary refreshed (187 functions, 19 views, 147 tables, 226 triggers). | Hosted push is **PENDING**: `supabase db push --linked --dry-run` could not authenticate because `SUPABASE_ACCESS_TOKEN` is absent. The three unowned ATC/CAS files remain excluded from verified runs and will be restored byte-for-byte after checks. Two Criticals remain (DA-009/DA-019); both depend on unresolved work, while DA-005/007 are implementation-complete but evidence-incomplete. | Next: close DA-005 with normal-trigger negative tests and DA-007 with a true two-session race; then fix PXL-AUD-051 completely (registry/code alignment plus branch-scoped repair of eight shipped two-argument numbering callers) to unblock DA-019. Do not use the held-out arbitrary-branch numbering overload. |
| 2026-07-01 | 1 | PXL-AUD-003 | Retested Passed | `CompanySetupPage` edit now fetches the full `companies` row with `select('*')`, hydrates all company form fields through a shared mapper, reuses the same mapper for view mode, and blocks save when required compliance fields are blank. `PXL_TRANSACTION_MATRIX.md` Company Creation row was updated. | `npm run build` passed. `npm run lint` passed with pre-existing warnings unrelated to this fix. Manual trace: `openEdit` no longer writes blank placeholders into required compliance fields before update. | Company onboarding/setup readiness remains incomplete; branch, fiscal period, COA, number series, tax setup, and GL posting config still need preflight controls. | Proceed to Session 2: PXL-AUD-002 and PXL-AUD-016 setup readiness/preflight checks. |
| 2026-07-01 | 2 | PXL-AUD-002, PXL-AUD-016 | In Progress | Added shared `useTransactionReadiness` and `SetupReadinessBanner`. SI, OR, VB, and PV pages now check selected company/branch, active branch, open fiscal period for transaction date, active number series for `SI`/`OR`/`VB`/`PV`, and required GL posting config before create/save/post. Transaction matrix rows for Company Creation, Number Series, SI, OR, VB, and PV were updated. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Manual trace confirms core pages disable create/save/post and show blockers when readiness requirements are missing. | Broader setup remains incomplete: Company Setup still does not create branch/period/COA/series/config, and non-core transactions still need readiness coverage. | Proceed to Session 3 continuation: broaden posting/setup permission hardening beyond the scoped SI/OR/VB/PV migration. |
| 2026-07-01 | 3 | PXL-DA-003, PXL-AUD-004 | In Progress | Added `20260701000006_permissions_hardening.sql` and `20260701000007_lifecycle_permissions_broaden.sql`. Core and additional setup/control writes now require `can_admin_company(company_id)`. Restricted lifecycle status inserts/transitions now require owner/admin across core AR/AP plus CM/DM, purchasing extensions, banking, inventory, depreciation, amortization, revenue recognition, and selected compliance filing tables. Matrix rows were updated with the lifecycle permission scope. | Static SQL review; `npm run build` passed; `npm run lint` passed with pre-existing warnings. Remote pushes of `20260701000006_permissions_hardening.sql` and `20260701000007_lifecycle_permissions_broaden.sql` succeeded. The second push emitted expected idempotent drop-trigger notices and PostgreSQL identifier-truncation notices for long compliance trigger names. | This remains a scoped trigger-based guard. Full `can_perform` RPC checks, business role matrix, operational master-data policy, and live owner/admin/member tests are still missing. | Proceed to Session 4 GL Impact if accepted, or add live owner/admin/member RLS tests before more accounting changes. |
| 2026-07-01 | 4 | PXL-DA-001, PXL-DA-002 | In Progress | Added reusable `GLImpactPanel` and wired it into SI, OR, VB, and PV pages. The panel resolves configured/default accounts, shows debit/credit preview rows before posting, highlights imbalance or missing account mappings, and switches to posted journal entry lines for linked source documents. Matrix rows for SI/OR/VB/PV were updated. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Static UI trace confirms SI/OR/VB/PV derive the expected AR/AP, cash, revenue/expense, VAT, CWT, and EWT rows. | Preview logic is frontend-derived and only covers core AR/AP. A server-side preview RPC, rule explanations, broader module coverage, and seeded preview-vs-posted JE tests remain missing. | Proceed to Session 5 accounting readiness before approval/post, or add seeded SI/OR/VB/PV preview-vs-posted JE tests first. |
| 2026-07-01 | 5 | PXL-AUD-009, PXL-AUD-010 | In Progress | Added `20260701000008_accounting_readiness_approval.sql`, with SI/VB accounting-readiness validator functions and status triggers that block approved/posted transitions unless active lines have postable accounts and correct active VAT-code direction. Replaced SI/VB approve RPCs to call the validators. SI/VB pages now show immediate approve/post errors for missing revenue/expense accounts or invalid VAT codes. Matrix SI/VB rows were updated. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000008_accounting_readiness_approval.sql` succeeded with expected first-run drop-trigger notices. | This is scoped to SI/VB. Seeded database tests are still missing, and company VAT-registration enforcement remains open. | Proceed to Session 6 posted immutability, or add seeded DB tests for Sessions 2-5 before more controls. |
| 2026-07-01 | 6 | PXL-AUD-005 | In Progress | Added `20260701000009_core_line_immutability.sql`, with SI, OR, VB, and PV line-table triggers that block insert/update/delete once the parent document is no longer `draft`. Matrix SI/OR/VB/PV edit-restriction notes were updated. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000009_core_line_immutability.sql` succeeded with expected first-run drop-trigger notices. | This protects core AR/AP lines only. Header business-field immutability and broader transaction line tables still need similar controls plus live direct-write tests. | Proceed to Session 7 AR/AP as-of reporting, or add direct Supabase mutation tests for SI/OR/VB/PV line immutability first. |
| 2026-07-01 | 7 | PXL-AUD-011, PXL-AUD-012 | In Progress | AR aging now filters source invoices, posted receipt applications, and applied credit memos by the report as-of date. AP aging now recomputes posted vendor bill balances from bill date and posted payment voucher applications dated on or before the as-of date instead of relying on the current-balance-only `vw_ap_aging`. Logged new out-of-scope vendor-credit aging gap as PXL-AUD-019. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Static trace confirms future OR/PV applications no longer reduce earlier AR/AP aging in the scoped UI paths. | No seeded Supabase as-of scenario was executed. AR/AP aging still needs server-side RPCs, GL/subledger reconciliation tests, and AP vendor credit application handling under PXL-AUD-019. | Proceed to PXL-AUD-019 vendor-credit AP aging gap or add seeded AR/AP as-of database tests before the next accounting-control change. |
| 2026-07-01 | 8 | PXL-AUD-019 | In Progress | AP aging now subtracts vendor credit applications for selected vendor bills when `applied_date <= asOfDate` and the linked vendor credit is `open` or `applied`. Logged new out-of-scope vendor-credit application-date UI gap as PXL-AUD-020. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Static trace confirms stored vendor credit applications are included in the scoped AP aging UI path. | No seeded Supabase VB -> VC -> AP aging scenario was executed. AP aging still needs server-side RPC/view implementation and PXL-AUD-020 must add user-controlled application dates. | Proceed to PXL-AUD-020 vendor credit application date control, or add seeded AP aging tests before changing the VC apply flow. |
| 2026-07-01 | 9 | PXL-AUD-020 | In Progress | Vendor credit apply modal now captures `Application Date`, validates it is not before the vendor credit or selected vendor bill date, checks that an unlocked fiscal period covers the date, and passes it to `fn_apply_vendor_credit` as `p_date`. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Static trace confirms the UI no longer relies on the RPC default `CURRENT_DATE` for credit applications. | No seeded Supabase VC application-date scenario was executed. Server-side `fn_apply_vendor_credit` still does not independently enforce open-period/date-boundary rules if called outside the UI. | Add seeded AP aging/supplier-ledger tests for backdated vendor credit applications, then consider server-side RPC validation for application-date rules. |
| 2026-07-01 | 10 | PXL-AUD-020 | In Progress | Added and pushed `20260701000010_vendor_credit_application_date_enforcement.sql`, replacing `fn_apply_vendor_credit` with server-side application-date validation, open fiscal-period validation, and date-boundary checks. The migration also blocks direct authenticated inserts into `vendor_credit_applications` so inserts route through the RPC. Logged delete/reversal control gap as PXL-AUD-021. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Migration push succeeded with `supabase db push --linked --yes`; the project-ref form is unsupported by this CLI. Static SQL review confirms RPC checks date, credit/bill status, supplier/company match, remaining balance, bill outstanding balance, and fiscal period before insert. | No seeded Supabase VC application-date scenario was executed. Direct delete/reversal controls remain open under PXL-AUD-021. | Add seeded AP aging/supplier-ledger tests for vendor credit applications, then address PXL-AUD-021 reversal controls. |
| 2026-07-02 | 11 | PXL-AUD-021 | In Progress | Added and pushed `20260701000011_vendor_credit_application_reversal.sql`, adding reversal metadata to `vendor_credit_applications`, replacing the old unique application constraint with a partial active-application unique index, replacing `fn_apply_vendor_credit` so bill outstanding calculations ignore reversed applications, adding `fn_reverse_vendor_credit_application` to restore vendor credit balance/status, and denying direct authenticated deletes. `APAgingPage` now excludes reversed vendor credit applications. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000011_vendor_credit_application_reversal.sql` succeeded with the expected first-run drop-policy notice. Static SQL/code trace confirms direct authenticated deletes are denied by policy, reversal requires owner/admin plus an open fiscal period, and AP aging filters `reversed_at IS NULL`. | No seeded Supabase VB -> VC -> apply -> reverse scenario was executed. Supplier ledger remains based on posted vendor credit documents, not application rows. | Add seeded reversal tests, then continue to VAT registration enforcement under PXL-AUD-006/PXL-AUD-014. |
| 2026-07-02 | 12 | PXL-AUD-006, PXL-AUD-014 | In Progress | Added `20260701000012_vat_registration_enforcement.sql`, with reusable company/VAT-code validators, SI/VB line triggers, SI/VB approved/posted status validation, and `vat_returns` registration gating. `SalesInvoicePage` and `VendorBillsPage` now filter VAT codes by company registration and direction, and 2550M/2550Q pages block create/generate/save for non-VAT/exempt companies. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000012_vat_registration_enforcement.sql` succeeded. Static SQL/code trace confirms non-VAT/exempt companies cannot use VAT-bearing SI/VB codes or save VAT returns. | No seeded VAT/non-VAT/exempt database test scenario was executed. Gating is scoped to SI/VB and VAT returns; other VAT-bearing modules and VAT ledger-to-GL reconciliation remain open. | Add seeded VAT registration tests, then continue with EWT/ATC/2307 basis under PXL-AUD-007/PXL-AUD-008/PXL-AUD-015. |
| 2026-07-02 | 13 | PXL-AUD-007 | In Progress | Added `20260701000013_pv_ewt_atc_validation.sql`, with PV line EWT validation against active EWT ATC rate, header-vs-line EWT total validation, and a replaced `fn_post_payment_voucher` that validates before creating JE and tax detail rows. `PaymentVouchersPage` now recalculates EWT from the selected ATC rate and blocks mismatched manual amounts before save/post. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000013_pv_ewt_atc_validation.sql` succeeded. Static SQL/code trace confirms PV lines with EWT require active EWT ATC and amount must match the rate on `payment_amount + ewt_amount` within tolerance. | No seeded PV/EWT/2307 scenario was executed. Supplier withholding defaults, explicit net-of-VAT EWT taxable base, variance reasons, and server-side 2307 generation remain open under PXL-AUD-008/PXL-AUD-015. | Add supplier withholding defaults and explicit EWT basis, then replace direct 2307 UI mutations with server-side generation/status RPCs. |
| 2026-07-02 | 14 | PXL-AUD-008 | In Progress | Added `20260701000014_supplier_atc_defaults.sql`, adding `suppliers.is_subject_to_ewt` and `suppliers.default_atc_code_id` plus validation that defaults are active EWT ATCs. `SuppliersPage` now captures the AP EWT default, and `PaymentVouchersPage` applies the supplier default ATC and calculated EWT when a bill is selected. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000014_supplier_atc_defaults.sql` succeeded. Static trace confirms supplier defaults flow into PV line ATC/EWT calculation. | No seeded supplier-default PV scenario was executed. Customer CWT defaults, explicit EWT taxable base, income nature, and variance reason controls remain open. | Add explicit EWT basis/income nature and replace direct 2307 UI mutations with server-side generation/status RPCs. |
| 2026-07-02 | 15 | PXL-AUD-015 | In Progress | Added `20260701000015_form2307_issued_generation_rpc.sql`, with `fn_generate_form_2307_issued` for server-side supplier/ATC grouping from `vw_ewt_summary_ap`, required supplier/TIN/ATC checks, sent/acknowledged locking, RPC-only RLS write policies, and audit triggers for 2307 headers/lines. `Form2307IssuedPage` now calls RPCs instead of directly upserting, deleting, inserting, or updating 2307 rows. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000015_form2307_issued_generation_rpc.sql` succeeded. Static trace confirms generation/status writes now route through RPCs and direct authenticated table writes are denied by policy. | No seeded PV/EWT/2307 generation/regeneration/status scenario has been executed yet. Version/supersede workflow remains open. | Add seeded PV/EWT/2307 tests or define 2307 version/supersede controls. |
| 2026-07-02 | 16 | PXL-AUD-007, PXL-DA-009 | In Progress | Added `20260701000016_pv_ewt_explicit_basis.sql`, adding PV line `ewt_tax_base`, `ewt_income_nature`, and controlled `ewt_variance_reason`; replacing PV EWT validation/save/posting RPCs to validate against explicit base; writing explicit income nature/base to `tax_detail_entries`; and updating `vw_ewt_summary_ap`. `PaymentVouchersPage` now captures EWT base, income nature, and variance reason and filters ATCs to active EWT codes. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000016_pv_ewt_explicit_basis.sql` succeeded. Static trace confirms PV EWT no longer posts tax ledger base from implicit cash-plus-withholding alone. | No seeded PV/EWT/2307 scenario has been executed. Existing AP EWT defaulting is scoped; customer CWT defaults, ATC versioning, and full supplier withholding profile remain open. | Add seeded full/partial payment PV/EWT/2307 tests, then continue customer CWT defaults or ATC versioning. |
| 2026-07-02 | 17 | PXL-AUD-008, PXL-DA-009 | In Progress | Added `20260701000017_customer_cwt_defaults.sql`, adding customer `is_subject_to_cwt` and `default_cwt_atc_code_id`, validating defaults against active withholding ATCs, validating receipt-line CWT against selected ATC rate, preserving CWT-only receipt applications, and posting receipt CWT rows into `tax_detail_entries` as `cwt_receivable`. `CustomersPage` now captures customer CWT defaults, and `ReceiptsPage` defaults CWT/ATC from customer profile when loading open invoices. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000017_customer_cwt_defaults.sql` succeeded. Static trace confirms customer CWT defaults flow into OR lines and posted receipts create CWT tax ledger detail. | No seeded OR/CWT/SAWT/2307-received scenario has been executed. ATC versioning, richer withholding profile rules, and 2307 received claim/reversal controls remain open. | Add seeded customer CWT receipt/SAWT/2307-received tests, then continue ATC versioning or 2307 received controls. |
| 2026-07-02 | 18 | PXL-DA-010 | In Progress | Added `20260701000018_atc_effective_date_governance.sql`, adding ATC effective dates, deprecation/supersession metadata, delete protection for used ATCs, and immutability for used ATC code/category/rate. Supplier/customer defaults and PV/OR withholding validations now require active, current, non-deprecated EWT ATCs. `TaxSetupPage` now displays ATC effective windows and deprecation status. | `npm run build` passed. `npm run lint` passed with pre-existing warnings only. Remote push of `20260701000018_atc_effective_date_governance.sql` succeeded. Static trace confirms used ATC code/category/rate edits and deletes are blocked by trigger. | VAT/tax-code versioning, release approval workflow, audit UI, and seeded historical-rate mutation tests remain open. | Extend governance beyond ATCs and add seeded historical transaction tests. |
| 2026-07-02 | 19 | PXL-AUD-001, PXL-AUD-022, PXL-AUD-023 | PXL-AUD-001 In Progress; PXL-AUD-022/023 Retested Passed | Stood up the local Supabase test harness (Docker + `supabase init/start`, non-essential services disabled in `supabase/config.toml`) and the first seeded pgTAP critical-flow test `supabase/tests/001_critical_flow_test.sql` (company -> branch -> fiscal periods -> COA -> GL config -> number series -> customer/supplier -> SI save/approve/post -> OR post with CWT -> VB save/approve/post -> PV post with EWT; 41 assertions on JE balance, account-level debits/credits, AR/AP clearing, tax detail rows, posted-line immutability, GL zero-net). Fresh-database replay of the migration chain initially failed: added guards in `20260630000021_gap_fill.sql` for the already-dropped `ref_atc_codes`. The harness then exposed two runtime-fatal defects fixed by new migrations: `20260702000001_number_series_document_code_alignment.sql` (PXL-AUD-022: number_series lacked the `document_code`/`current_sequence`/`padding`/`suffix` columns the numbering RPC reads; added columns, backfill, UI/RPC shape sync trigger, active-series unique index) and `20260702000002_si_post_tax_base_column_fix.sql` (PXL-AUD-023: `fn_post_sales_invoice` referenced non-existent `total_net_amount`; output VAT base corrected to `total_taxable_amount`). | `supabase db reset --local` replays all migrations cleanly; `supabase test db` passes 41/41; `npm run build` passed; `npm run lint` passed with pre-existing warnings only; both new migrations pushed to remote and verified in `supabase migration list --linked`. | Harness runs locally only (no CI/`npm test` wiring); remaining test book scenarios (as-of aging, vendor credit application/reversal), non-VAT gating, EWT partial payment, 2307 generation, and role-based RLS tests are not yet automated. Remote schema drift implied by PXL-AUD-022 has not been diffed against migrations. | Automate the documented test book scenarios next (AR/AP as-of aging, VC application/reversal), then wire `supabase test db` into CI. |
| 2026-07-02 | 20 | PXL-AUD-011, PXL-AUD-012, PXL-AUD-019, PXL-AUD-020, PXL-AUD-021, PXL-DA-013 | PXL-AUD-011/012/019/020/021 Retested Passed; PXL-DA-013 In Progress | Added `20260702000003_ar_ap_aging_asof_rpcs.sql` with `fn_ar_aging_asof` and `fn_ap_aging_asof` (server-side as-of aging: posted documents dated <= as-of, posted OR/PV applications by receipt/voucher date, applied CMs by `cm_date`, non-reversed VC applications by `applied_date` with credit status open/applied). Rewired `ARAgingPage` and `APAgingPage` to call the RPCs instead of recomputing client-side. Automated all five documented test book scenarios as seeded pgTAP tests: `002_ar_aging_asof_test.sql` (AR-AGING-ASOF-001 incl. GL AR reconciliation), `003_ap_aging_asof_test.sql` (AP-AGING-ASOF-001/002 incl. GL AP reconciliation), `004_vendor_credit_application_controls_test.sql` (VC-APPLICATION-DATE-001 date validation/locked-period rejection/direct-insert denial; VC-APPLICATION-REVERSAL-001 reversal restore/evidence/direct-delete denial). | `supabase test db` passes 81/81 assertions across 4 files; `npm run build` passed; `npm run lint` passed with pre-existing warnings only (none on the rewired pages); migration pushed to remote and verified in `supabase migration list --linked`. | Customer/supplier ledger views remain current-only (PXL-DA-013); legacy `vw_ap_aging` view still exists for other consumers; CI wiring for `supabase test db` remains open under PXL-AUD-001. | Continue PXL-AUD-001 scenario coverage (non-VAT gating, EWT partial payment, 2307 generation) or wire the harness into CI. |
| 2026-07-02 | 21 | PXL-AUD-006, PXL-AUD-007, PXL-AUD-014, PXL-AUD-015, PXL-DA-009 | PXL-AUD-007 Retested Passed; others In Progress with executed seeded evidence | Added three seeded pgTAP scenarios with no product changes required: `005_non_vat_registration_gating_test.sql` (non-VAT company blocked from VAT-bearing SI/VB codes and VAT returns; exempt documents post with zero VAT), `006_pv_ewt_partial_payment_test.sql` (two partial PVs withholding 2% on explicit 5,000 bases: per-voucher tax detail, cumulative EWT 200/base 10,000 in tax ledger and GL, over-payment rejection, rate-mismatch rejection without authorized variance reason, invalid-reason rejection, expired-ATC rejection), and `007_form2307_issued_generation_test.sql` (quarter-batch generation, regeneration refresh of a generated certificate, sent-status lock, direct-write denial). The controls shipped in sessions 12-18 all held under seeded execution. | `supabase test db` passes 117/117 assertions across 7 files; `npm run build` passed; `npm run lint` passed with pre-existing warnings only. No new migrations, so no remote push was needed. | Noted risk (tracked under PXL-AUD-010 supplier completeness): a PV with EWT can post without a supplier TIN snapshot, and 2307 generation then fails later with a clear error; upstream TIN-required-when-withholding validation would move the failure earlier. Version/supersede workflow for 2307 and VAT ledger/GL reconciliation remain open. | Wire `supabase test db` into CI (PXL-AUD-001), or continue with 2307 version/supersede or VAT report reconciliation. |
| 2026-07-02 | 22 | PXL-AUD-001 | Retested Passed | Wired the seeded pgTAP harness into CI: added the `test` script (`supabase test db`) to `package.json` and created `.github/workflows/ci.yml` with two jobs — `build-lint` (npm ci, `npm run lint`, `npm run build` on Node 24) and `db-tests` (supabase/setup-cli pinned to 2.108.0, `supabase db start`, `supabase test db`). Because each CI runner starts with no database, the `db-tests` job replays the entire migration chain from scratch on every run, which also delivers the fresh-replay migration gate recommended under PXL-AUD-022/PXL-DA-006. No transaction behavior changed, so `PXL_TRANSACTION_MATRIX.md` is not impacted; the test book execution instructions were updated to reference `npm test` and CI. | Executed locally 2026-07-02: `supabase db reset --local` replayed all migrations cleanly, then `npm test` passed 117/117 assertions across 7 files. `npm run lint` passed with pre-existing warnings only; `npm run build` passed. Sessions 19-22 work was then committed (dfeb542, 67269d9) and pushed to `main`; the first hosted GitHub Actions run (28582917878) completed green: `build-lint` success and `db-tests` success with `Files=7, Tests=117, Result: PASS`. | Role-based RLS scenarios remain open under PXL-AUD-004/PXL-DA-003. CI runs on GitHub-hosted runners against a migrations-only database, so remote-schema drift (noted under PXL-AUD-022) is still not diffed by CI. | Continue with 2307 version/supersede (PXL-AUD-015) or VAT ledger-to-GL reconciliation (PXL-AUD-014/PXL-DA-008). |
| 2026-07-02 | 23 | PXL-AUD-014, PXL-DA-008 | In Progress | Added `20260702000004_vat_ledger_gl_reconciliation.sql`: `fn_vat_gl_reconciliation(company, date_from, date_to)` compares output/input VAT in `tax_detail_entries` (by `document_date`, the accounting date aligned with `je_date`) against the configured GL VAT control accounts (posted JEs only, matching `vw_general_ledger` semantics), and `trg_vat_returns_status_reconciled` blocks VAT returns from `final`/`filed` while the period is unreconciled or the return's own output/input VAT figures diverge from the tax ledger beyond 0.01. Added shared `VATReconciliationPanel` showing tax ledger vs GL vs return figures on `VATReturn2550MPage` and `VATReturn2550QPage`. Matrix VAT Return row updated. Logged two new out-of-scope findings discovered while tracing: PXL-AUD-024 (GL/TB views double-apply JE reversals) and PXL-AUD-025 (`tax_detail_entries.posting_date` stores the system date, not the accounting date). | Seeded scenario VAT-RECON-001 executed passing 2026-07-02 (`supabase/tests/008_vat_ledger_gl_reconciliation_test.sql`): January SI/VB reconcile (ledger = GL, variance 0) and a matching return goes final; a final return with figures diverging from the tax ledger is rejected; a manual JE on the VAT control account without tax detail produces a -500.00 variance, drafts still save, and final/filed transitions are blocked. `supabase db reset --local` replays cleanly; `npm test` passes 126/126 across 8 files; `npm run build` passed; `npm run lint` passed with pre-existing warnings only. Migration pushed to remote. | VAT review/report pages still mix document-derived views with the tax ledger; filed/exported snapshot provenance remains open (PXL-DA-015). Legitimate VAT settlement/remittance JEs on the control account will surface as variance until a controlled VAT closing/remittance flow exists — by design, they require tax-detail support or a controlled process. | Address PXL-AUD-024 GL reversal double-count (new Critical), or continue with 2307 version/supersede (PXL-AUD-015). |
| 2026-07-02 | 24 | PXL-AUD-024 | Retested Passed | Added `20260702000005_gl_reversal_visibility.sql`. Verified every path that marks a JE `reversed` (SI/OR/VB void in hardening_v2/gap_fill, PV cancel, banking `fn_reverse_journal_entry`, `fn_reverse_je`) first posts a swapped-line counter-JE, then adopted the both-visible convention: `vw_general_ledger`, `vw_trial_balance`, and `fn_vat_gl_reconciliation` now include `je.status IN ('posted','reversed')` so reversed activity nets to zero with the full audit trail on the ledger. Matrix Manual JE row updated; test book scenario GL-REVERSAL-001 added. | Seeded scenario GL-REVERSAL-001 executed passing 2026-07-02 (`supabase/tests/009_gl_reversal_visibility_test.sql`): expense nets to zero in GL and TB after `fn_reverse_je` with both entries visible (was -1,000 under the old filter), whole-GL zero-net holds, and a same-period reversal restores VAT reconciliation to zero variance. `supabase db reset --local` replays cleanly; `npm test` 135/135 across 9 files (aging-to-GL and VAT scenarios unaffected); `npm run build`; `npm run lint` pre-existing warnings only. Migration pushed to remote; CI green. | `BooksDashboardPage` still sums `journal_entries` with `status = 'posted'` client-side and needs a page-level consumer audit; period-crossing reversals net to zero only across both periods (correct as-of behavior). | Continue with 2307 version/supersede (PXL-AUD-015), PXL-AUD-025 posting_date consumer audit, or VAT report standardization (PXL-AUD-014). |
| 2026-07-02 | 25 | PXL-AUD-025, PXL-AUD-024 (residue) | PXL-AUD-025 Retested Passed | Executed the `tax_detail_entries.posting_date` consumer audit: zero readers exist — all period filters already use `document_date` (via `vw_ewt_summary_ap.invoice_date`), `tax_period_id`, or the session-23/24 reconciliation function. Added `20260702000006_tax_detail_date_semantics.sql` with schema `COMMENT`s marking `posting_date` as the system entry date and `document_date` as the accounting date, so future consumers cannot pick the wrong column. Closed the PXL-AUD-024 residue: page-level JE consumer audit found only `BooksDashboardPage` summing `status = 'posted'` client-side; its month count/total now include `reversed` (tile relabeled "Journal Entries — This Month"), matching the books-visibility convention. Books journal pages query source documents and review/reversal workflow pages filter by status intentionally — unchanged. No transaction behavior changed, so `PXL_TRANSACTION_MATRIX.md` is not impacted; no new test book scenario needed (F2307-ISSUED-001 and VAT-RECON-001 already prove accounting-date period filtering with backdated documents). | `supabase migration up` applied; `npm test` 135/135 across 9 files; `npm run build` passed; `npm run lint` passed with pre-existing warnings only (39). Migration pushed to remote; CI green. | Documentation-level control only — a future consumer could still ignore the column comments; the durable fix (renaming to `entered_date`) was rejected as requiring replacement of every posting-function INSERT for no behavioral gain. | Continue with 2307 version/supersede (PXL-AUD-015) or VAT report standardization on the tax ledger (PXL-AUD-014). |
| 2026-07-02 | 26 | PXL-AUD-015 | Retested Passed | Added `20260702000007_form2307_version_supersede.sql`: certificate `version`, `supersedes_issuance_id`/`superseded_by_issuance_id` linkage, `superseded_at`, a `superseded` status, and `fn_supersede_form_2307_issued(issuance, reason)` — owner/admin-only, allowed only for sent/acknowledged certificates, generates a new version from current `vw_ewt_summary_ap` detail (same TIN/ATC completeness checks as generation), retires the old certificate as immutable evidence, and links both directions. The whole-history unique key was replaced with a partial unique index enforcing one active (non-superseded) certificate per company/supplier/quarter, and `fn_generate_form_2307_issued` was updated to treat superseded certificates as history (excluded from lock counts, conflict target, and upserts). `Form2307IssuedPage` shows certificate versions and offers Supersede with a reason on sent/acknowledged rows. Matrix Form 2307 Issued row and test book updated. | Seeded scenario F2307-SUPERSEDE-001 executed passing 2026-07-02 (`supabase/tests/010_form2307_supersede_test.sql`, 14 assertions): late PV after sent lock, supersede creates v2 with refreshed totals/lines and two-way links, old certificate and lines preserved, one active certificate per quarter, regeneration refreshes only the active version, generated/superseded certificates rejected for supersession, direct un-superseding denied (42501). `supabase db reset --local` replays cleanly; `npm test` 149/149 across 10 files; `npm run build` passed; `npm run lint` pre-existing warnings only. Migration pushed to remote; CI green. | QAP/1601EQ/EWT summary reports read live tax detail, not certificate versions, so a superseded chain is invisible to them by design; report-level provenance for filed returns continues under PXL-DA-015. | Continue with VAT report standardization on the tax ledger (PXL-AUD-014) or role-based RLS test scenarios (PXL-AUD-004/PXL-DA-003). |
| 2026-07-02 | 27 | PXL-AUD-004, PXL-DA-003, PXL-AUD-026 | PXL-AUD-026 Retested Passed; PXL-AUD-004/PXL-DA-003 In Progress | Added seeded scenario RLS-ROLES-001 (`supabase/tests/011_role_based_access_test.sql`, 16 assertions) — the first test to run as the `authenticated` role with per-user JWT claims, exercising RLS itself: cross-company read isolation, member/viewer reads, member/viewer denied setup writes (branches/number series 42501, period lock silently filtered), admin setup writes, member drafts via RPC, member denied posting by the lifecycle trigger, admin posts, outsider denied document creation. Its first query exposed PXL-AUD-026: the migration chain never granted table privileges, so `authenticated` had zero access on a migrations-only database and production would break on Supabase's 2026-10-30 legacy-default removal. Fixed by `20260702000008_authenticated_table_grants.sql` (grants + default privileges for `authenticated`/`service_role`; RLS confirmed enabled on all 143 public tables first; `anon` gets nothing). Rewrote the direct-write denial assertions in tests 004/007/010 to effect-based checks because `USING (false)` UPDATE/DELETE policies silently match no rows once grants exist. Test book scenario RLS-ROLES-001 added. No transaction behavior changed, so `PXL_TRANSACTION_MATRIX.md` is not impacted. | Verified landing session 2026-07-02: `supabase db reset --local` replays the full chain incl. migration 008 cleanly; `npm test` passes 165/165 across 11 files; `npm run build` passed; `npm run lint` passed with pre-existing warnings only. Remote push of migration 008 was recorded by the discovering session; not re-verified in the landing session (no Supabase access token in this workspace). | Approval remains role-unrestricted (PXL-DA-012). `can_perform` role/action enforcement and operational master-data role policy remain open (PXL-AUD-004/PXL-DA-003). Remote grant posture vs the legacy auto-expose defaults has not been diffed. | Continue VAT report standardization on the tax ledger (PXL-AUD-014/PXL-DA-008) or `can_perform` role/action RPC enforcement (PXL-DA-003). |
| 2026-07-02 | 28 | PXL-AUD-027 | Retested Passed | Started PXL-AUD-014 VAT report standardization and immediately found a Critical defect in the session-23 reconciliation control, logged as PXL-AUD-027: void/cancel/bounce paths netted the GL with counter-JEs but left `tax_detail_entries` un-netted (SI void, PV cancel, and OR bounce never touched tax rows; VB void mutated originals in place), so voided documents produced false VAT reconciliation variance that blocked returns, cancelled PVs kept feeding 2307 source data, and bounced ORs kept claiming CWT. Added `20260702000009_tax_ledger_void_reversal.sql`: shared non-user-callable `fn_reverse_tax_detail_entries` inserting negating counter-rows (linked via `reverses_tax_detail_id`, dated on the reversal date to match the counter-JE period); replaced `fn_void_sales_invoice`, `fn_void_vendor_bill` (flag-flip removed), `fn_cancel_payment_voucher`, and `fn_bounce_receipt` to call it; made `is_reversal` semantics uniform with schema comments; `vw_ewt_summary_ap` now excludes reversed originals; backfill repairs previously flipped VB rows and inserts missing counter-rows for already-voided documents dated at their reversal JE. Matrix SI/OR/VB/PV rows updated; test book scenario TAX-LEDGER-VOID-001 added. | Seeded scenario TAX-LEDGER-VOID-001 executed passing 2026-07-02 (`supabase/tests/012_tax_ledger_void_reversal_test.sql`, 17 assertions): baseline January reconciliation; SI void preserves the original row, adds a linked counter dated on the void date, keeps January reconciled, and reconciles the void month at -1,200 both sides; VB void leaves no flag-flipped originals; full-year VAT nets to zero both sides; bounced OR CWT nets to zero with a linked dated counter; cancelled PV vanishes from `vw_ewt_summary_ap` while the raw ledger preserves both rows netting to zero; every counter exactly negates its original; double-void rejected; final full-year reconciliation shows only live activity. `supabase db reset --local` replays cleanly; `npm test` 182/182 across 12 files; `npm run build` passed; `npm run lint` pre-existing warnings only (39). | VAT review/report page standardization on `tax_detail_entries` remains open (PXL-AUD-014/PXL-DA-008): the ledger writes no rows for zero-VAT (exempt/zero-rated) documents of VAT companies, stores no exempt/zero-rated bases, and cash sales/purchases have no tax-detail writers — prerequisites documented in this session's trace. Remote sync completed later in session 28 with a user-provided access token: `supabase db push --linked` applied BOTH pending migrations (20260702000008 and 20260702000009 — the session-27 "008 pushed" claim proved false) and `supabase migration list --linked` verified both present on the hosted project. | Continue PXL-AUD-014 (VAT ledger completeness: classification bases, zero-VAT rows, CS/CP writers, then ledger-backed review views) or `can_perform` enforcement (PXL-DA-003). |
| 2026-07-02 | 29 | PXL-DA-003, PXL-AUD-004, PXL-DA-012, PXL-DA-017 | Unblocked (statuses unchanged) | Governance session, no code changed. The user granted a standing autonomy delegation: business-policy decisions are now made by the agent with PH-compliance-conservative defaults and recorded as DEC entries (DEC-008). Recorded DEC-009 (role/action matrix on existing owner/admin/member/viewer roles: members capture drafts and operational master data; only owner/admin approve/post/void/reverse; enforced via `can_perform`), DEC-010 (approval SoD: approved instance required and approver ≠ creator wherever a workflow is configured), and DEC-011 (branch is a reporting dimension, not a security boundary; company remains the tenant boundary). Index Next Action cells updated for the four findings; AIOS bumped to 1.2.0. | `scripts/check_docs_consistency.sh` green (documentation-only change; no migration, so no pgTAP rerun). | The findings themselves remain unimplemented — the decisions only remove the "needs user decision" blocker. | Implement PXL-DA-003 `can_perform` per DEC-009 (Critical, now unblocked), then PXL-DA-012 approval gates per DEC-010; PXL-AUD-014 VAT ledger completeness remains the parallel P0 track. |
| 2026-07-03 | 30 | PXL-DA-003, PXL-AUD-004 | Retested Passed (both) | Implemented DEC-009: `20260702000010_can_perform_role_actions.sql` adds `fn_can_perform(company_id, action, document_type)` (owner/admin: all actions; member: create/edit/master_data; viewer/non-member: nothing) and reroutes the lifecycle gate `fn_require_admin_for_accounting_lifecycle` through it — all 37 gated tables pick the matrix up without trigger re-creation, `approved` joined the default restricted-status list (members can no longer approve SI/OR/VB/PV), and previously missing gates were added for petty cash voucher approval and `journal_entries` insert/status (the JE gate backstops every posting/reversal path, since BEFORE triggers fire inside SECURITY DEFINER RPC DML with `auth.uid()` as the caller). Customers/suppliers/items RLS write policies now use the `master_data` action: members create/edit, viewers read-only, delete stays owner/admin (PXL-AUD-004). RLS-ROLES-001 extended (member denied approval, admin approves, 17 assertions); new RBAC-CANPERFORM-001 (`supabase/tests/013_can_perform_test.sql`, 13 assertions) exercises the matrix per role/action plus the master-data policies as `authenticated`. Test book updated; schema summary regenerated (128 functions, 136 triggers). | Executed 2026-07-03: `supabase db reset --local` replays the full chain incl. migration 010 cleanly; `npm test` 196/196 across 13 files; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `scripts/check_docs_consistency.sh` green. Trigger coverage verified live: 37 tables carry the lifecycle gate incl. `journal_entries`. | Approver-not-creator SoD when a workflow is configured remains open under PXL-DA-012 (DEC-010). `fn_can_perform.p_document_type` is recorded but not yet consulted — reserved for future named-role refinement. Remote push pending until hosted sync step. | Implement PXL-DA-012 approval gates per DEC-010, or continue PXL-AUD-014 VAT ledger completeness (parallel P0 track). |
| 2026-07-03 | 31 | PXL-DA-012 | Retested Passed | Implemented DEC-010 approval SoD: `20260703000001_approval_sod_enforcement.sql` adds `fn_required_approval_workflow` (resolves the governing active workflow: module + optional document-type label with blank = all, `amount_exceeds` against the document total, other trigger conditions conservatively always-required) and `fn_enforce_approval_sod` BEFORE triggers on sales_invoices, vendor_bills, receipts, payment_vouchers, purchase_orders, and petty_cash_vouchers. When a workflow matches: self-approval is blocked (approver ≠ creator), the approval is recorded as an `approval_instances` row (workflow, first step, actor, timestamp; `workflow_step_id` made nullable for stepless workflows), and a two-step post requires a recorded qualifying approval (legacy `approved_by` ≠ creator evidence honored). Direct-post documents (OR/PV) treat posting as the approval act; direct status UPDATEs are equally caught. No workflow configured → DEC-009 role gate only; workflows are not force-enabled. `journal_entries` deliberately not gated (system JEs indistinguishable from manual; would brick all posting). Test book scenario APPROVAL-SOD-001 added; schema summary regenerated. | Executed 2026-07-03: `supabase db reset --local` replays the full chain incl. the new migration cleanly; new APPROVAL-SOD-001 (`supabase/tests/014_approval_sod_test.sql`, 14 assertions) passes; `npm test` 210/210 across 14 files; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `scripts/check_docs_consistency.sh` green. | Approval invalidation on post-approval edits remains under PXL-AUD-005/PXL-DA-011; manual-JE approval gating needs a manual/system discriminator; multi-step approval routing records the first step only. Hosted push of migrations 010 and 20260703000001 PENDING (no access token in this workspace). | Continue PXL-AUD-014 VAT ledger completeness (P0 track), or PXL-DA-017 dimension propagation per DEC-011. |
| 2026-07-03 | 32 | PXL-AUD-014, PXL-DA-008, PXL-AUD-028 | PXL-AUD-028 Retested Passed; PXL-AUD-014/PXL-DA-008 In Progress | VAT ledger completeness (`20260703000002_vat_ledger_completeness.sql`): SI/VB posting now writes one tax ledger row per (document, vat_code) — zero-rated/exempt bases preserved with zero tax — gated on `companies.tax_registration = 'vat'` and line-level VAT codes (non-VAT companies still write nothing, NON-VAT-GATING-001 preserved). Cash purchases gained input VAT writers; cash sales gained output VAT + CWT writers. Legacy lump rows (NULL `vat_code_id`) remain as evidence; consumers treat NULL as regular. Backfill adds only missing per-code rows for posted documents (regular codes skipped where a lump row exists; non-posted excluded so 20260702000009 void netting stays exact) plus CWT rows for posted cash-sale receipts. First-ever execution of `fn_save_cash_sale` exposed new Critical PXL-AUD-028: phantom `remarks`/`total_net_amount` columns, NULL payment mode into a NOT NULL column, posted-before-lines receipt insert, zero totals from UI payloads, AR over-application by the CWT, and no ATC — fixed by rebuilding the function on the fn_save_sales_invoice recompute pattern; `CashSalesPage` gained the CWT ATC selector. | Executed 2026-07-03: fresh `supabase db reset --local` replay; new VAT-LEDGER-COMPLETE-001 (`supabase/tests/015_vat_ledger_completeness_test.sql`, 13 assertions) passes; `npm test` 223/223 across 15 files; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `scripts/check_docs_consistency.sh` green. | The VAT review view rebase called out here was completed in session 33. CM/DM/VC per-classification rows follow the same pattern when needed. Cash sale + configured sales approval workflow = blocked by DEC-010 for the creator (by design; use SI + OR flow). Hosted push of migrations 010, 20260703000001, and 20260703000002 PENDING (no token). | Session 33 completed the view rebase; continue PXL-DA-015 filed/exported snapshots or PXL-DA-017 dimension propagation. |
| 2026-07-03 | 33 | PXL-AUD-014, PXL-DA-008 | In Progress | Added `20260703000003_vat_review_views_ledger_backed.sql`: `vw_output_vat_review` and `vw_input_vat_review` now aggregate from `tax_detail_entries` instead of document lines/header totals, join `vat_codes` for classification, treat NULL `vat_code_id` legacy rows as regular, and preserve the existing public view columns used by VAT review, 2550M/2550Q, SLSP/RELIEF, and dashboard pages. The 2550 pages already generate from these views, so their source totals now inherit the same ledger-backed basis. Schema summary regenerated. | Executed 2026-07-03: fresh `supabase db reset --local` replay through migration 003; VAT-LEDGER-COMPLETE-001 extended to 17 assertions proving output/input VAT review totals, cash-sale source detection, cash-purchase inclusion, and reversal rows visible in the reversal period; `npm test` 227/227 across 15 files; `npm run build` passed; `npm run lint` passed with pre-existing 39 warnings; `scripts/check_docs_consistency.sh` green. | PXL-AUD-014 remains In Progress for filed/exported snapshot provenance (PXL-DA-015) and CM/DM/VC per-classification VAT writers when those document classes are brought into the VAT report scope. Hosted push of migrations 010, 20260703000001, 20260703000002, and 20260703000003 PENDING (no token). | Continue AIQ-008 with the highest-priority Critical: PXL-DA-015 immutable filed/exported report snapshots, or PXL-DA-017 dimension propagation per DEC-011 if choosing the next unblocked accounting architecture task. |
| 2026-07-03 | 34 | PXL-DA-015, PXL-AUD-014 | In Progress | Added `20260703000004_report_snapshots_vat_returns.sql`: generic append-only `report_snapshots` table with RLS read/no-direct-write policies, SHA-256 `source_hash`, source payload, report payload, row count, and source linkage. VAT returns now create a `final` snapshot when marked final and a separate `filed` snapshot when filed. Snapshot payloads include the frozen VAT return, ledger-backed output/input VAT review rows, and `fn_vat_gl_reconciliation` rows. A guard blocks period/amount mutation once any snapshot exists while allowing filing metadata updates. | Executed 2026-07-03: fresh `supabase db reset --local` replay through migration 004; VAT-RECON-001 extended to 13 assertions proving final snapshot creation, 64-character source hash, amount immutability after snapshot, final-to-filed metadata update, and separate filed snapshot; `npm test` 231/231 across 15 files; `npm run build` passed; `npm run lint` passed with pre-existing 39 warnings; `scripts/check_docs_consistency.sh` green. | Session 35 extended the same snapshot/export model to Form 2307 issued. Hosted push of migrations 010, 20260703000001, 20260703000002, 20260703000003, and 20260703000004 was still pending at this point (no token). | Historical row; see session 35 for current PXL-DA-015 next action. |
| 2026-07-03 | 35 | PXL-DA-015 | In Progress | Added `20260703000005_report_snapshots_form2307.sql`: Form 2307 issued certificates now create versioned `report_snapshots` rows when marked `sent` or `acknowledged`. Snapshot payloads include the frozen certificate header, certificate ATC lines, and current `vw_ewt_summary_ap` source rows for the supplier/quarter; the source hash is SHA-256 over that canonical payload. A guard blocks post-snapshot mutation of certificate identity/amount fields while allowing status and supersede metadata, so controlled replacements still preserve old certificate evidence. | Executed 2026-07-03: fresh `supabase db reset --local` replay through migration 005; F2307-SUPERSEDE-001 extended to 18 assertions proving v1 sent snapshot creation, 64-character source hash, amount immutability, supersede still allowed after snapshot, generated/superseded negative checks, and a separate v2 sent snapshot; `npm test` 235/235 across 15 files; `npm run build` passed; `npm run lint` passed with pre-existing 39 warnings; `scripts/check_docs_consistency.sh` green. Schema summary regenerated (138 functions, 18 views, 146 tables, 141 triggers). | Session 36 extended the same snapshot/export model to SLSP/RELIEF. Hosted push of migrations 010, 20260703000001, 20260703000002, 20260703000003, 20260703000004, and 20260703000005 was still pending at this point (no token). | Historical row; see session 36 for current PXL-DA-015 next action. |
| 2026-07-03 | 36 | PXL-DA-015 | In Progress | Added `20260703000006_report_snapshots_vat_exports.sql`: `fn_snapshot_vat_export(company, report_type, year, month, export_part)` creates append-only `exported` snapshots for SLSP and RELIEF. Logical source IDs are deterministic per company/report/month/export part, repeated exports increment `snapshot_version`, and source payloads include SLSP summary rows or RELIEF detail rows plus `fn_vat_gl_reconciliation` evidence. `SLSPExportPage` and `RELIEFExportPage` now call the RPC before browser CSV generation; unreconciled periods raise and do not export. | Executed 2026-07-03: fresh `supabase db reset --local` replay through migration 006; VAT-RECON-001 extended to 21 assertions proving SLSP sales/purchases snapshots, RELIEF exported snapshot row count and version history, 64-character source hashes, and blocked RELIEF export for an unreconciled period; `npm test` 243/243 across 15 files. Schema summary regenerated (140 functions, 18 views, 146 tables, 141 triggers). | PXL-DA-015 remains In Progress: extend the same snapshot/export model to SAWT, QAP, books, and CAS exports; add reader UI/drilldown for `report_snapshots`. Hosted push of migrations 010, 20260703000001, 20260703000002, 20260703000003, 20260703000004, 20260703000005, and 20260703000006 PENDING (no token). | Continue AIQ-008 with SAWT or QAP snapshots, or implement the snapshot reader UI. Do not redo VAT return, Form 2307, or SLSP/RELIEF snapshots. |
| 2026-07-03 | 37 | PXL-DA-015 | In Progress | Added `20260703000007_report_snapshots_wht_exports.sql`: `vw_cwt_summary_ar` ledger-backed SAWT source view (security_invoker, active `cwt_receivable` rows only, gross income payments), `fn_wht_gl_reconciliation` comparing ewt_payable/cwt_receivable tax ledger sums to the configured EWT Payable/CWT Receivable GL control accounts (same shape/semantics as `fn_vat_gl_reconciliation`), and `fn_snapshot_wht_export` creating versioned append-only `exported` snapshots per company/report/quarter with SHA-256 source hashes, frozen detail + payer/payee summary + reconciliation payloads, and reconciliation blocking scoped to the report's own control account. `SAWTPage` switched from a browser-side `receipt_lines`-through-`sales_invoices` aggregation (which missed cash-sale CWT and understated income payments by the CWT) to `vw_cwt_summary_ar`; both `SAWTPage` and `QAPPage` snapshot before CSV download. | Executed 2026-07-03: fresh `supabase db reset --local` replay through migration 007; new WHT-EXPORT-SNAP-001 (`supabase/tests/016_wht_export_snapshots_test.sql`, 14 assertions) passes; `npm test` 257/257 across 16 files; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `scripts/check_docs_consistency.sh` green. | Books and CAS exports plus a snapshot reader/drilldown UI remain under PXL-DA-015. Same remittance caveat as the VAT slice: 0619-E/1601EQ remittance JEs on the control accounts surface as variance until a controlled remittance flow exists. Hosted push of migrations 20260702000010 through 20260703000007 PENDING (no token). | Continue PXL-DA-015 with books/CAS export snapshots or the snapshot reader UI, or PXL-DA-017 dimension propagation per DEC-011. |
| 2026-07-03 | 38 | PXL-DA-015 | In Progress | Added `20260703000008_report_snapshots_cas_exports.sql`: `fn_snapshot_cas_export` builds each CAS DAT extract payload server-side (SLSP from `vw_output_vat_review`, RELIEF from `vw_input_vat_review`, GL from `vw_general_ledger`, alphalist from `vw_ewt_summary_ap`), gates SLSP/RELIEF on `fn_vat_gl_reconciliation`, the alphalist on `fn_wht_gl_reconciliation` ewt_payable, and the GL extract on period debit=credit balance, creates versioned append-only `exported` snapshots (report types CAS_SLSP/CAS_RELIEF/CAS_GL/CAS_QAP) with SHA-256 hashes, writes server-attested `cas_export_log` rows linked via a new `snapshot_id` column, and returns the frozen rows. Direct client inserts into `cas_export_log` are closed (`WITH CHECK (false)`), so export-log row counts and periods are server evidence. `CASDATFileGenerationPage` now renders the file from the RPC's frozen rows instead of re-querying live views and self-logging. Matrix gained a CAS DAT File Generation row. | Executed 2026-07-03: fresh `supabase db reset --local` replay through migration 008; new CAS-EXPORT-SNAP-001 (`supabase/tests/017_cas_export_snapshots_test.sql`, 15 assertions) passes; `npm test` 272/272 across 17 files; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `scripts/check_docs_consistency.sh` green. | Books journal pages plus the snapshot reader/drilldown UI remain under PXL-DA-015. The extract is still CSV-shaped; the true BIR DAT record layout is PXL-DA-019 scope. Hosted push of migrations 20260702000010 through 20260703000008 PENDING (no token). | Continue PXL-DA-015 with books export snapshots or the snapshot reader UI, or PXL-DA-017 dimension propagation per DEC-011. |
| 2026-07-03 | 39 | PXL-DA-015 | In Progress | Added `20260703000009_report_snapshots_books_exports.sql`: `fn_snapshot_books_export` builds all seven BIR books payloads server-side over arbitrary date ranges (sales journal, purchase journal, cash receipts book with ORs gross of CWT plus cash-sale SIs, cash disbursements book with PVs net of EWT plus check vouchers and cash purchases, general journal gated on debit=credit balance, cash sales journal, cash purchases journal), freezes them in versioned `BOOKS_*` exported snapshots with SHA-256 hashes and integrity totals, writes server-attested `cas_export_log` rows (`csv_export`, range in remarks, `snapshot_id` link), and returns the frozen rows. All seven Books pages now render their CSVs from the frozen payload instead of browser-assembled queries. Matrix gained a BIR Books of Accounts Export row. With this slice every compliance export surface snapshots; only the snapshot reader/drilldown UI remains under PXL-DA-015. | Executed 2026-07-03: fresh `supabase db reset --local` replay through migration 009; new BOOKS-EXPORT-SNAP-001 (`supabase/tests/018_books_export_snapshots_test.sql`, 13 assertions) passes; `npm test` 285/285 across 18 files; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `scripts/check_docs_consistency.sh` green. | Books print views still render live page data (screen output, not export evidence). Hosted push of migrations 20260702000010 through 20260703000009 PENDING (no token). | Build the snapshot reader/drilldown UI to close out PXL-DA-015 implementation, or PXL-DA-017 dimension propagation per DEC-011. |
| 2026-07-04 | 40 | PXL-DA-015, PXL-AUD-029 | PXL-DA-015 Retested Passed; PXL-AUD-029 Open (new) | Completed PXL-DA-015 with the snapshot reader/drilldown UI: new `src/pages/ReportSnapshotsPage.tsx` (route `/report-snapshots`, Compliance → Audit & CAS nav) lists `report_snapshots` RLS-scoped with report-type/status/period filters, and drills into any snapshot showing the full SHA-256 source hash, source table/id, per-source version history with click-through between versions (e.g. final vs filed VAT return evidence), and generic rendering of the frozen report/source payloads — scalar values, row tables (capped at 200 rows with a count note), integrity totals, and reconciliation blocks — without any live-report recomputation. Read-only page; snapshot immutability continues to be enforced by RLS deny policies and guard triggers. Logged new Medium PXL-AUD-029 discovered during live verification: `AppShell` nav feature gating selects the non-existent `sys_feature_enablement.feature_key` column, 400s on every page load, and silently fails open. | Executed 2026-07-04: reader UI driven live in Chromium against the local Supabase stack with seeded evidence (2550Q final+filed snapshot pair sharing one source, one BOOKS_SALES_JOURNAL exported snapshot): list rendering, all filters, filter-aware empty state, hash display, version-history switching, and frozen row/reconciliation tables verified; `npm test` 285/285 across 18 files on a fresh `supabase db reset --local`; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `scripts/check_docs_consistency.sh` green. | PXL-DA-015 scope is closed; the true BIR DAT record layout (record-type/fixed-width formats) remains under PXL-DA-019. Hosted push of migrations `20260702000010` and `20260703000001`-`20260703000009` still PENDING (no `SUPABASE_ACCESS_TOKEN`). PXL-AUD-029 fix is a small `AppShell` query change when scheduled. | Continue AIQ-008 with PXL-DA-017 dimension propagation per DEC-011, or pick up PXL-AUD-029 as a small UI-integrity fix. |
| 2026-07-04 | 41 | PXL-DA-017 | Retested Passed | Implemented DEC-011 dimension propagation (`20260704000001_je_line_dimensions.sql`): `journal_entry_lines` gained `branch_id`/`department_id`/`cost_center_id` with partial indexes; `trg_je_dimensions_guard` validates the JE header branch belongs to the JE company; `trg_je_line_dimensions_guard` welds line company to the JE company (new integrity check), inherits the header branch when a line has none — covering all 34 JE writers centrally without per-writer changes — and validates every line dimension's company; existing lines backfilled from their headers (guarded against hypothetical cross-company legacy branches); `vw_general_ledger.branch_id` became line-accurate via `COALESCE(line, header)` keeping name/type/position so Branch P&L and every consumer upgraded transparently, with line `department_id`/`cost_center_id` appended; `fn_post_manual_je` accepts optional per-line dimensions in `p_lines`; `fn_reverse_je` copies line dimensions onto reversal lines. Schema summary regenerated. | Executed 2026-07-04: fresh `supabase db reset --local` replay through `20260704000001`; new JE-DIMS-001 (`supabase/tests/019_je_line_dimensions_test.sql`, 14 assertions) passes; `npm test` 299/299 across 19 files; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `scripts/check_docs_consistency.sh` green. | Document-line department/cost-center capture is a backlog enhancement; stock transfer JEs stay branch-unattributed by design; `fn_bt_reverse_je`/doc-void counter-JEs inherit header branch but do not copy line dept/cc (no capture path writes them on those JEs today). Hosted push of `20260704000001` pending until landing. | Next Criticals: PXL-DA-001 server-side GL preview, PXL-DA-011 status-aware immutability, or the small PXL-AUD-029 AppShell feature-gating fix. |
| 2026-07-04 | 42 | PXL-AUD-029, PXL-AUD-030 | Both Retested Passed (PXL-AUD-030 new) | Bounded frontend safety session (no framework rewrite). Introduced generated database types: `src/lib/database.types.ts` via new `npm run gen:types`, and typed the shared client `createClient<Database>` — table names, columns, RPC names/args, and embeds are now compile-time checked across all 206 pages. First typecheck surfaced 71 errors: the genuine defects became PXL-AUD-030 (8 pages querying non-existent columns — quotation/SO/DR/customer-return reference fetches, cash sale item/customer lists, asset acquisition and warehouse supplier lists, and the SLP register tab were runtime-dead), fixed with PostgREST aliases/embeds and an SLP month-range aggregation; PXL-AUD-029 fixed by resolving feature keys through the `ref_feature_definitions` embed (default-open preserved). Remaining 60 errors were behavior-neutral type friction (optional RPC args now omitted instead of null — same SQL NULL; intentional null-for-new-document args non-null-asserted; read-list nullability casts; dynamic RPC/table names typed as literal unions; `TablesInsert`/`TablesUpdate` casts on spread payloads). Corrected `PXL_ARCHITECTURE_SUMMARY.md` stack line (TanStack Query/Zustand/react-hook-form/Zod are installed but NOT adopted — documentation must never overstate reality) and added a Frontend Architecture backlog section (selective TanStack Query targets, react-hook-form form candidates, shared reference-data hooks, CI types-drift gate, Zustand adopt-or-remove, profile-before-optimizing rule). No memoization applied: no measured bottleneck exists. | Executed 2026-07-04: all 10 repaired query shapes return 200 against the local REST API (previously 400); `npx tsc -b` zero errors; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `npm test` 299/299 across 19 files (no DB change); `scripts/check_docs_consistency.sh` green. No accounting, tax, or posting behavior changed — no migration in this session. | Types regeneration after each migration is manual (`npm run gen:types`); CI drift gate recorded in backlog. Customer-return prefill now derives price from the SO line and VAT/revenue defaults from the item master — server RPC remains authoritative on save. | Resume Criticals: PXL-DA-011 status-aware immutability or PXL-DA-001 server-side GL preview. |
| 2026-07-04 | 43 | PXL-DA-011, PXL-AUD-005 | Both Retested Passed | Implemented status-aware immutability across all transactional tables (`20260704000002_status_immutability.sql`): generic `fn_guard_doc_lines` (parent-status line guard, 18 line tables including `journal_entry_lines` and `bank_recon_items`; SI/OR/VB/PV keep their PXL-AUD-005 triggers) and `fn_guard_doc_header` (diff-based header guard on 34 tables — once a document leaves its editable statuses only status, `updated_at/by`, and per-table lifecycle metadata may change; business columns frozen; DELETE blocked per DEC-002; `frozen` statuses for posted schedule entries allow no change). Same-transaction construction exception via `fn_row_written_by_current_txn` (visible row whose xmin is still in progress ⇒ written by the current transaction/subtransaction — plain xmin equality fails under plpgsql EXCEPTION blocks and pgTAP subtransactions) keeps all posting writers and the CM/DM apply RPCs working, while PostgREST clients (single-transaction calls) can never satisfy it. CM/DM total columns allowlisted because their apply path zeroes and recomputes totals from a locked status. `vat_returns` excluded (PXL-DA-015 snapshot guard governs it); `fixed_assets` guarded with draft/active editable so transfers keep working. Schema summary and `database.types.ts` regenerated. | Executed 2026-07-04: fresh `supabase db reset --local` replay through `20260704000002`; new IMMUT-001 (`supabase/tests/020_status_immutability_test.sql`, 25 assertions — the file COMMITs fixtures so tamper attempts run cross-transaction like real REST calls) passes; `npm test` 324/324 across 20 files; `npm run build` passed; `npm run lint` pre-existing 39 warnings only; `scripts/check_docs_consistency.sh` green. | Future data backfills on non-draft rows need `session_replication_role = replica`; `tax_detail_entries` direct-write posture stays under tax-ledger findings; per-table allowlists are the contract — new lifecycle columns must be added there. Hosted push of `20260704000002` pending until landing. | Next Criticals: PXL-DA-001 server-side GL preview RPC, PXL-DA-002 drilldown contracts, or PXL-DA-004 posting-engine consolidation. |
| 2026-07-04 | 44 | PXL-AUD-017 | Retested Passed | Small bounded session per user request. Context persistence: `AppContextProvider` now lazily restores company/branch/period IDs from localStorage (`pxl.ctx.*`) and persists every change; `ContextSelectors` in `AppShell` validates restored IDs against the RLS-scoped selector queries it already performs on load, clearing anything the signed-in user cannot see (company staleness cascade-clears branch/period). Refreshing the browser no longer drops working context, and a stale/foreign restored ID can never silently scope pages to an inaccessible company. No migration, no DB change. | Executed 2026-07-04: `npm run build` passed; `npm run lint` pre-existing 39 warnings only (an interim ternary-statement warning was fixed); no schema change so the 324/324 suite from session 43 stands. | URL-based deep-link context is a possible future enhancement; localStorage wrapped for storage-unavailable environments. | Next Criticals: PXL-DA-001 server-side GL preview RPC, PXL-DA-002 drilldown contracts, or PXL-DA-004 posting-engine consolidation. |
| 2026-07-04 | 45 | PXL-AUD-018 | In Progress | Small bounded session. Cleared the 10 mechanically safe lint warnings (39→29) — and the unstable-dependency subset turned out to be a real production defect: six compliance dashboards recreated `now = new Date()` every render (WT also `quarterMonths`), so their `load` useCallback changed identity each render and `useEffect([load])` refetched from Supabase in a continuous loop; pinned via `useMemo` so each dashboard loads once per company change. Ternary-as-statement Set toggles became if/else; unused `department_name` destructure `_`-prefixed. No migration, no DB change. | Executed 2026-07-04: `npm run build` passed; `npm run lint` exit 0 with 29 warnings (was 39). | Remaining 27 `useEffect` missing-dependency warnings need per-page loader stabilization verified against refetch/stale-closure behavior (not mechanical); 2 fast-refresh export notes remain. | Next Criticals: PXL-DA-001 server-side GL preview RPC, PXL-DA-002 drilldown contracts, or PXL-DA-004 posting-engine consolidation. |
| 2026-07-04 | 46 | PXL-AUD-018 | Retested Passed | Small bounded session completing the lint cleanup: 29→0 warnings, exit 0. The 27 `useEffect` missing-dependency warnings all flag the same benign idiom (`useEffect(() => { load() }, [companyId, ...])` with a per-render loader; other inputs refetch via direct handler calls, so every effect run executes a fresh closure — no stale-state bug). Each site now carries a per-line `eslint-disable` WITH explanation, which the finding's expected behavior explicitly allows; a 27-page useCallback restructure was rejected as regression risk without user value. The 2 fast-refresh only-export-components notes (`button.tsx`, `context.tsx`) suppressed with reason. Frontend comments only — zero behavior change, no migration. | Executed 2026-07-04: `npm run lint` 0 warnings exit 0; `npm run build` passed. | Lint is now a zero-warning baseline — any new warning is a visible regression. | Next Criticals: PXL-DA-001 server-side GL preview RPC, PXL-DA-002 drilldown contracts, or PXL-DA-004 posting-engine consolidation. |
| 2026-07-04 | 47 | PXL-AUD-031..PXL-AUD-049 (new) | All Open | AUDIT-ONLY session: definitive end-to-end EWT audit (no fixes). Inspected ATC/EWT master data + governance, supplier/customer withholding profiles, compliance profile gating, PV/OR/CV/cash-sale/cash-purchase withholding paths, GL impact, tax ledger + void/cancel/bounce reversal, 2307 issued/received, SAWT/QAP/1601EQ, WHT/GL reconciliation, DAT/books exports, UI defaults, and pgTAP coverage. 19 new findings (4 Critical / 9 High / 6 Medium): OR CWT base forced VAT-inclusive with the statutory net-based amount hard-rejected (AUD-031); check-voucher EWT unlinked/unvalidated and aborting quarterly 2307 generation (AUD-032) with a non-convention cancel reversal leaving phantom QAP rows (AUD-033); 1601EQ figures free-entry with no reconciliation gate (AUD-034); ATC validity checked as of CURRENT_DATE not document date (AUD-035); ATC rate changes unrepresentable under the official code (AUD-036); no accrual-basis withholding (AUD-037); PV/OR header cash totals unreconciled to lines (AUD-038); over-apply guards ignore CM/VC applications (AUD-039); 2307 lacks monthly breakdown (AUD-040); remittance JEs deadlock SAWT/QAP exports (AUD-041); withholding profile gates nothing (AUD-042); no EWT on cash purchases/advances (AUD-043); duplicate withholding flags/masters (AUD-044); gross-based UI defaults (AUD-045); receipts.total_amount semantics split (AUD-046); uncontrolled 2307-received claiming (AUD-047); tolerance mismatch 0.02 vs 0.01 (AUD-048); missing GL preview/drilldown on CV/cash pages (AUD-049). EWT maturity assessed ~55% (AP-side PV pipeline ~75%). Matrix addendum + Check Voucher row added; 9 Not-Yet-Implemented scenarios added to the test book. | Documentation only — no code/migration changes; `npm test` not run (no behavior changed); `scripts/check_docs_consistency.sh` green. | All 19 findings Open; the 4 Criticals block the withholding slice of production readiness. | Start with PXL-AUD-031 (OR CWT explicit base) — smallest Critical with the widest tax-correctness impact; then AUD-032+033 together (CV module), then AUD-034. |
| 2026-07-04 | 48 | PXL-AUD-050 (new) | Open | AUDIT-ONLY session: transaction experience architecture audit (Sales, Purchasing, JE, OR, PV, cash documents, CM/DM/VC) against the long-term PXL vision. Created `docs/PXL/PXL_TRANSACTION_EXPERIENCE_STANDARD.md` — the Phase 2 UI/UX blueprint: standard document anatomy with per-document routes (replacing the modal-overlay pattern), 13-tab standard set + Compliance Evidence tab, standard line grid with column groups (business/withholding/accounting/dimensions/reference/system) and role visibility, auto-population matrix, five-step account-determination ladder (normal users never pick GL accounts), per-type Financial Summary contracts, GL Impact + new Tax Impact panel standards, posting-validation checklist, related-documents drill contract, audit/workflow visibility, configurability phases, component inventory (GLImpactPanel/SetupReadinessBanner/StatusBadge adopted; DataTable/LookupDialog/FormSection/AuditTrailSection/ConfirmDialog/EmptyState built but unused; DocumentLayout/LineGrid/TaxImpactPanel/FinancialSummaryPanel to build), and gap analysis. Maturity vs standard: core four ~60%, JE ~40%, secondary documents ~30%, overall ~45%. One genuine defect logged (PXL-AUD-050: complete server-side audit trail invisible on every transaction page; `AuditTrailSection` exists with zero adopters). Backlog Target section now defers to the standard; UI_UX_PRINCIPLES stack notes declared aspirational (selective-adoption policy governs). | Documentation only — no code changes; `scripts/check_docs_consistency.sh` green. | Experience gaps are Phase 2 enhancements by design; PXL-AUD-050 is the only new release-relevant finding. | Resume the finding queue: PXL-AUD-031 (OR CWT explicit VAT-exclusive base) remains the next fix session. |
| 2026-07-04 | 49 | PXL-AUD-031 | Retested Passed | Receipt CWT explicit VAT-exclusive base (`20260704000003_receipt_cwt_explicit_base.sql`): `receipt_lines.cwt_tax_base` + `cwt_variance_reason`; `fn_validate_receipt_line_cwt` rewritten to PV mechanics (explicit base preferred, payment+CWT fallback for legacy/gross-convention rows, 0.02 tolerance, controlled variance reasons; old 4-arg overload dropped); trigger/ready-check/`fn_save_receipt`/`fn_post_receipt` thread the base into `tax_detail_entries`; `fn_save_cash_sale` accepts net convention first (records VAT-exclusive base), legacy gross second, rejects amounts matching neither. `ReceiptsPage` gains a net-defaulted CWT Base column (auto-tracking the VAT-exclusive proportion of the amount applied until manually overridden) + variance-reason select; `CashSalesPage` shows the statutory net CWT hint. The statutory entry (cash 11,000 + CWT 200 on base 10,000 for an 11,200 VAT invoice) — previously hard-rejected — now posts. | CWT-NET-BASE-001 executed in `supabase/tests/021_receipt_cwt_net_base_test.sql` (20 assertions). Fresh `supabase db reset --local` replays through `20260704000003`; `npm test` 344/344 across 21 files; `npm run build` green; `npm run lint` zero warnings; `npm run gen:types` regenerated; schema summary regenerated. | Pre-fix tax detail rows keep gross bases (verify SAWT before filing pre-fix periods); PV gross UI defaults under PXL-AUD-045; ATC as-of-date under PXL-AUD-035. | Next: PXL-AUD-032+033 together (check-voucher EWT validation/supplier linkage + counter-row cancel; scenario CV-EWT-2307-001). |
| 2026-07-04 | 50 | PXL-AUD-045 | In Progress | Small frontend-only slice: `PaymentVouchersPage` now fetches `vendor_bills` VAT/base totals and defaults PV `ewt_tax_base` to the proportional VAT-exclusive outstanding amount when a bill is selected, instead of defaulting to gross outstanding. Manual base override and existing variance handling remain unchanged. | `npm run build` passed; `npm run lint` zero warnings; `scripts/check_docs_consistency.sh` green. | Finding remains In Progress because SI expected CWT still does not flow to receipts and is not ATC-validated. No migration or remote Supabase push needed. | Continue recommended Critical path with PXL-AUD-032+033, or finish the remaining PXL-AUD-045 SI/OR default flow as a later medium frontend slice. |
| 2026-07-04 | 51 | PXL-AUD-050 | In Progress | Small frontend-only slice: `PaymentVouchersPage` now imports the existing shared `AuditTrailSection` and renders an Audit Evidence block for saved PVs, showing created/updated/approved/posted timestamps plus lock status, followed by lazy-loaded `sys_audit_logs` history for `payment_vouchers`. | `npm run build` passed; `npm run lint` zero warnings; `scripts/check_docs_consistency.sh` green. | Finding remains In Progress because other transaction pages still do not expose lifecycle facts or audit history. No migration or remote Supabase push needed. | Continue adopt-on-touch rollout to SI/OR/VB next, or resume the Critical path with PXL-AUD-032+033. |
| 2026-07-04 | 52 | PXL-AUD-050 | In Progress | Small frontend-only slice: `VendorBillsPage` now imports `AuditTrailSection` and renders the same saved-document Audit Evidence block used on PVs, showing created/updated/approved/posted timestamps plus lock status, followed by lazy-loaded `sys_audit_logs` history for `vendor_bills`. | `npm run build` passed; `npm run lint` zero warnings; `scripts/check_docs_consistency.sh` green. | Finding remains In Progress because remaining transaction pages still do not expose lifecycle facts or audit history. No migration or remote Supabase push needed. | Continue adopt-on-touch rollout to SI/OR next, or resume the Critical path with PXL-AUD-032+033. |
| 2026-07-04 | 53 | PXL-AUD-050 | In Progress | Small frontend-only slice: `ReceiptsPage` now imports `AuditTrailSection` and renders a saved-receipt Audit Evidence block with created/updated/posted timestamps plus lock status, followed by lazy-loaded `sys_audit_logs` history for `receipts`. | `npm run build` passed; `npm run lint` zero warnings; `scripts/check_docs_consistency.sh` green. | Finding remains In Progress because remaining transaction pages still do not expose lifecycle facts or audit history. No migration or remote Supabase push needed. | Continue adopt-on-touch rollout to SI/JE/CM/DM/VC/cash/CV next, or resume the Critical path with PXL-AUD-032+033. |
| 2026-07-04 | 54 | PXL-AUD-050 | In Progress | Small frontend-only slice: `SalesInvoicePage` now imports `AuditTrailSection` and renders a saved-SI Audit Evidence block with created/updated/approved/posted timestamps plus lock status, followed by lazy-loaded `sys_audit_logs` history for `sales_invoices`. | `npm run build` passed; `npm run lint` zero warnings; `scripts/check_docs_consistency.sh` green. | Finding remains In Progress because remaining transaction pages still do not expose lifecycle facts or audit history. No migration or remote Supabase push needed. | Continue adopt-on-touch rollout to JE/CM/DM/VC/cash/CV next, or resume the Critical path with PXL-AUD-032+033. |
| 2026-07-04 | 55 | PXL-AUD-050 | In Progress | Small frontend-only slice: `JournalEntriesPage` now imports `AuditTrailSection` and renders a saved-JE Audit Evidence block with created/updated/status/lock facts, followed by lazy-loaded `sys_audit_logs` history for `journal_entries`. | `npm run build` passed; `npm run lint` zero warnings; `scripts/check_docs_consistency.sh` green. | Finding remains In Progress because remaining transaction pages still do not expose lifecycle facts or audit history. No migration or remote Supabase push needed. | Continue adopt-on-touch rollout to CM/DM/VC/cash/CV next, or resume the Critical path with PXL-AUD-032+033. |
| 2026-07-04 | 56 | PXL-AUD-050 | In Progress | Small frontend-only slice: `CreditMemosPage` now imports `AuditTrailSection` and renders a saved-CM Audit Evidence block with created/updated/posted/status/lock facts, followed by lazy-loaded `sys_audit_logs` history for `credit_memos`. | `npm run build` passed; `npm run lint` zero warnings; `scripts/check_docs_consistency.sh` green. | Finding remains In Progress because remaining transaction pages still do not expose lifecycle facts or audit history. No migration or remote Supabase push needed. | Continue adopt-on-touch rollout to DM/VC/cash/CV next, or resume the Critical path with PXL-AUD-032+033. |
| 2026-07-05 | 57 | PXL-AUD-032, PXL-AUD-033 | Retested Passed | CV EWT hardening (`20260705000001_cv_ewt_supplier_validation.sql`): `check_vouchers.supplier_id`/`ewt_tax_base`/`ewt_variance_reason` with a REQUIRED supplier when EWT > 0; `trg_cv_ewt_validation` + post-time re-validation reuse `fn_validate_payment_voucher_line_ewt` (PV parity); `fn_post_check_voucher` writes the supplier link + master TIN/name, explicit base (fallback gross), and the ATC master rate to `tax_detail_entries`; `fn_cancel_check_voucher` switched to `fn_reverse_tax_detail_entries` counter-rows (reverses link, cancel date); legacy bare CV counter-rows backfilled + re-dated onto their reversal JE; `fn_generate_form_2307_issued` skips supplier-unlinked rows with `skipped_unlinked_count`/`skipped_unlinked_ewt` in its result instead of aborting the quarter. `CheckVouchersPage` gains supplier select, auto-tracking EWT base, variance-reason select, and the PXL-AUD-050 Audit Evidence block + `AuditTrailSection`; `Form2307IssuedPage` surfaces the skip warning. | CV-EWT-2307-001 executed in `supabase/tests/022_cv_ewt_2307_test.sql` (17 assertions). Fresh `supabase db reset --local` replays through `20260705000001`; `npm test` 361/361 across 22 files; `npm run build` green; `npm run lint` zero warnings; types + schema summary regenerated. | ATC as-of-document-date validation remains under PXL-AUD-035; legacy posted CVs keep NULL supplier links (skipped with warning at 2307 generation until relinked); pre-backfill exported QAP snapshots keep what they recorded. | Next: PXL-AUD-034 (1601EQ reconciliation gate, mirrors the VAT-return gate; pairs with PXL-AUD-041's remittance flow). |
| 2026-07-10 | 58 | PXL-AUD-034 | Retested Passed | 1601EQ reconciliation gate (`20260710000001_ewt_return_reconciliation_gate.sql`): `fn_compute_ewt_return` server-computes quarterly tax base/EWT withheld from the `ewt_payable` tax ledger; `trg_ewt_returns_status_reconciled` blocks final/filed unless figures match the ledger within 0.01, `still_due = withheld - remitted_prior` with a non-negative `remitted_prior`, and `fn_wht_gl_reconciliation` reconciles the quarter to the EWT Payable GL control account (mirrors `trg_vat_returns_status_reconciled`; metadata-only updates of a validated return pass; fires before the PXL-DA-011 guard so validated figures then freeze). `EWT1601EQReturnPage`: Generate calls the RPC; tax base/EWT withheld are read-only server-computed fields; gate hint added. | EWT-RETURN-GATE-001 executed in `supabase/tests/023_ewt_return_gate_test.sql` (12 assertions). Fresh `supabase db reset --local` replays through `20260710000001`; `npm test` 373/373 across 23 files; `npm run build` green; `npm run lint` zero warnings; types + schema summary regenerated. | `remitted_prior` stays operator-entered (arithmetic-gated only) until the PXL-AUD-041 remittance flow derives it; in-quarter remittance JEs now block 1601EQ finalization the same way they block SAWT/QAP exports — both resolve under PXL-AUD-041. 1601EQ working papers stay free-form; `fwt_returns` ungated until FWT posts tax detail. | Next: PXL-AUD-035+036 (ATC as-of-document-date validation + rate versioning) or resume the pre-existing Critical lane (PXL-DA-001 GL preview RPC). |
| 2026-07-10 | 59 | PXL-AUD-002, PXL-AUD-006, PXL-DA-001 | Retested Passed (all three) | Closed the next three Criticals. Company Setup gained a live, action-linked accounting readiness checklist and applicable transaction gates. `20260710000002_vat_registration_all_documents.sql` centralizes VAT registration/direction/amount checks across SI/VB/CM/DM/cash/VC, canonicalizes per-code CM/DM/VC tax detail, denies direct ledger mutation, and gates VAT exports. `20260710000003_posting_engine_preview_trace.sql` adds governed source types, exact rollback GL preview (including operator-dated recurring runs), source linkage, trace routes, and central period/account/balance/idempotency invariants; the shared panel/preflight now spans AR/AP, cash, treasury, inventory, fixed assets, schedules, recurring journals, and purchase returns. DA-002/004/005/007 and AUD-049 advanced; DA-006 closed. | Fresh reset replays through `20260710000003`; VAT-REG-ALL-DOCS-001 35/35 and GL-PREVIEW-PARITY-001 40/40; full pgTAP suite 448/448 across 25 files; production build and zero-warning lint pass; generated types/schema summary refreshed. Scoped Supabase dry-run listed only 00002/00003; both were pushed and `migration list --linked` verified local=remote through 00003. | Atomic create-and-post cash/fixed-asset forms use labeled client draft estimates because no saved source exists; physical source-row existence, full writer refactor/locking, and report/compliance drillback remain under DA-005/004/007/002. Unrelated local migrations 00004/00005 and test 027 were excluded from reset, deployment, docs generation, and commit. | Next Critical: PXL-DA-002 full report/compliance drillback or PXL-DA-004 posting-engine completion; DA-008/009/019 remain parallel Critical lanes. |
| 2026-07-10 | 60 | PXL-AUD-051 (new) | Open | TEST-ENVIRONMENT session, user-directed: replaced the five seeded demo companies in hosted Supabase (no transactional data existed) with **PXL Demo Trading Corporation** (HO branch, FIN department, CC-GA cost center, admin membership), then created the repeatable idempotent seed `supabase/seeds/demo_company_setup_seed.sql`: 63-account PH trading COA (cash header hierarchy, contra accounts), all 7 `company_accounting_config` mappings, FY 2026 open with 12 unlocked monthly periods (retained earnings linked to 3200-00), VAT/EWT compliance profile (quarterly VAT, RCIT 25%, SAWT/SLSP/RELIEF/QAP/1604E, no CAS/PTU claims), 4 global EWT tax codes + 5 company `ewt_codes` on current ATCs, and 27 active `<CODE>-2026-` 6-digit number series covering every UI/RPC-consumed code plus remaining registry codes. Verified: seed is idempotent (identical second run), works from scratch on fresh local reset (identical counts), all 10 Company Setup Checklist checks pass by SQL replay, and `fn_next_document_number` returns SI/OR/JE-2026-000001 under the member JWT (sequences reset to 0 after the probe). No posted transactions created; no accounting/tax/RLS logic touched. Migrations 00004/00005 and test 027 held out during the local run and restored. | `npm run build` passed; `npm run lint` zero warnings; pgTAP 448/448 across 25 files on fresh reset; `scripts/check_docs_consistency.sh` green. | One genuine defect discovered and logged (PXL-AUD-051 document-code registry drift: DM readiness vs DM-S numbering; JE/SDM/PRT/FA missing from `ref_document_types`; FA numbering unconfigurable). Unsupported-by-schema setup requested but not invented: FS/cash-flow/control-account/tax classifications on COA; bank/petty-cash/revenue/expense/inventory/COGS/returns/discounts/suspense/retained-earnings/interbranch GL mapping keys beyond the 7 existing `company_accounting_config` fields; period-13/adjustment period (period_number capped at 12); inventory (SA/ST/GI/GR/PC) and fixed-asset (FA/DISP) series codes; approval workflow left unconfigured (single test user; SoD needs a second user). | Fix PXL-AUD-051 (registry rows + DM readiness code + pgTAP registry assertion), then resume PXL-DA-002. |
| 2026-07-10 | 60 | PXL-AUD-052 (new) | Retested Passed | Session 60 continuation (test-environment master data): created idempotent seeds `supabase/seeds/demo_master_data_seed.sql` (5 payment terms COD/NET7/15/30/45; 5 customers CUST-0001..0005 with VAT/non-VAT, credit-limit, terms, CWT-agent mix and AR/PHP defaults; 5 suppliers SUP-0001..0005 with goods/professional/logistics/rental mix, EWT ATC + company `ewt_codes` defaults, natural expense GLs) and `supabase/seeds/demo_items_inventory_seed.sql` (3 FA-lifecycle COA accounts 4220-00/6170-00/6180-00; 5 UOMs incl. BOX=12 PC conversion; 4 item categories with GL defaults; 10 items — 7 inventory weighted-average, 2 services, 1 non-inventory — with VAT/account defaults; WH-MAIN/WH-TRANSIT warehouses; BPI/BDO bank accounts at 0 opening balance; 3 employees; Head Office petty cash fund; OFFEQ/FURN fixed-asset categories). Both seeds applied to hosted and local, double-run idempotent. Seeding items exposed PXL-AUD-052: `CashPurchasesPage` item lookup selected non-existent `units_of_measure.uom_name` and `items.purchase_account_id`, always rendering an empty picker; fixed in the same session via `uom_code` + `purchase_account_id:purchase_expense_account_id` alias. No stock balances or transactions created (stock must come from posted documents). | REST replay of every consuming page select (SI/CS/GI/CP items, FT/recon banks, GI warehouses) returns seeded rows; the previously failing CP shape now returns rows; `npm run build` passed; `npm run lint` zero warnings. | Master data is enhancement-scope; only the CP column drift was a defect and it is fixed and retested. Deferred by design: stock balances, exchange rates, `fwt_codes`/`percentage_tax_codes` (profile has FWT/PT disabled), approval workflows (needs a second user). | Fix PXL-AUD-051, then resume PXL-DA-002. |
| 2026-07-11 | 61 | PXL-DA-002, PXL-DA-004, PXL-DA-005, PXL-DA-007 | In Progress | Session 61 was interrupted mid-wrap-up; a same-day recovery session reconstructed, verified, fixed, and deployed its work. `20260711000001_posting_engine_completion.sql` (DA-004/005/007): registry-backed `fn_resolve_posting_source` with source-row locking and deferred-JE integrity; shared tax writer with stable source-line identity and safe uniqueness; shared posting/reversal audit and exact-opposite reversal mutation primitive; core AR/AP writers migrated to create/add/finalize + the shared tax writer with unchanged public signatures; core reversal paths and secondary saved-source writers moved onto the shared primitives; internal helpers revoked from application callers. `20260711000002_accounting_trace_reports.sql` (DA-002): canonical `source_doc_type`/`source_doc_id` on customer/supplier ledger and VAT/EWT/CWT review views; fail-closed `fn_get_accounting_trace` routing to the new generic read-only `/accounting-source` page; read-only `fn_get_report_snapshot_trace_links`; membership-scoped `fn_get_report_trace_set`. Drillback UI adopted across GL, account detail ledger, FS/comparative statements, AP/AR aging, VAT summaries/review, SAWT/QAP/2307, and report snapshots. Recovery fixed one defect in the interrupted migration draft (`v_ref.date_column` → `document_date_column` in `fn_resolve_posting_source`; only proven by fresh-reset replay because the session had applied a corrected version ad hoc). | Fresh local reset through `20260711000002` with unowned migrations 00004/00005 and test 027 held out; full pgTAP **474/474 across 26 files** (updated `025` 40/40; new `026` ACCOUNTING-TRACE-REPORTS-001 26/26 incl. fail-closed orphan/cross-company trace fixtures); `npm run build` passed; `npm run lint` zero warnings; types and scoped schema summary regenerated; `scripts/check_docs_consistency.sh` green (72 findings, 26 test files); hosted push of both migrations verified by `supabase migration list --linked` (local=remote through 20260711000002) and remote function probe. | User directed the push (2026-07-11) and chose to hold the incomplete ATC/CAS migrations 00004/00005 out of hosted and Git after evidence that 00005's `fn_next_document_number` reservation model breaks test 021 and its own test 027 fails 15/30. Finding statuses deliberately unchanged pending a dedicated retest pass. | Retest and decide DA-002/004/005/007 statuses; then PXL-AUD-051 or Criticals DA-008/DA-009/DA-019. |
