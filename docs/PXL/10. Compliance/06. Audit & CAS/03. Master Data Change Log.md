# Master Data Change Log

## 1. Module Overview
The Master Data Change Log is a strict requirement for Computerized Accounting System (CAS) compliance. It automatically records system events, ensuring an immutable audit trail that pulls context from user sessions and accounting operations. Data is heavily dependent on accurate Setup configurations and posted Journal Entries.

## 2. Dashboard UI
### The Action Bar
* **Search:** Find specific records by reference or period.
* **Filter:** Filter by Date Range, Status, or Type.
* **Export:** Download to Excel/CSV for BIR submission or offline review.
* **Import:** Import external adjustments or offline templates.
* **Generate:** Pull latest data from Accounting ledgers.

### The Data List
Displays generated records with:
* Reference / Period
* Total Amounts
* Status (Draft, Final, Filed)
* Actions (View, Edit, Print)

## 3. Data Fields
### Header Information
| Field Name | Description | Required? | UI Component |
| :--- | :--- | :--- | :--- |
| Log Date | Date the log was generated. | Yes | Date Picker |
| Log Type | Category of the audit event. | Yes | Text Input |

### Line Items
| Field Name | Description | Required? | UI Component |
| :--- | :--- | :--- | :--- |
| Timestamp | Exact time of the event. | Yes | Text Input |
| User ID | The user who performed the action. | Yes | Dropdown linked to parent |
| Action Taken | Detailed description of the operation. | Yes | Text Input |
| Old Value | Previous state of the data (if applicable). | Optional | Text Input |
| New Value | New state of the data. | Yes | Text Input |

## 4. Supabase Database Architecture
Data is structured into Header and Line-Item tables to maintain proper normalization and support BIR CAS standards.

**Critical Database Rule:** A Unique constraint applies to `company_id` + `period` to prevent duplicate reports for the same period.

### Table 1: `compliance_master_data_change_log_headers`
| Column Name | Data Type | Rules | What it stores |
| :--- | :--- | :--- | :--- |
| `id` | UUID | Primary Key | System ID. |
| `company_id` | UUID | Required, Foreign Key | Links to Setup module. |
| `period` | Date | Required | Reporting period. |
| `status` | Text | Required | Draft, Final. |
| `created_by` | UUID | Required, Foreign Key | User ID. |
| `updated_by` | UUID | Nullable, Foreign Key | User ID. |
| `created_at` | Timestamp | Auto | Date created. |
| `updated_at` | Timestamp | Auto | Date updated. |

### Table 2: `compliance_master_data_change_log_lines`
| Column Name | Data Type | Rules | What it stores |
| :--- | :--- | :--- | :--- |
| `id` | UUID | Primary Key | System ID. |
| `header_id` | UUID | Required, Foreign Key | Links to `compliance_master_data_change_log_headers`. |
| `log_identifier` | Text | Required, Unique | Unique identifier for the log event. |
| `amount` | Numeric | Required | Item amount. |
| `created_by` | UUID | Required, Foreign Key | User ID. |
| `updated_by` | UUID | Nullable, Foreign Key | User ID. |
| `created_at` | Timestamp | Auto | Date created. |
| `updated_at` | Timestamp | Auto | Date updated. |
