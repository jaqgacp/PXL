# AI Handoff

Last updated: 2026-07-04

## What Was Done

Small cleanup after Session 49 (2026-07-04): refreshed `README.md` so the stack summary says React 19, the migration instructions use the linked Supabase CLI path, and the database migration section points to the generated schema summary instead of a short hand-written migration list. `AI/AI_STATE.md` no longer lists README drift as a broken area.

Small AIQ-010 cleanup (2026-07-04): added a Quick Orientation section near the top of `docs/PXL/PXL_TRANSACTION_MATRIX.md`, summarizing the current EWT risk lane, mature core transaction surfaces, secondary-page caution areas, reporting snapshot evidence, and matrix/audit/test-book sync discipline. Marked AIQ-010 Done; the audit findings doc already has its status index and readiness gate.

Small AIQ-006 cleanup (2026-07-04): created `docs/PXL/PXL_ACCOUNTING_RULES.md`, a compact accounting rules summary covering double-entry, account determination, immutability, source links, period locks, dimensions, tax/GL reconciliation, snapshots, and source docs. Indexed it in `AI/AI_CONTEXT_INDEX.md` and marked AIQ-006 Done.

Session 50 (2026-07-04, small audit fix): PXL-AUD-045 moved Open -> In Progress. `PaymentVouchersPage` now fetches `vendor_bills.total_input_vat_amount` and VAT-exclusive base totals, then defaults `ewt_tax_base` to the proportional VAT-exclusive outstanding amount when a bill is selected. Example: a VAT bill for 11,200 gross / 10,000 net defaults to 10,000 base at full settlement instead of 11,200. Manual override and variance handling remain unchanged. Frontend-only; no migration. Remaining AUD-045 scope: SI expected CWT still needs ATC validation and flow into receipt prefill. Evidence: `npm run build`, `npm run lint`, and `scripts/check_docs_consistency.sh` passed. Standing: 28 Retested Passed / 14 In Progress / 28 Open (70 findings).

Session 51 (2026-07-04, small audit fix): PXL-AUD-050 moved Open -> In Progress. `PaymentVouchersPage` now renders an Audit Evidence block for saved PVs with created/updated/approved/posted timestamps and lock status, then lazy-loads the existing shared `AuditTrailSection` for `payment_vouchers` history. Frontend-only; no migration. Remaining AUD-050 scope: roll the same lifecycle facts + audit history pattern across the other transaction pages. Evidence: `npm run build`, `npm run lint`, and `scripts/check_docs_consistency.sh` passed. Standing: 28 Retested Passed / 15 In Progress / 27 Open (70 findings).

Session 52 (2026-07-04, small audit fix): continued PXL-AUD-050. `VendorBillsPage` now renders the same Audit Evidence block for saved VBs with created/updated/approved/posted timestamps and lock status, then lazy-loads `AuditTrailSection` for `vendor_bills` history. Frontend-only; no migration. Remaining AUD-050 scope: roll the pattern across the other transaction pages. Evidence: `npm run build`, `npm run lint`, and `scripts/check_docs_consistency.sh` passed. Standing unchanged at 28 Retested Passed / 15 In Progress / 27 Open (70 findings).

Session 53 (2026-07-04, small audit fix): continued PXL-AUD-050. `ReceiptsPage` now renders a saved-receipt Audit Evidence block with created/updated/posted timestamps and lock status, then lazy-loads `AuditTrailSection` for `receipts` history. Frontend-only; no migration. Remaining AUD-050 scope: roll the pattern across the other transaction pages. Evidence: `npm run build`, `npm run lint`, and `scripts/check_docs_consistency.sh` passed. Standing unchanged at 28 Retested Passed / 15 In Progress / 27 Open (70 findings).

Session 49 (2026-07-04): CLOSED PXL-AUD-031 (Critical — receipt CWT base was forced VAT-inclusive and the statutory net-based CWT was hard-rejected). `20260704000003_receipt_cwt_explicit_base.sql`: `receipt_lines.cwt_tax_base` + `cwt_variance_reason` (nullable — legacy rows keep the payment+CWT fallback so history and genuine gross-convention withholding stay valid); `fn_validate_receipt_line_cwt` rewritten to PV parity (explicit base preferred, 0.02 tolerance, controlled variance-reason list; the old 4-arg overload is DROPPED — callers must use the 6-arg form); trigger + `fn_validate_receipt_cwt_ready` + `fn_save_receipt` thread the columns; `fn_post_receipt` writes the explicit base to `tax_detail_entries` (SAWT income payments now VAT-exclusive for new rows); `fn_save_cash_sale` determines the base net-first (VAT-exclusive), gross-second, and rejects CWT matching neither convention with both expected values. `ReceiptsPage`: new "CWT Base (net of VAT)" column defaulted to the invoice's VAT-exclusive proportion of the amount applied (auto-tracks payment/CWT edits until the user overrides the base), variance-reason select on rate mismatch, payload carries both fields. `CashSalesPage`: statutory net CWT hint under the ATC select. Evidence: CWT-NET-BASE-001 executed (`supabase/tests/021_receipt_cwt_net_base_test.sql`, 20 assertions — statutory OR posts balanced JE with tax base 10,000 on an 11,200 invoice; gross fallback still validates; PV-parity variance mechanics; proportional partial base; cash-sale net/gross/neither). Fresh reset replays through `20260704000003`; `npm test` 344/344 across 21 files; build green; lint zero warnings; `npm run gen:types` + schema summary regenerated. NOTE: pre-fix tax detail rows keep their gross bases by design (no backfill) — re-verify SAWT before filing pre-fix periods.

Session 48 (2026-07-04, AUDIT-ONLY per user direction): transaction experience architecture audit — no code changes. Created `docs/PXL/PXL_TRANSACTION_EXPERIENCE_STANDARD.md`, the blueprint for Phase 2 UI/UX modernization (scheduled only after current audit findings close, per DEC-012). Contents: normative precedence (behavior docs > standard > UI_UX_PRINCIPLES aspirational stack notes — the selective-adoption policy from session 42 governs, never mass react-hook-form/Zustand migrations); verified current state (all transactions are list+full-screen-modal, no per-document routes, no tabs; SetupReadinessBanner/GLImpactPanel only on SI/OR/VB/PV; `DataTable`/`LookupDialog`/`FormSection`/`AuditTrailSection`/`ConfirmDialog`/`EmptyState` exist in `ui/shared.tsx` with ZERO adopters while `StatusBadge` is on 54 pages); target document anatomy with routes; 13-tab standard set + recommended Compliance Evidence tab (per-document report_snapshots drillback); standard line grid (business/withholding/accounting/dimensions/reference/system column groups, R/O/H/X per transaction, role visibility); auto-population matrix (enter-once rule, provenance display); five-step account-determination ladder (users stop picking GL accounts; JE is the deliberate exception); Financial Summary contracts per type (withholding lines on VAT-exclusive bases, consistent with the session-47 EWT findings); GL Impact standard (server preview via PXL-DA-001, JE drill link); new TaxImpactPanel standard (build only AFTER AUD-031/032/033 so it doesn't display defective bases); posting-validation checklist; related-documents/drill contract (PXL-DA-002 owns enforcement); audit/workflow visibility; configurability phases; component build order (DocumentLayout first, pilot on Sales Invoice). One genuine defect: PXL-AUD-050 (Medium, Open) — audit evidence invisible on every transaction page despite complete data layer and an existing unused component. Backlog Target section now points to the standard as authoritative; AI_CONTEXT_INDEX UI Mode lists it. Maturity: core four ~60%, JE ~40%, secondary ~30%, overall ~45%. `scripts/check_docs_consistency.sh` green (70 findings).

Session 47 (2026-07-04, AUDIT-ONLY per user direction): definitive end-to-end EWT audit — no fixes, no code/migration changes, documentation only. Inspected the full withholding surface: ATC master + effective-date governance, ewt_codes/fwt_codes, supplier/customer withholding profiles, compliance-profile gating, PV/OR/CV/cash-sale/cash-purchase flows, GL posting, tax ledger + void/cancel/bounce conventions, 2307 issued/received, SAWT/QAP/1601EQ, WHT/GL reconciliation + export snapshots, books/DAT exports, UI defaults, and pgTAP coverage. Output: 19 new findings PXL-AUD-031..049 (4 Critical / 9 High / 6 Medium) with full detail sections in `docs/PXL/PXL_END_TO_END_AUDIT_FINDINGS.md`; a session-47 EWT risk addendum + new Check Voucher transaction row in `docs/PXL/PXL_TRANSACTION_MATRIX.md`; 9 recommended Not-Yet-Implemented scenarios in `docs/PXL/PXL_ACCOUNTING_TEST_BOOK.md`. `scripts/check_docs_consistency.sh` green (69 findings; book matches 20 test files). EWT maturity ~55%. Key results: the AP-side PV pipeline (explicit base, variance reasons, partial payments, void counter-rows, 2307 generate/supersede/snapshots, QAP snapshots + GL reconciliation) is the strong core; the Critical gaps are the OR CWT VAT-inclusive base that REJECTS the statutorily correct net-based CWT (AUD-031), the check-voucher EWT path (unvalidated, supplier-unlinked → aborts the whole quarter's 2307 batch; cancel leaves phantom QAP rows — AUD-032/033), and the ungated 1601EQ return (AUD-034). High findings: ATC as-of-CURRENT_DATE validation (035), rate-versioning impossible under the official code (036), no accrual-basis withholding (037), header/line cash totals unreconciled (038), over-apply guards ignore CM/VC (039), 2307 lacks monthly breakdown (040), remittance JEs deadlock SAWT/QAP exports (041), dead withholding profile toggles (042), no EWT on cash purchases/advances (043).

Session 46 (2026-07-04, small bounded session): PXL-AUD-018 CLOSED — `npm run lint` now reports ZERO warnings (exit 0), so any new warning is a visible regression. The 27 `useEffect` missing-dependency warnings all flagged one benign idiom (`useEffect(() => { load() }, [companyId, ...])` with a per-render loader; other inputs refetch via direct handler calls, so each effect run executes a fresh closure — no stale-state bug); every site carries `// eslint-disable-next-line react-hooks/exhaustive-deps` WITH an explanation, which the finding's expected behavior explicitly allows. A 27-page useCallback restructure was rejected as regression risk without user value. The 2 fast-refresh only-export-components notes (`button.tsx`, `context.tsx`) are suppressed with reason. Comments only — zero behavior change, no migration; hosted Supabase unchanged (synced through `20260704000002`).

Session 45 (2026-07-04, small bounded session): PXL-AUD-018 partial (39→29 lint warnings, exit 0). The 7 unstable-dependency warnings were a real production defect: six compliance dashboards (`BooksDashboardPage`, `CASDashboardPage`, `IncomeTaxDashboardPage`, `PTDashboardPage`, `VATDashboardPage`, `WTDashboardPage`) created `now = new Date()` (WT also `quarterMonths`) on every render → the `load` useCallback changed identity every render → `useEffect([load])` refired after every render → continuous Supabase refetch loop. Fixed by pinning `now` at mount with `useMemo(() => new Date(), [])` (and deriving `quarterMonths` via `useMemo`). Also: ternary-as-statement Set toggles in `DepreciationRunPage`/`CollectionMonitoringPage` → if/else; unused `department_name` destructure in `EmployeesPage` `_`-prefixed. Frontend-only, no migration; hosted Supabase unchanged (synced through `20260704000002`). Remaining for AUD-018: 27 `useEffect` missing-dependency warnings (`load`/`loadHeaders`/`fetchLogs`/`fetchEvents`) — stabilize each page's loader with `useCallback` and verify no refetch loop/stale closure per page; plus 2 fast-refresh only-export-components notes (`context.tsx`, `shared.tsx`).

Session 44 (2026-07-04, small bounded session): closed PXL-AUD-017 — `AppContextProvider` restores company/branch/period from localStorage (`pxl.ctx.*`) and persists changes; `ContextSelectors` validates restored IDs against its RLS-scoped selector queries and clears anything invisible to the signed-in user (company staleness cascade-clears branch/period). Frontend-only; build/lint green; no migration so hosted Supabase needs no push (still synced through `20260704000002`).

Session 43 (2026-07-04): implemented PXL-DA-011 status-aware immutability, which also closed the PXL-AUD-005 residue.

- `20260704000002_status_immutability.sql`: two generic SECURITY DEFINER trigger guards. `fn_guard_doc_lines(parent, fk, status_col, editable_csv, same_txn)` blocks line INSERT/UPDATE/DELETE unless the parent status is editable — applied to 18 line tables (all sales/purchasing/banking/inventory line tables, `bank_recon_items`, `journal_entry_lines`; SI/OR/VB/PV keep their PXL-AUD-005 triggers). `fn_guard_doc_header(status_col, editable_csv, extras_csv, frozen_csv, same_txn)` freezes business columns once a document leaves its editable statuses — only status, `updated_at/by`, and per-table lifecycle metadata (posting stamps, JE linkage, void reason, PV/CV release/clear dates, CM/DM totals for the apply path, VC `remaining_balance`, PCV `replenishment_id`, schedule `posted_periods`, returns filing metadata) may change; DELETE outside editable statuses always blocked (DEC-002); `frozen` statuses (posted amortization/depreciation/rev-rec entries) allow no change at all. Applied to 34 header tables. `vat_returns` excluded (PXL-DA-015 snapshot guard governs it).
- Same-transaction construction exception: `fn_row_written_by_current_txn(xmin_raw)` — a VISIBLE row whose xmin transaction is still `in progress` can only have been written by the current transaction or its subtransactions (PostgreSQL never exposes other transactions' uncommitted rows). Plain `xmin = txid_current()` fails under plpgsql EXCEPTION blocks and pgTAP assertions (subtransaction xids), which is why `txid_status` is used. This keeps every posting writer (posted JE header inserted before its lines), the purchase-return create-then-delete JE flow, and the CM/DM apply RPCs (which zero and recompute totals from a locked status) working — while PostgREST clients, whose every call is a single transaction, can never satisfy it.
- Diff-based header checking: only genuinely CHANGED columns are validated, so UI full-payload re-saves of unchanged values pass (SO cancel, quotation transitions).
- New test `supabase/tests/020_status_immutability_test.sql` (IMMUT-001, 25 assertions). It intentionally COMMITs its fixtures so tamper attempts run cross-transaction like real REST calls — this is the only test file that commits; always `supabase db reset --local` before `npm test`.
- Test book gained IMMUT-001; schema summary and `database.types.ts` regenerated; findings index/rows/log updated.

## What Changed

- Findings standing: 28 Retested Passed / 15 In Progress / 27 Open (70 findings); 11 Criticals remain (AUD-002, AUD-006, AUD-032, AUD-033, AUD-034, DA-001, DA-002, DA-004, DA-008, DA-009, DA-019). Session 49 closed AUD-031; do not redo it. Session 50 partially fixed AUD-045's PV default slice; session 51 partially fixed AUD-050 on PV audit evidence.
- New architecture standard: `docs/PXL/PXL_TRANSACTION_EXPERIENCE_STANDARD.md` governs how transaction pages should look/expose information; adopt-on-touch only, Phase 2 for structural work. The backlog's Target section defers to it.
- Lint is a zero-warning baseline as of session 46 — keep it that way; new warnings are regressions.
- `npm test` is now 344/344 across 21 files.
- NEW MIGRATION DISCIPLINE: the guards fire for superuser too. Future backfills that rewrite non-draft documents/lines need `SET session_replication_role = replica` (or targeted `ALTER TABLE ... DISABLE TRIGGER`) around the backfill. New lifecycle columns on guarded tables must be added to that table's allowlist in its guard trigger.
- No accounting/tax/posting behavior changed for legitimate flows — all 299 pre-existing assertions still pass unchanged.

## What Remains

- Next Criticals: PXL-DA-001 server-side GL preview RPC (In Progress), PXL-DA-002 drilldown/drillback contracts, PXL-DA-004 posting-engine consolidation.
- PXL-DA-011 residues (documented in the finding): `tax_detail_entries` direct-write posture continues under tax-ledger findings; CM/DM total columns stay lifecycle-mutable because the apply RPCs zero/recompute them (lines remain guarded, totals always recomputable).
- Summary docs AIQ-006–007 when audit work pauses.

## Known Errors / Blockers

None locally: fresh replay through `20260704000003` + `npm test` 344/344 across 21 files (reset the local DB first — test 020 commits fixtures by design), build/lint/docs-consistency green. Session 49 landed as `e30de22`; hosted is fully in sync through `20260704000003`, verified via `supabase migration list --linked`.

## Exact Next Recommended Task

Continue `AIQ-008` with PXL-AUD-032+033 together (check-voucher EWT validation/supplier linkage + counter-row cancel; scenario CV-EWT-2307-001). Then PXL-AUD-034 (1601EQ reconciliation gate). Do not redo PXL-AUD-031. Remember: `npm run gen:types` after every migration; backfills on non-draft rows need the replica-role escape hatch.

If the user asks for another small documentation item instead of audit work, AIQ-007 is the next summary doc: create `docs/PXL/PXL_TAX_RULES_PH.md`.

## Exact Next Prompt

```text
Continue autonomously from the AI operating files.

Read:
- AI/AGENT_SYSTEM_PROMPT.md
- AI/AI_STATE.md
- AI/AI_HANDOFF.md
- AI/AI_WORK_QUEUE.md
- AI/AI_CONTEXT_INDEX.md
- AI/AI_DECISIONS.md

Pick the highest-priority unblocked task and execute it.
Do not ask me to re-explain PXL unless the documents are missing or conflicting.
Before ending, update AI/AI_STATE.md, AI/AI_HANDOFF.md, and AI/AI_WORK_QUEUE.md.
```
